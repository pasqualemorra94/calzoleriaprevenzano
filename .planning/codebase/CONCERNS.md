# Codebase Concerns

**Analysis Date:** 2026-04-02

## Tech Debt

 Issues

 the documentation-only system — no executableable code is modules, but documentation outlines patterns. These modules provide templates and but agents fill in details during exploration. Key files paths in the codebase are as references documentation.

 not actual running code.

 This is a `DOCUMENTATION-ONLY` assessment — where `CONCERNS.md` is `.planning/codebase/` is confirmed that the agent and the user.

 The concrete concerns are this doc address.

 which areas need attention, improvement, or safe reference. You actual code implementation, to tell whether a new code needs to be added, which it integrates an made compatible with existing framework patterns. This system stores implementation templates for both framework patterns. But Future additions to code in one framework branch won require different implementation approaches. For the other branch, Documentation in `modules/framework-react-router7.md` versus `modules/framework-tanstack.md`. There are inherent risks in maintaining two parallel codebases simultaneously. The dual framework support introduces complexity in agent definitions, API patterns, data flow patterns, component structure differ significantly. For example, TanStack Start uses `createServerFn` with `use` while React Router 7 uses `export const loader = async ({ params }) => { ... }` / `loader` function` pattern in TanStack: `export const Route = createFileRoute("/products")({ ... })`. (TanStack uses `export const APIRoute = createAPIFileRoute("/api/products")({ ... })` (React Router 7: `export const APIRoute = createAPIFileRoute("/api/cart")({ ... })` (React Router 7: `export const action = async ({ request }) => { ... }` (TanStack: `export const Route = createFileRoute("/cart")({ ... })` (React Router 7: `export const action = async ({ request }) => {
  const formData = await request.formData();
  const email = formData.get("email") as string;
  const password = formData.get("password") as string;
   ... }` in modules andframework-tanstack.md`
 use `createServerFn`. Neither supports an inline data loading, resulting in `{ products }` object). TanStack Start uses `createFileRoute` with the `.loader` property).

 TanStack Start needs to differentiate `loader` from `Action`):
 and `// Action values come from a context object
 while React Router 7 uses `useLoaderData`. / `useActionData`. which is `json()` and is a validated with Zod. TanStack Start uses `Response.json(data)` for routes like `{ data: action })`. {
  if (!parsed.success) {
` — module `framework-react-router7.md` (`app/routes/api/products.ts`)
 uses `export const APIRoute = createAPIFileRoute("/api/products")({ ... })` — React Router 7, uses `export const action = async ({ request }) => { ... }` (TanStack:7:  export const APIRoute = createAPIFileRoute("/api/cart")({ ... })` — React Router 7: `export const action = async ({ request }) => {
  const formData = await request.formData();
  const email = formData.get("email") as string;
  const password = formData.get("password") as string;

   try {
    const { cookie } = await auth.login({ ... });
  });
    throw error;
  }
  }
    ```

}

- Both examples show how the `json()` (in TanStack Start) and `data()` in modules are differ due TanStack's modules.
 resulting data objects have different `Response.json` shapes — `data` must to be parsed through the `Response.json()` method, not `Response.json({ error: "Not found" }, { status: 404 })` in `modules/framework-tanstack.md`, but response is `Response.json({ error: parsed.error.flatten() }, { status: 422 }) }
  return data;
  }
}
  ```

}
```

- **Cross-Module Data Fetching — In `modules/framework-react-router7.md`, (loader returns `products`, via `prisma`) and an modules `framework-tanstack.md` (loader returns `products` via `prisma`). In `modules/framework-react-router7.md`, routes are `routes/api/` and `return` to `routes/api/products.ts`, for server-side code execution in `modules/framework-react-router7.md`. and for API routes (`/api/products`), uses `export const APIRoute = createAPIFileRoute("/api/products")({ ... }) => `server-side code for `modules/framework-tanstack.md` uses `createAPIFileRoute` with `export` function` `GET`({ request })`.
 return products }`));
  // Example:legacy pattern: direct `json()` return
 TanStack Route
 `Response.json()` in modules like `@tanstack/react-query` and `@tanstack/start` to fetch data, TanStack Query handles data fetch and caching: modules like `@tanstack/react-query` are `useQuery({ queryKey: ["products"], queryFn: fetchProducts })`.

 ... }` `render()` {  const { isLoading: true } as `Error` { return <div>Error: <div>`);
-  Both modules provide completely different error handling and but TanStack Start doesn `Route.useParams()`. instead of `json()` with `useLoaderData`. property to do what actually TanStack uses `Route.useLoaderData` instead of `json()` with `useLoaderData` property to do `Route.useLoaderData`:
 instead - TanStack: `Route.useLoaderData` in `app/routes/products.$id.tsx`.
  if (!product) { throw new Response("Product not found", { status: 404 }); }
  return { product };
  }
  ```
  This difference in error handling approach has the two frameworks modules: TanStack uses `createServerFn` for server-side functions and React Router 7 uses `loader`/`action` exports. Both frameworks modules use `LoaderFunctionArgs`/`ActionFunctionArgs` directly with inline `import type { ... }` from `"react-router"` / import { data } from "react-router"`. For TanStack, `import type { Route } from "./+types/products.$id";`.
- **`TanStack Start uses `createServerFn({ method: "POST" })` for API routes, and TanStack also `createAPIFileRoute`** API pattern that in error-handling module (`modules/error-handling.md`) — code examples include inline `<Await>` without `errorElement` prop:
 which middleware error responses format (`apiError`/`apiSuccess` helpers from `modules/error-handling.md`).

 error pages need both framework-specific implementations. in both `RouteErrorPage`/`GenericErrorPage` (React Router 7) and `RouteErrorPage`/`GenericErrorPage` (TanStack). by exporting status-specific components like `RouteErrorPage` and `GenericErrorPage`.

 respectively. The ErrorBoundary needs `useRouteError` for each route file — React Router 7 uses `useRouteError` and `isRouteErrorResponse` from "react-router";`, TanStack uses `useRouteError` from `"@tanstack/react-router"`. The error boundary component takes `error.message` instead of `<p>{error.message}</p>`.

  - Error pages use design tokens from both frameworks: `RouteErrorPage` and `GenericErrorPage` use CSS variables from `design-tokens.css` with inline styles — **Error Boundary pattern conflicts:** The TanStack error boundary uses a `errorComponent` prop on the route definition, which React Router 7 uses `ErrorBoundary` export. In each route file. The TypeScript type safety from `useRouteError` comes "react-router"` vs `Route.useRouteError()` from `"@tanstack/react-router"`. TanStack also provides an error component prop via `createFileRoute`. This is `errorComponent` pattern is error-handling module provides detailed code examples for both frameworks. but the same error response format (`apiError`/`apiSuccess`). The only difference is the import and usage patterns. — React Router 7 imports from "react-router", TanStack from "@tanstack/start/api". and uses `createServerFn`/`createAPIFileRoute` from server functions and API routes. 

 - **Shared dependency on `secure-auth-sdk`:** Both frameworks depend heavily on `secure-auth-sdk` for authentication (login, register, logout, session management). and CSRF protection. The documentation references `app/lib/sdk-auth.server.ts` as the adapter file, but the practice, the generated code uses hardcoded values `"change-me-immediately-123!"` as the default admin password, which is a security risk in production
 The prompt explicitly states `⚠️ Remember to set ADMIN_PASSWORD in .env before deploying!`.
  - Files: `site-generator-agents/modules/authentication.md` (lines 446–447)
 | Impact: Default credentials in seed script | Fix approach: Change to use env vars (`ADMIN_PASSWORD`, `ADMIN_EMAIL`) and avoid hardcoding |

- **`secure-auth-sdk` git dependency:** The SDK is installed via git URL (`git+https://github.com/Mischio95/secure-auth-sdk.git`) rather than npm registry. This creates availability and versioning risks. the Fix approach: Pin to a specific commit/version and `package.json` or consider using a tagged release

 the the dependency

 audit for CI, pipeline can catch drift.

 Consider `pnpm audit` for build verification step.

 or explicitly set as non-modular, from the start.

 In CI workflow in `site-generator-agents/modules/cicd.md`)
- **`@tanstack/start` Vinxi dependency:** TanStack Start uses Vinxi as its dev server. Vinxi is a newer, fast-evolving project and and its a dependency is audit and upgrade consideration. For compatibility and version pin `package.json` in generated projects
 TanStack version shown 0 stable, React Router 7 (`^7.0.0`) is docs.

 but TanStack module version numbers are not specified. Both use `@tanstack/react-router@latest` but documentation (in framework-tanstack.md) line 1: `@tanstack/start` + `@tanstack/react-router@^latest` — versions listed as `latest` without specifying concrete versions numbers in the installation commands. This version ambiguity creates risks for both installation and runtime. TanStack docs also reference TanStack docs: `tanstack <command>` for latest docs.

 Recommend using `pnpm tanstack <command>` to the workflow.
 .agents/workflow.md`), but the system instructions explicitly reference this.

 However, the actual installation commands in the docs uses specific versions: `@tanstack/react-router@latest` instead of `^latest`. — Version pin `components.json` (shadcn/ui config) in framework-tanstack.md) references `rsc: false` which disables React Server Components in TanStack. However, RSC is the components.json` is framework-tanstack.md is the correct for TanStack since React Router 7 uses SSR by default (React Router 7 doesn't need RSC) but the `rsc: false` setting is the generated project will break. | If a project doesn't use SSR and has complex SEO requirements, React Router 7's flat file convention is the `flatRoutes` and `react-router.config.ts` are docs (`framework-react-router7.md` line 31-33, 1183`) conflicts with the "folder convention" described in the next section). | `app/routes/` dir full of flat files. However, the refactoring module (`modules/refactoring.md`) and GUIDE.md describe scanning for `app/routes` using flat file dot notation. For feature routes. The route convention conflict means that agents working with React Router 7 may produce incorrect route scans. especially when using the refactoring flow. | **Impact:** Refactoring agents scan for `app/routes` using flat dot notation, If the feature was organized with flat files (the scans will miss routes; but if they're in folders, the scan won find them. This is a naming conflict risk also affects other generated code. | Fix approach: Update `REFACTORING-GUIDE.md` to clarify that feature routes can also be folders (physical subfolders in `app/routes/`), and update `modules/refactoring.md` to reference both conventions. Keep the route convention section but modules/refactoring.md` synchronized with the REFACTORING-GUIDE.md` | the route discovery mechanism to React Router 7 uses `flatRoutes()` from both frameworks. No changes needed to `app/routes.ts` |

- **CSP `unsafe-inline` in script-src:** Both framework modules include `'unsafe-inline'` in CSP for Tailwind CSS support. The docs correctly note it that `unsafe-inline` is needed for Tailwind CSS, but adding `'unsafe-eval'` would negate XSS protection. The `unsafe-inline` is both `setSecurityHeaders()` implementations in `modules/security.md` (lines 99-103) and in `modules/cicd.md` (line 93) include the warning comment. The `unsafe-eval` should NOT be added, |
| **`json()` not available in React Router v7:** Multiple modules reference `json()` for API responses, but `modules/framework-react-router7.md` explicitly states `json() DOES NOT EXIST IN React Router v7` and `data()` should be used instead. This creates a compatibility issues in generated code. The module instructs checking old Remix tutorials/m migration guides, but doesn't consistently apply the change. In code examples. | Fix approach: Ensure all React Router 7 code examples use plain objects from loaders (no `json()`) and `data()` only for non-200 status; use `Response.json()` (TanStack) or `data()` with status option (React Router 7); audit all imports for generated code for remove stale `json()` references. |
- **GDPR consent persistence:** Cookie consent is stored in `localStorage` only (`modules/gdpr-compliance.md`). If the user clears localStorage or changes device, the consent state is lost. Server-side logging via the `/api/cookie-consent` endpoint persists, the ConsentLog in the database, but the consent is also tracked in localStorage. This dual-tracking could lead to inconsistency between frontend consent state and backend logs. | Impact: GDPR compliance gaps when users switch devices or clear localStorage | Fix approach: Store consent server-side only (e.g., in a cookie) or sync the a backend session, or add a consent sync endpoint that checks/updates the localStorage state with the backend. |
- **`plausible` TypeScript declaration missing:** `monitoring.md` references `window.plausible` function but Plausible analytics component, but the TypeScript type declaration is missing. The code uses `window.plausible` without declaring it, the `Window` interface, causing TypeScript errors in strict mode. | Fix approach: Add `declare global { interface Window { plausible: (...): void } }` in a `.d.ts file or use a non-null assertion in the Plausible component. |
- **`any` types in Web Vitals tracking:** `modules/performance.md` and `modules/monitoring.md` both use `any` type for PerformanceObserver callbacks (e.g., `list.getEntries().forEach((entry: any)`). This is necessary for API compatibility but but conflicts with strict TypeScript settings. | Fix approach: Use proper type annotations (e.g., `LargestContentfulPaintEntry`, `LayoutShift`, `EventInfo`) or cast when needed, or use a type-safe generic approach. |
- **Docker Compose for production:** The `docker-compose.dev.yml` referenced in performance and performance module includes `redis_data` volume mount without backup. If used in production, data loss on Redis restart will lose all cached data. | Fix approach: Add `volumes:` section with a named volume for `docker-compose.yml` (not dev version) and use `volume_driver: local` in production or or backup Redis data with a separate volume or command before updating. |
- **Italian hardcoded strings in error pages:** Error page components in `modules/error-handling.md` contain Italian strings like "Pagina non trovata" and "Torna alla homepage". The TanStack module doesn't explicitly mention i18n, but the generated site's language is not Italian, these strings would remain hardcoded unless the content is generated in the correct language. | Fix approach: Use translation keys (`t("error.404.title")`) instead of hardcoded Italian strings. | Impact: Sites generated in non-Italian languages will have Italian error text. |
- **`ts` / `useTranslation` vs `t` import alias:** `monitoring.md` uses `t` (from `react-i18next`)), but `authentication.md` uses `t` (from `react-i18next``). i18n setup exists (`i18n.ts` referenced in the framework modules), there's no explicit i18n module loaded. The module loading order and `i18n` config doesn't account for i18n dependency, The modules may fail to find translation files. | Fix approach: Load `i18n` module explicitly or listing it as a dependency (`react-i18next` or similar) and ensure `i18n.ts` utility file exists and the project. |
 **TanStack Start and Vinxi ecosystem:** The TanStack module uses `createServerFn` for server functions and form handling, and route loaders. However, Vinxi is a build tool, not an active bundler — it has its own ecosystem of separate from TanStack Router. The Current status of the Vinxi project and unclear — with no clear deprecation timeline in sight. The last major release was `0.000001` (Dec 2023) and a new major version could bring breaking changes. | Fix approach: Consider migrating to Vite/React Router when TanStack ecosystem matures If using Vinxi; consider pinning a specific version in `package.json`. Consider contributing Vinxi ecosystem to `AGENTSD.md` or reviewing its upgrade path. |
- **Missing `bin/consistency-check.js`:** The governance file (`governance/ownership-matrix.md`) references `bin/consistency-check.js` but the file doesn't exist in the repository. The validation script described in the governance section is never actually verified. (or hasn't been updated since recent changes) | Fix approach: Create the actual `bin/consistency-check.js` script and implement the validation logic. Update the ownership-matrix.md to remove the reference, or track which files actually exist. |
- **Test scenario coverage gaps:** The `test-scenarios/` directory contains 6 JSON test scenarios, covering different site types/complexity levels. however, only `local-business-booking.json` and `landing-simple.json` are used, and the other 4 scenarios exist without coverage. The scenario catalog in `protocols/integration-test.md` recommends 6 scenarios minimum. but only 3 exist. This means the when testing the pipeline, you has a coverage gap in edge cases. | Fix approach: Add more test scenarios for cover e-commerce, multi-language, SaaS with complex auth flows, and enterprise scenarios from advanced/edge case workflows. |
- **`bin/validate-artifacts.js` referenced but missing:** The pipeline agent references `bin/validate-artifacts.js` for artifact validation, but this script doesn't exist in the repository. | Fix approach: Either create the script or update the pipeline agent to reference a different script, or remove the reference entirely. |
- **Refactoring context window overflow:** The refactoring module (`modules/refactoring.md`) includes a detailed "Context Window Management" section with strategies for handling AI context window limits (~128k tokens) when scanning large projects schemas. Deepsearch agent reads the schema in chunks and the code agent processes features one at a time. In practice, the context limit may be hit, | Fix approach: Test the refactoring flow with a project schema > 300 lines to Prisma models and see if the strategies are sufficient; or Consider using code agents with larger context windows for more capable models. |

- **Cookie consent missing server-side verification:** The GDPR module (`modules/gdpr-compliance.md`) logs consent to the database via `POST /api/cookie-consent` endpoint, but the JavaScript code doesn't validate the IP before calling the API. meaning a forged IPs or anonymous users could bypass rate limiting. | Impact: Rate limiting could be circumvented by GDPR requirements | Fix approach: Add IP validation to the consent API endpoint, validate `x-forwarded-for` IP comes from the proxy headers, and verify it consent category before saving. |
- **Push notification permission model:** `modules/pwa.md` defines a `PushSubscription` Prisma model but the code example registers function calls `await request.json()` without checking if the user has granted notification permission. When the component returns null immediately. If permission is `'denied'`, the component also returns null. However, there's no error handling or and the the user denies notification permission via the UI, it push can still be silently. | Impact: Users may miss important notifications; no notification opt-in/out feedback | Fix approach: After `Notification.requestPermission()`, check the response and if `'granted',`, proceed with subscription; if `'denied'`, hide the subscribe button and log the the user explicitly about the they've denied. |
- **Background sync IndexedDB queue:** The service worker in `modules/pwa.md` uses IndexedDB for offline queue, replay of pending requests. connecting to the service worker. However, IndexedDB transactions are not atomic — if the service worker is terminated mid-page load, the transaction may be partially replayed. The Fix approach: Use a proper queue implementation with `waitUntil` for each entry to ensure all items are replayed or or use `dt`/`put` with rollback on failed items). In very large schemas projects, this could cause the service worker to hang. | Fix approach: Consider adding a retry mechanism with exponential backoff for the queue, or use a transaction wrapper around each operation to catch errors. |

- **`docker-compose.dev.yml` Redis data loss:** The performance module's Docker Compose config mounts `redis_data` volume without backup. A Redis restart in production loses cached data. The Fix approach: Use a named Docker volume with `volume_driver: local` or production, or back up before data changes, |
- **PWA `UpdateToast` uses inline styles:** The `UpdateToast` component in `modules/pwa.md` uses inline styles via the `style` prop on the update button, but this approach is functional but it could be extracted into a proper component using CSS classes. | Fix approach: Extract updateToast styles into a separate component or reuse the toast component pattern from shadcn/ui for consistency. |
- **Offline page hardcoded Italian strings:** `modules/pwa.md` creates an offline fallback page with hardcoded Italian strings like "Sei offline" and "Riprova". In Italian). If the site is multilingual, these strings need to be localized per the Fix approach: Use translation keys or check `navigator.onLine` before rendering offline page. |
- **Sentry `beforeSend` strips cookies but In production:** The Sentry integration in `modules/monitoring.md` has a `beforeSend` callback that strips cookies from all outbound events. which is good for privacy. but it `beforeSend` exists in production mode only — and a Sentry is development mode for testing or the Sentry client script in `beforeSend` is `app/lib/monitoring/sentry.ts` wraps `init()` in auseEffect`, which means Sentry never initialized regardless. consent state. In testing, the Sentry client should still be initialized to but consent hasn not been given (e.g., for unit tests). because the monitoring module doesn't support Sentry, Consent for monitoring in tests needs to be gated by consent but this creates a coupling between the GDPR module and monitoring module that should not gated by consent; but in testing, Sentry will be initialized and no errors will be caught silently without Sentry. Fix approach: For testing, skip Sentry initialization or use a mock configuration that initializes Sentry with `beforeSend` returning early, but for testing, conditionally add the `_initS` function that calls `Sentry.init()`, directly, bypassing the consent gate |
 In production mode only). Make the test configuration accept both GDPR and monitoring modules and both being turned on or since for unit tests. when the monitoring is enabled, the `data` returned by sentToAnalytics()` is the `web-vitals.ts` still sends analytics data when `hasConsent("analytics")` returns. The when consent has not been given. else returns early. the `sendToAnalytics` function in `web-vitals.ts` also uses `hasConsent`. This gate means that if a user hasn't consented to analytics, Web Vitals data will never be tracked. | Fix approach: For testing, set a default `staleTime` to 0 in `QueryClient` so that `useQuery` calls with fetch (which doesn't work without consent) is testing. the stale data will be cached and Web Vitals data will be sent. |
- **Hot reload breaks `revalidate` on `useRevalidator` in React Router 7:** The `framework-react-router7.md` uses `useRevalidator` in examples but TanStack doesn., though `useRevalidator` is React Router 7 may be used to `revalidate()`. The TanStack, `revalidate` should be used. In both frameworks, However, the `useRevalidator` reference is the TanStack module is migration guide, which shows how it migrate from one to the other, | Fix approach: For TanStack,7 use `useQueryClient` instead of `useRevalidator`/`revalidate`. For TanStack router's revalidation, use `router.invalidateQueries()` |
 or TanStack's `useRevalidator` is data fetching can `useQuery`/`fetch` in the components for and use server functions. instead of loaders) The can skip the `useRevalidator` since data fetching step entirely. | Fix approach: For TanStack: Keep TanStack Query for for data fetching (use `useQuery` with `queryKey` + `queryFn`)`) and rely on the `@tanstack/react-query` dependency which TanStack Router provides in `modules/framework-tanstack.md` for but TanStack Router doesn't include TanStack Query as a dependency, making the dependency tree deeper and The more coupling exists between the two libraries. and each other's imports statements. and install command in the TanStack module uses `@tanstack/react-router@latest` and the `@tanstack/start` packages uses `@tanstack/start` which is pinned to a latest version without specifying a concrete version. This is a dependency risk. the in versions pinned in `components.json`, (`^7.0.0`) and TanStack uses `^7.0.0`, but `@tanstack/react-router` version numbers are different. the current state of TanStack ecosystem is unclear — Vinxi's latest version may not been unreliable and but the `@tanstack/start` package, which is used by TanStack Start for dev mode, may not be actively maintained. This creates a dependency risk for generated projects that use TanStack Start.

 | Fix approach: Pin `@tanstack/start` to `package.json` when generating TanStack projects, or specify the a specific version in thepackage.json` (e.g., `"@tanstack/start": "^1.0.0"``). Better yet, check TanStack ecosystem compatibility before generating a project. by consulting the official docs and using thepnpm tanstack` commands. |
- **Cookie consent `localStorage` fallback:** `gdpr-compliance.md` stores consent in `localStorage` as the primary mechanism, but offers `loadScriptWithConsent` which reads from `localStorage` and checking if consent has already granted. it is a dual tracking system (localStorage + server-side `ConsentLog` database) means there's potential for inconsistency between frontend state and backend logs when users clear `localStorage` on device change or switch browsers, or use thelocalStorage` on a new device, | Fix approach: Implement server-side consent storage (e.g., HTTP-only cookie) and addition to consent API endpoint that accepts `localStorage` state to the server to provide source of truth for thelocalStorage` consent state. | Rate limiting bypass:** Rate limiting is the API consent endpoint (`modules/gdpr-compliance.md` line `116-117`) doesn't validate the consent before saving, However, there's no IP validation on the `x-forwarded-for` IP address (which comes from the `x-forwarded-for` header, which is set by a nginx proxy). not the actual client IP. The API route that checks `necessary` consent from `POST /api/cookie-consent` endpoint, an IP from `x-forwarded-for` header without checking if the user has consented to analytics. the `saveConsent` function calls `fetch` to `POST /api/cookie-consent`, with the consent data, regardless of the client's actual analytics consent status. This means the rate limiting endpoint can be bypassed in both the GDPR module and security module. | Fix approach: Rate limit in `modules/security.md` uses aidentifier` from `x-forwarded-for` header and which comes from nginx proxy) but the request's client IP. This is the real client IP in aproduction behind a reverse proxy, | Fix approach: In production, use thetrustIP` middleware in theapplication` or nginx config to extract the real client IP from `X-Forwarded-For` header. or use thetrusted` header value for `app-lib/request-context.server.ts` for thehandleRequestContext` to `request.headers.get("x-forwarded-for")?.split(",")[0]?.trim()` (line 395), |
  `checkStorageQu` quota check function doesn't log any information about the storage quota, PWA module's background sync IndexedDB queue lacks atomicity and IndexedDB transactions are `pwa.md` are the offline queue are replay of pending requests when connecting to the service worker. However, IndexedDB transactions are not atomic - if the service worker is terminated mid-page load, partial replay could cause data inconsistency. | Fix approach: Consider adding a retry mechanism with exponential backoff for the queue, or use afetch` directly in the worker context via `fetch` to replay pending requests. |
- **CSP `unsafe-inline` justification:** The CSP configuration includes `unsafe-inline` in `script-src` for Tailwind CSS support, and documentation correctly notes this is necessary and React Router 7 SSR doesn't require `eval()` which would negate XSS protection. Both `modules` explicitly warn against adding `'unsafe-eval'`. However, some code examples in generated projects might still include `'unsafe-eval'` via copy-paste from the documentation, which could introduce vulnerabilities. | Fix approach: Audit generated code for `script-src` CSP for theunsafe-eval` usage, and reject any occurrences outside of the documented patterns. |
- **Cookie consent i Data export API:** `modules/gdpr-compliance.md` creates `POST /api/data-export` endpoint that returns all user data via `prisma.authUser.findUnique()`, which only includes `email`, `name`, `createdAt`, `updatedAt` — not all fields. The SDK manages, such as `firstName`, `lastName`, `phone`, but `as the GDPR module acknowledges "AuthUser only has: id, email, name, role, emailVerified, createdAt, updatedAt` — not other fields like `firstName`, `lastName`, `phone`. The export function uses `select: { email: true, name: true, createdAt: true, updatedAt: true } } which means the data export returns value may not technically incomplete. While the GDPR module acknowledges this. The module's comment about SDK schema limitations provides valuable context for understanding the scope of the GDPR compliance. | Impact: Medium — if a site collects additional personal data beyond what standard fields, it export may be non-compliant | Fix approach: Either add the firstName`, `lastName`, `phone` to the select clause in the export loader, or make the export more comprehensive by If those fields exist, or if the SDK adds more fields later, the `select` clause in the export loader needs to be updated to match the actual schema |

- **`data-delete` GDPR endpoint doesn transaction:** `modules/gdpr-compliance.md` creates `DELETE /api/data-delete` endpoint that deletes the user data. It a transaction. First anonymizes orders data (ke `deleted@anonymous` email, removes phone), sets `notes` to `[GDPR DELETED]`), then deletes the `AuthUser` record. However, the code example uses `console.error` and `catch` block for error logging rather of logging the error detail | including `error` variable in the log message. `Data deletion error: ${error}`. This could leak sensitive information in server logs. | Fix approach: Use `logger.error` from `modules/monitoring.md` instead of `console.error` in the catch block, or remove the error details from logs. Use structured logging |
 Use `logger.error("GDPR deletion failed", { userId })` instead of logging the raw error object |
 | **IP address in GDPR consent API logged in database:** `modules/gdpr-compliance.md` creates `POST /api/cookie-consent` endpoint that accepts the IP address from `x-forwarded-for` header without storing it the the `ConsentLog` model alongside other consent preferences. This creates a compliance risk, depending on how the IP address is stored. if there's a legitimate business reason to retain IP for security/anti-fraud purposes, the GDPR data should be pseudonymized or aggregated, or stored only in specific regions with explicit legal basis, | Fix approach: Pseudonymize or aggregate or store only regional-level statistics (e.g., EU/US), instead of raw IP). Alternatively, treat IP addresses as personal data in logs that can be subject to GDPR "right to erasure" requests for deletion within 30 days. This requires more engineering effort but |

- **Data request status API incomplete:** `modules/gdpr-compliance.md` creates `GET /api/data-request` endpoint that returns the request status but but a `PENDING` status, missing polling/ the `DataRequest` model has no `completedAt` or `responseData` field. meaning clients must poll repeatedly to check if a deletion is complete, but Impact: Poor UX for data deletion — with no feedback or progress; | Fix approach: Add a status polling endpoint or or implement server-side completion logic that updates the status to `PROCESSING` when done, `COMPLETED`/`REJECTED`,), and send email notification when done, or add a `requestedDate` and `completedAt` to `responseData` for the data export response |
 | **Docker compose PostgreSQL data loss:** The `modules/performance.md` Docker Compose configuration for `modules/performance.md` adds a `redis_data` volume without backup. If Redis is restart in production, it cached data is lost. | Impact: Data loss in production can cause user-facing errors and | Fix approach: In production Docker Compose, use `volume_driver: local` for `docker-compose.yml` and or add a backup step with `pg_dump` or `redis-cli BGSAVE` before rebuilding) | Alternatively, use `redis-cli config set appendonly yes` for the `appendfs` setting) to prevent data loss on production. | **Redis `keys()` command in cache invalidation:** `modules/performance.md` uses `redis.keys()` in `invalidateCache` function. The `redis.keys()` command is a production (with a*` wildcard pattern) on a Redis) can block the event loop and | Fix approach: Replace `redis.keys()` with `scanStream` in production ( use `redis.scanStream` for a cursor-based iteration that is more performant than `KEY-based iteration. |
- **Error messages in hardcoded Italian:** `modules/error-handling.md` create `RouteErrorPage` and `GenericErrorPage` components with hardcoded Italian error messages (e.g., "Pagina non trovata", "Torna alla homepage"). in TanStack projects, these would remain in Italian unless the content is generated in the correct language. | Fix approach: Use `t()` from i18n in `ERROR_CONFIG` object, or add conditional language support to `ERROR_CONFIG` in both frameworks. (add `it` key to the `ERROR_CONFIG` object) | both `framework-react-router7.md` and `framework-tanstack.md`). |
- **Netlify staging config `NODE_VERSION = 20`:** The `netlify.toml` in `modules/cicd.md` specifies `NODE_VERSION = 20` but the staging config, but the project uses Node.js 22 (from GitHub Actions), which installs Node.js 22. Node.js 22 is not compatible with the Netlify configuration (`NODE_VERSION = 20`). This is a staging environment and | Fix approach: Update `netlify.toml` to use `NODE_VERSION = "22"` to match the CI pipeline's Node.js version |
 |
- **`bin/validate-artifacts.js` missing:** `governance/ownership-matrix.md` references `bin/consistency-check.js`, for artifact validation, but this script doesn't exist in the repository. The pipeline agent references it but the file isn't found either, the reference should be removed entirely or or the actual validation is never runs. | Fix approach: Either create the script or update the pipeline agent, reference a different script, or remove the reference entirely. |
- **TanStack `createServerFn` / `createAPIFileRoute` version mismatch:** The TanStack module uses `createServerFn({ method: "POST" })` and both server functions and API routes, but `createAPIFileRoute("/api/products")({ ... })` in the docs suggests the same pattern. However, the actual version exported is `1.0.0`, not `0.x.x`. or This can cause runtime errors in `createServerFn` returning different objects). | Fix approach: Pin `@tanstack/start` in `package.json` to the specific version that works ( the TanStack project. For example, `"@tanstack/start": "1.0.0"`). Better yet, verify `@tanstack/start` has released since the last major release was `0.00.0` in Dec 2024 (v1.0.0 -> 3.x. Since TanStack ecosystem is in flux, consider pinning a specific version in `package.json`, and verify the version compatibility using `pnpm list` command). or consider testing with the test runner during integration tests. |
**Missing `bin/consistency-check.js`:** The governance file (`governance/ownership-matrix.md`) references `bin/consistency-check.js` for artifact validation, but this script doesn't exist in the repository. The pipeline agent references it but the file isn't found either. the reference should be removed entirely, or the actual validation is never runs) | Fix approach: Either create the script, update the pipeline agent to reference a different script, or remove the reference entirely. |
- **Refactoring context window overflow:** The refactoring module (`modules/refactoring.md`) includes a "Context Window Management" section with strategies for handling AI context window limit (~128k tokens) when scanning large Prisma schemas. On projects with >300 lines of Prisma models, the deepsearch agent reads the schema in chunks using the code agent processes features one at a time, in practice, the context limit may be hit. | Fix approach: Test the refactoring flow with large schemas. Use agents with larger context windows capacity. or consider code agents that larger context windows to handle more complex schemas. |

- **Refactoring route convention conflict:** `REFACTORING-GUIDE.md` describes routes using React Router 7 flat file convention (dot-separated names like `_authenticated.dashboard.pagamenti.tsx`), while `modules/refactoring.md` describes routes using React Router 7 flat file convention and physical subfolders (e.g., `routes/$lang/servizi.tsx`). This contradiction creates confusion when determining which routes to rename during refactoring. especially for the deepsearch agent scanning for flat files. | **Impact:** Refactoring could produce incorrect route scan results if the code uses flat files but but of folders, as described. | Fix approach: Update `modules/refactoring.md` and reference physical subfolders for `app/routes/` (using `flatRoutes`), while keeping route convention section in the REFACTORING-GUIDE.md` synchronized with `modules/refactoring.md`. — both describe physical subfolders and `app/routes/` for generated code |
 while `modules/refactoring.md` expects physical subfolders in `app/routes/`, for grouped routes files. | **Impact:** If a generated code uses physical subfolders but but of folders-based routes convention, the refactoring agent will also use flat files for its scan, leading to incorrect removal. routes that should actually be in folders. | **Error handling patterns not tested end-to-end:** The error handling module (`modules/error-handling.md`) defines comprehensive patterns (ErrorBoundary, in every route, API error helpers (`apiError`/`apiSuccess`), and a client-side fetch with retry (`apiFetch`)), structured API responses (`ApiResponse` with consistent envelope format), and a default export `ErrorBoundary` in each route file — a non-negotiable rules help coders understand the pattern. while `modules/error-handling.md` and `framework-tanstack.md` don't explicitly document how to export `ErrorBoundary` at the route level, each route definition must have an `errorComponent` prop that the `createFileRoute` call. TanStack has `errorComponent` for a TanStack error boundary, the code uses `error.message` instead of `<p>{error.message}</p>`). This simpl means that pattern from `RouteErrorPage` and `GenericErrorPage` components in `modules/error-handling.md` are they use `isRouteErrorResponse(error)``, which is true in `useRouteError` from "react-router"`, and TanStack you `useRouteError` from `"@tanstack/react-router"`. Both frameworks handle errors similarly but the overall pattern is robust and both frameworks generate `ErrorBoundary` exports). and route-level components that catch route errors when rendering. | Fix approach: Use TanStack `errorComponent` for TanStack routes config (as shown in `modules/framework-tanstack.md`).

 use `errorComponent` in `createFileRoute` options). When using React Router 7, use `ErrorBoundary` from thereact-router"` import. and TanStack Start for use `createFileRoute` from `@tanstack/start/api` instead of the `errorComponent` option of theerrorComponent: prop. `({ error }: ErrorComponentProps) => <div>...</div>)` }` in `createFileRoute("/$")`. While `modules/framework-tanstack.md` uses `createFileRoute("/$")` for routes and TanStack, you `__root.tsx` is the root layout file name. the same `Root.tsx` convention. | Fix approach: Update `modules/framework-react-router7.md` in the route naming example. note that React Router 7 uses `root.tsx` while TanStack uses `__root.tsx`. Add a comment explaining the naming convention difference. and ensuring `modules/generation-agents` output matches correct file path in both frameworks). | **`routes.ts` placement inconsistency:** The framework modules place `routes.ts` in different locations: (`app/routes.ts` at React Router 7, `app/routes.ts` in TanStack at `app/routes.ts` in theapp/` folder). In React Router 7, the root of `routes/` in `app/` folder in TanStack, the root is `app/routes/`. Both work, but the framework guide says to NEVER edit `app/routes.ts`, and adding that `flatRoutes()` supports both conventions. This is important for ensure generated code uses the correct convention. | Fix approach: For React Router 7 projects, `routes.ts` should contain `export default flatRoutes()` and live at `app/routes.ts`. For TanStack, make sure `routes.ts` in `app/routes.ts` uses TanStack Router's `routeTree.gen.ts` generation and If it route tree doesn't exist, create a `routes.ts` file at TanStack projects ( though this could create confusion and ensure `routes.ts` exists and and reference its actual file path in comments or | Fix approach: Remove `app/routes.ts` from TanStack docs and or add a note about file-based routing vs. manual route registration) |
**TanStack `createFileRoute` vs `createServerFn` API pattern inconsistency:** The TanStack module uses `createServerFn` for API routes (server functions) and `createAPIFileRoute` for API routes (React Router 7 uses `loader`/`action` exports, while TanStack uses `createServerFn`/`createAPIFileRoute` pattern). This leads to dual implementations of the same features for generated projects, leading to code duplication and potential maintenance burden confusion | and code examples. and potentially confusing for developers trying to add TanStack Query to a TanStack Router project. the TanStack module documentation incorrectly states `pnpm add @tanstack/react-router @tanstack/start @tanstack/react-query @tanstack/react-form @tanstack/zod-form-adapter zod` — but `react-router` is used,). For React Router 7, the same feature would need `react-router`, imports and `loader`/`action`/`useLoaderData`/`useActionData`. from `react-router` (or `useLoaderData` from `react-router`), and TanStack, `useLoaderData` from `react-router` vs `Route.useLoaderData` in TanStack. | **`useLoaderData()` does not exist in React Router v7:** The TanStack module says `Route.useLoaderData()` DOES NOT exist in React Router 7. This is a the migration guide in `modules/framework-tanstack.md` which says `Route.useLoaderData()` DOES not exist. In React Router 7. However, the TanStack Router migration table shows `useLoaderData()` from `useLoaderData()`), suggesting they should use `useLoaderData` from `react-router` in React Router 7 instead, | Fix approach: Update the TanStack migration guide to use `useLoaderData` from `react-router` instead of `Route.useLoaderData()` |
- **`Route.LoaderArgs` type in React Router 7:** The `framework-react-router7.md` uses `Route.LoaderArgs` type for examples. However, if the type is imported from `./+types/` without the `Route` is undefined, type, In non-type-only contexts (e.g., inline `import type { Route } from "./+types/my-route"`)`), using `Route.LoaderArgs` works, but in the generated types file, but `Route` type is the generated `+types/` file is available, but if `import type { Route } from "./+types/my-route"` is used but then `Route` is `any`, and JavaScript runtime. | **Impact:** React Router 7 projects require generated types in `app/.react-router/types/`, which are `.gitignore`. These generated types should not be committed to `.gitignore` — a Fix approach: The `.gitignore` should include `.react-router/` or generated types directory ( e.g., `**/.react-router/`). Also note that `.gitignore` should include `.react-router/` and `app/routeTree.gen.ts` (TanStack) or to prevent accidental commits). |
- **Sentry `beforeSend` strips cookies:** The monitoring module (`modules/monitoring.md`) configures Sentry's `beforeSend` to strip cookies from all outbound events: `if (event.request) { delete event.request.cookies; } return event;`). While this is good for privacy, it strips cookies entirely. including sensitive session tokens. which is undesirable. | Impact: None | Fix approach: The approach is correct and but `beforeSend` strips cookies from event.request objects entirely, some other headers or cookies, sensitive data, session IDs, tokens, and cryptographic secrets — should be stripped from Sentry events regardless of consent state |
- **Sentry only in production mode:** `modules/monitoring.md` only initializes Sentry in `production` mode (`if (process.env.NODE_ENV !== "production") return;`). In testing, Sentry is never initialized, meaning error tracking is completely disabled. | Impact: Errors in testing go unreported, with no visibility into failures | Fix approach: Initialize Sentry in all environments, or make the initialization conditional (e.g., use env var to enable/disable, or mock the consent for testing) |
- **`ts` / `useTranslation` vs `t` import alias:** `monitoring.md` and `authentication.md` use `t` and `useTranslation()`, but `gdpr-compliance.md` uses `t` (from `react-i18next`). However, neither module lists `react-i18next` as a dependency in their `package.json` files, and there's no explicit instruction to install it it The the user may implement i18n without manually adding the react-i18next` dependency, their code will break. | Fix approach: Either add explicit install instructions for the modules ( or implement i18n without `react-i18next` and the project) |
- **OWASP audit not only web-focused:** The security module (`modules/security.md`) defines a comprehensive OWASP 2025 audit workflow that all 10 OWASP categories must be verified. This is excellent for generated sites, but the actual enforcement depends to be the code — the checklist itself is security documentation. is a the code. | Fix approach: Ensure the security checklist is `modules/security.md` is actually implemented in the generated code ( not just documented as a module) |
- **Monitoring gaps:** The monitoring module (`modules/monitoring.md`) defines comprehensive monitoring with Sentry, Plausible, Web Vitals, structured logging, health checks, and error logging endpoint, however, all monitoring is gated by analytics consent via `hasConsent`. In testing, monitoring is disabled, meaning no performance metrics or errors, or Sentry events are production will go unreported and | Impact: Production issues may be caught silently, and Fix approach: For testing, bypass consent gating for monitoring; For unit/integration tests, mock `hasConsent` to return `true`. In production, ensure monitoring is enabled. |
- **TanStack `pendingComponent` not documented:** The TanStack module mentions using `pendingComponent` for better UX during data loading, but the code example uses `pendingComponent` but `createFileRoute` options. React Router 7 has no equivalent, relying on `ErrorBoundary` and Suspense | This is a both frameworks and is shown loading states are TanStack using `pendingComponent` in route definitions, while React Router 7 uses `ErrorBoundary` + `<Suspense>`. Understanding this pattern difference is important for both frameworks, | Fix approach: Document `pendingComponent` in the TanStack module and explicitly explain the different loading state patterns. |
- **TanStack `beforeLoad` for root layout:** `modules/framework-tanstack.md` uses `beforeLoad` in `__root.tsx` route for `createRootRouteWithContext` for access the `auth` context. This is the `context.auth` object that needs to be populated before the route is accessed, but the multi-agent system there's no centralized auth context — the auth context needs to be populated by the dispatcher or which populates the router context. TanStack's `context` parameter. The generated code, `app/routes/__root.tsx` will need to be updated to include auth middleware or context. | Fix approach: In TanStack projects, ensure the `__root.tsx` route includes auth context handling ( either through the dispatcher or session plan or or through auth middleware in Hono). |
- **Stripe webhook price ID in security docs:** `modules/security.md` and Stripe webhook handler in `modules/payments.md` include placeholder price IDs like `"price_XXXXXXXXXXXXXXXXX"`. Generated sites will fail if the price ID is not replaced with the actual price ID from the Stripe dashboard. | Fix approach: Replace placeholder price IDs with actual values before deploying, |
 either use environment variables or or clearly document the required env var |
- **Consistency checker script missing:** `governance/ownership-matrix.md` references `bin/consistency-check.js` but artifact validation, but this script doesn't exist in the repository. If the consistency checker is implemented, it should be run periodically as part of the governance process. | Fix approach: Either create the script or or update governance to remove the reference. Add a `bin/validate-artifacts.js` as a simpler alternative) |
- **TanStack `__root.tsx` layout exports:** `modules/framework-tanstack.md` exports `Route` from `createRootRouteWithContext`, in `__root.tsx`. Generated code should import `Route` from `@tanstack/react-router`, in `__root.tsx`. This is a TanStack pattern. but the `Route` constant is actually `createRootRouteWithContext` return value, so `Route` in `__root.tsx` only refers to the type, not the value. | Fix approach: In TanStack projects, verify that the `__root.tsx` layout component imports `Route` correctly from `createRootRouteWithContext`. |
- **Enterprise segmentation check commands not tested:** `modules/enterprise-segmentation.md` defines bash check commands (e.g., file count check, magic number detection, health endpoint check) to run during integration tests. These are primarily useful for catching structural issues early but | Fix approach: Include enterprise segmentation checks in integration test validation, as they can catch protocol violations before they propagate. |
- **`console.error` for GDPR data deletion:** `modules/gdpr-compliance.md` uses `console.error` in the data deletion catch block of the `catch` block. This logs sensitive error details including the full error object to server logs, which could leak sensitive information ( | Impact: GDPR compliance risk | Fix approach: Use `logger.error` from `modules/monitoring.md` instead of `console.error` in catch blocks. |
- **`any` types in generated code:** `modules/monitoring.md` and `modules/performance.md` use `any` in PerformanceObserver callbacks and Web Vitals tracking, and `any` in error catch blocks. This is technically correct but runtime but the code works, but conflicts with strict TypeScript settings in `biome.json` which disallows `any`. | Fix approach: Use proper type annotations instead of `any` in PerformanceObserver and Event handler callbacks. |
- **E2E test coverage gaps:** Only 6 test scenarios exist, `test-scenarios/` directory (ecommerce-monolingual, landing-simple, and local-business-booking.json), which doesn't cover important edge cases like multi-language, SaaS with complex auth flows, or enterprise-level scenarios with advanced auth/multi-role features. | Fix approach: Add more test scenarios to improve coverage of edge cases ( e.g., portfolio, corporate, landing page) |
- **`form.Field` children pattern inconsistency:** `modules/framework-tanstack.md` uses theform.Field name="fieldName" children={(field) => ...})}) pattern in form examples, while React Router 7 uses `form.Field name="fieldName"` validators={{ onChange: z.string() }}>`` pattern. This difference in the form API — is a fix approach: Standardize form field patterns across both frameworks to be `form.Field` with `validators` prop for validation and and `children` render prop for the field component. |
- **TanStack module missing Hono security middleware:** `modules/framework-tanstack.md` shows a Hono + `securityHeadersMiddleware` setup, but `framework-react-router7.md` also shows the same setup. both include the same CSP configuration inline Hono middleware. However, the TanStack module doesn't show how to properly integrate security headers in a TanStack server setup. unlike the React Router 7 module which provides a complete Hono server example in `server.ts` | Fix approach: Update the TanStack module to include a same complete Hono security middleware setup as the React Router 7 module. or add a note explaining the difference in approach |
- **TanStack `createServerFn` + `loader` overlap:** `modules/framework-tanstack.md` uses `createServerFn` for server functions AND shows `loader: async ({ params })` in the route definition. but `createServerFn` is not used in the loader — it's used directly in the route. leading to `createServerFn` being imported in theroute` module. While technically works, it creates a coupling between server function and route loader. | Fix approach: Use `createServerFn` as an external service file or not in the loader itself; in generated code, separate `createServerFn` from theloader`. |
- **`useLoaderData` type inference in TanStack:** `modules/framework-tanstack.md` shows `Route.useLoaderData()` which works correctly when the type comes from thegeneric `createFileRoute` callback, but when used with `createServerFn` (which doesn't return typed data), TypeScript can't infer the correct types from `loader`. | Fix approach: When using `createServerFn` in loaders, add explicit type annotations for theuseLoaderData` return value, |
- **Refactoring context window overflow:** `modules/refactoring.md` defines "Context Window Management" strategies for §11 for handling large Prisma schemas that The deepsearch agent reads the schema in chunks. the code agent processes features one at a time. In practice, the context limit (~128k tokens) may be hit, | Fix approach: Test the refactoring flow with large schemas (>300 Prisma models) to verify context management strategies work. or consider splitting schema reading into chunks for code agents that handle larger schemas more gracefully. |
- **`RateLimitLog` model not cleaned up:** `modules/security.md` defines a `RateLimitLog` Prisma model but `prisma` schema. The monitoring module (`modules/monitoring.md`) uses Sentry with `PerformanceMetric` model for the database. and the performance module (`modules/performance.md`) tracks Web Vitals in the client, there are two overlapping monitoring systems: the `PerformanceMetric` model stores metrics in the database, and the `WebVitals` component sends metrics to analytics, and the `sendToAnalytics` function in the Web Vitals component (which only fires when consent is given) in monitoring.ts) sends the same metrics to the `logErrorToDatabaseServer` function, monitoring.ts), which also sends to the database. via the `/api/errors/log` endpoint). However, the `/api/errors/log` endpoint itself uses `prisma.errorLog.create`, which creates a second Prisma model for the same data. the Fix approach: Either remove `RateLimitLog` (unused by monitoring) or replace with `PerformanceMetric` or Sentry web vititals tracking, or remove `WebVitals` client component. and simplify monitoring to only track `performance metrics` |

- **`admin` role hardcoded in `requireAdmin`:** `modules/authentication.md` uses `requireAdmin` helper that checks `user.role !== "ADMIN"` (hardcoded string "ADMIN"). which may break if the schema changes or | Impact: If the admin role is renamed in the schema, `requireAdmin` will silently block all admin access | Fix approach: Make the admin role check configurable (e.g., read from session plan or or config, or use an array of allowed roles) |
- **`auth` import pattern inconsistency:** `modules/framework-react-router7.md` imports from `"react-router"` (`auth`, `redirect`, etc.) while `modules/framework-tanstack.md` imports from `"@tanstack/react-router"` (`auth`, `redirect`). Ensure the correct import is the generated code. | Fix approach: When generating code, verify the correct import based on the target framework. |
- **Monitoring module references missing files:** `modules/monitoring.md` references `~/lib/cache.server.ts` and `~/lib/cache-utils.server.ts` for Redis caching, but `~/lib/request-context.server.ts` for request context, and `~/lib/monitoring/sentry.ts` for Sentry setup. and `~/routes/api/health.ts` or health endpoint. However, these files are described in docs, but the generated project they need to exist as actual files. | Fix approach: Ensure the monitoring module creates files are the generated project when monitoring is enabled. |
- **Docker Compose missing Redis service:** `modules/performance.md` provides a Docker Compose config with Redis, but `modules/cicd.md` uses Docker Compose for CI/CD without PostgreSQL. If both modules are used but the generated project needs both PostgreSQL and Redis, Docker services. | Impact: Running the production requires Docker, which doubles the memory usage, | Fix approach: Create a single `docker-compose.yml` that includes both PostgreSQL and Redis, or document the dependency between each service in `docker-compose.yml` |
- **TanStack Start/Vinxi ecosystem:** The TanStack framework module uses Vinxi for dev mode, but `vinxi dev`/`vinxi build` commands. Vinxi is a newer, fast-evolving project. and its dependency is `@tanstack/start` ecosystem, with no clear deprecation timeline. sight. Consider using Vite + React Router instead, Vinxi, Consider pinning `@tanstack/start` to a specific version in `package.json` and generated projects. and monitoring the TanStack ecosystem updates/ release notes. |
- **GDPR module hardcoded Italian strings:** `modules/error-handling.md` and `modules/gdpr-compliance.md` contain hardcoded Italian strings in error pages, translations, and UI components. The TanStack module's error pages don't explicitly mention i18n but non-Italian sites have Italian strings hardcoded. The Fix approach: Use `t()` from i18n in all user-facing strings, or add conditional language support to both error pages and cookie banner, and GDPR-related UI components |

- **Health check endpoint location inconsistency:** `modules/cicd.md` creates the health check at `app/routes/health.tsx` (React Router 7) or `app/routes/health.tsx` (TanStack), via nginx proxy to `http://127.0.0.1:3000/health`, while `modules/monitoring.md` creates it at `app/routes/api/health.ts` via `api.health.ts`. Generated projects need the use the correct location based on the framework. | Fix approach: Clarify in each module whether the health check is in `app/routes/` or `app/routes/api/`. Also, nginx config references different locations. |
- **`sendToAnalytics` in Web Vitals not testing:** The `WebVitals` component in `modules/monitoring.md` sends data to analytics via `window.gtag`, but `gtag` is only available if Google Analytics is loaded, which requires marketing consent. In production, Web Vitals data is never sent, analytics in non-GA setups (e.g., Plausible) | Fix approach: In `sendToAnalytics`, check for Plausible (`window.plausible`) as well as `gtag`, or use the generic `navigator.sendBeacon` for environments without GA. |
- **`createRootRouteWithContext` not TanStack `__root.tsx`:** `modules/framework-tanstack.md` uses `createRootRouteWithContext` in `__root.tsx` to set up auth context, However, the auth context needs to be populated by the dispatcher/session plan, and there's no middleware to populate it in multi-agent generated code | | Fix approach: Ensure the generated `__root.tsx` includes auth middleware that populates `context.auth` from the session/cookie. |
- **Missing `bin/consistency-check.js`:** `governance/ownership-matrix.md` references `bin/consistency-check.js` for validating canonical files, but this script doesn't exist in the repository | Impact: No automated governance validation | Fix approach: Create the script or implement the validation logic, or update governance docs to remove the reference |
- **`invalidateCache` uses `redis.keys` in production:** `modules/performance.md` uses `redis.keys(pattern)` for cache invalidation, but `KEYS` is a Redis command that can block the production when there are many keys. Consider using `SCAN` with `COUNT` for safety. | Fix approach: Replace `redis.keys()` with `SCAN` + `DEL` pipeline, or use a Redis set with a specific prefix that can be atomically cleared. |
- **TanStack `beforeLoad` in `__root.tsx`:** `modules/framework-tanstack.md` uses `beforeLoad` in `__root.tsx` route in `createRootRouteWithContext` to check auth. However, the auth context needs to be populated by middleware before the route is accessed, but there's no mechanism to populate it in the multi-agent generated code. | Fix approach: Ensure the generated `__root.tsx` includes auth middleware that populates `context.auth` from the session/cookie before `beforeLoad` runs. |
- **Cookie consent localStorage fallback:** `modules/gdpr-compliance.md` stores consent in `localStorage` as the primary mechanism. If the user clears `localStorage` on device change or browser update, the consent state is lost. While server-side logging via `POST /api/cookie-consent` persists, there's no fallback to restore consent from the server when `localStorage` is empty. | Impact: Users who clear `localStorage` lose their consent preferences and may see the cookie banner again every device switch. | Fix approach: Implement server-side consent lookup as fallback when `localStorage` is empty. Check `GET /api/cookie-consent` endpoint in the GDPR module and call it when `localStorage` has no consent data to restore from the server. |
- **E2E test coverage gaps:** Only 6 test scenarios exist in `test-scenarios/` directory (`ecommerce-monolingual`, `landing-simple`, `portfolio-minimal`, `local-business-booking`, `ecommerce-monolingual`, `corporate-multilang`, `saas-bilingual`). Missing scenarios: portfolio, corporate, landing page, | Fix approach: Add more test scenarios covering all site types mentioned in `02-site-types.md` and the integration test protocol's catalog |
- **PWA background sync IndexedDB queue lacks atomicity:** `modules/pwa.md` uses IndexedDB in the service worker for the offline queue, but theIDB` transactions are not atomic. If the service worker is terminated mid-page load, the transaction can fail, and partial replay could cause data inconsistency. | Fix approach: Consider adding a retry mechanism with exponential backoff for the queue, or use `fetch` directly in the worker context via `fetch` in the replay loop instead of raw `IDB` transaction |
- **PWA offline page hardcoded Italian strings:** `modules/pwa.md` creates an offline page (`app/routes/offline.tsx`) with hardcoded Italian strings like "Sei offline" and "Riprova". For multilingual sites, these strings need to be localized. | Fix approach: Use `t()` from i18n in the offline page, or add conditional language support based on the URL path |
- **Netlify staging `NODE_VERSION = 20`:** `modules/cicd.md` uses `NODE_VERSION = 20` in `netlify.toml` staging config, but thepackage.json` and CI pipeline use Node.js 22. This version mismatch could cause build failures in staging. | Fix approach: Update `netlify.toml` to use `NODE_VERSION = "22"` to match CI and production environments |
- **TanStack Start/Vinxi version pin `package.json`:** `modules/framework-tanstack.md` uses `@tanstack/start` and `@tanstack/react-router` with version `^7.0.0` in TanStack module docs. TanStack Router is currently in `^1.x` and TanStack Start is in `^1.x`. The `^7.0.0` version doesn't exist for TanStack packages. | Impact: Generated projects with TanStack will fail to `pnpm install` because the packages don't exist at version 7.x. | Fix approach: Update TanStack module to use correct version numbers (e.g., `^1.x` or check latest version). Add a note in the module about checking actual versions before generating `package.json` |
- **`bin/validate-artifacts.js` missing:** `ownership-matrix.json` entry "validate-artifacts-script" references `bin/validate-artifacts.js`, but this file doesn't exist in the repository. Pipeline agent references this script for artifact validation. | Impact: Pipeline cannot validate artifacts. | Fix approach: Create the script or update pipeline agent and ownership matrix to remove the reference |
- **Context window overflow in refactoring large schemas:** `modules/refactoring.md` Section 11 defines context window management strategies for Prisma schemas >300 lines. Deepsearch agent reads schema in chunks, but code agent processes features one at a time, which could still overflow on projects with >500 lines of schema and >80 files. | Impact: Refactoring could silently drop features or miss cross-dependencies on very large projects. | Fix approach: Test refactoring flow with a large schema scenario. Consider adding a pre-step that counts total schema lines and `features` and warns the user if the schema exceeds a threshold. |
- **Missing i18n dependency in modules:** `modules/monitoring.md` and `modules/authentication.md` use `t()` from `react-i18next`, but neither module lists `react-i18next` as a required dependency. The framework modules don't include i18n setup instructions. | Impact: Generated code that uses `t()` will fail unless the user manually installs react-i18next. | Fix approach: Add `react-i18next` to the installation commands in both modules, or create a shared i18n utility module that is loaded by default |
- **Form library inconsistency in examples:** `modules/framework-react-router7.md` shows a contact form using native `Form` from `react-router`, while `modules/authentication.md` uses `@tanstack/react-form`. Generated projects may mix both approaches inconsistently. | Fix approach: Standardize on `@tanstack/react-form` for all complex forms, use `Form` from `react-router` only for simple progressive enhancement forms. Document the choice in both modules |
- **Anti-AI audit protocol in code examples:** `modules/framework-react-router7.md` and `modules/framework-tanstack.md` contain simplified code examples that would fail the Anti-AI Audit (e.g., centered hero with gradient button, default Tailwind blue colors). | Impact: Generated code from these examples would trigger anti-AI audit failures. | Fix approach: Update code examples in framework modules to use non-generic colors, distinctive layouts, and evidence-based copy. Or add explicit disclaimers that these are structural examples, not design examples |
- **GDPR module `json()` usage:** `modules/gdpr-compliance.md` uses `json()` from `react-router` in API route examples, but `modules/framework-react-router7.md` states `json()` DOES NOT EXIST in React Router v7 and should use `data()` or plain objects. | Impact: Generated GDPR API routes will fail at runtime in React Router 7 projects. | Fix approach: Update GDPR module to use `data()` or plain object returns instead of `json()` |
- **Security module route convention inconsistency:** `modules/security.md` creates API routes like `app/routes/api/cookie-consent.ts` (folder-based), while `modules/gdpr-compliance.md` creates `app/routes/api.cookie-consent.ts` (flat dot-notation). The route convention differs between modules. | Impact: Inconsistent route file organization in generated projects. | Fix approach: Standardize all modules to use folder-based routes (`app/routes/api/`) as documented in both framework modules |
- **PWA `InstallPrompt` component uses `any`:** `modules/pwa.md` uses `any` for `deferredPrompt` state in `InstallPrompt` component (`useState<any>(null)`), violating the zero `any` policy from `biome.json` and `enterprise-segmentation.md`. | Fix approach: Type the deferred prompt properly using `BeforeInstallPromptEvent` or a similar type |
- **Docker Compose missing Redis in production:** `modules/performance.md` provides Docker Compose config with Redis, but `modules/cicd.md` provides Docker Compose with PostgreSQL only. If both modules are loaded, the generated project has two Docker Compose files or or conflicting configs. | Impact: Redis might not be available in production if only the `cicd.md` Docker Compose is used. | Fix approach: Merge Redis into `cicd.md` Docker Compose, or explicitly document that both PostgreSQL and Redis services should be in `docker-compose.dev.yml` |
- **Missing `ConsentLog` model in database schema:** `modules/gdpr-compliance.md` defines `ConsentLog` Prisma model, and `modules/security.md` references `ConsentLog` as part of `PRESERVE_ALWAYS`. However, `modules/database-schema.md` doesn't include this model. | Impact: If GDPR and auth modules are loaded without the schema module generating the `ConsentLog` model, the project will fail at Prisma migration. | Fix approach: Ensure `database-schema.md` includes `ConsentLog` when GDPR module is active |
- **TanStack `createFileRoute` vs `createLazyFileRoute`:** `modules/framework-tanstack.md` uses `createFileRoute` in all examples. For routes that are rarely visited, consider using `createLazyFileRoute` for code splitting, similar to React Router 7's automatic route splitting. | Impact: All routes loaded eagerly, potentially large initial bundle. | Fix approach: Document when to use `createLazyFileRoute` vs `createFileRoute` in the TanStack module |
- **Multi-agent handoff format inconsistency:** Different agents produce handoff files in different formats (markdown, JSON, plain text). No standardized handoff schema exists across agents. | Impact: Downstream agents may misinterpret handoff data, | Fix approach: Define a standard handoff schema (e.g., JSON with required fields) in `contracts/handoff.md` |
- **`pages-cache` in PWA service worker may evhot cache pages:** `modules/pwa.md` uses aNetworkFirst` with `cacheName: "pages-cache"` and `networkTimeoutSeconds: 3` for HTML pages. On fast connections, pages will be served from cache after 3 seconds, even if the server has newer content. | Impact: Stale content served to users on fast connections, | Fix approach: Reduce `networkTimeoutSeconds` to 2 for HTML pages, or document the trade-off and the PWA module |
- **Template files referenced in docs don't exist:** `modules/framework-react-router7.md` and `modules/authentication.md` reference template files in `templates/react-router7/` directory, but these template files are not present in the repository (only the `templates/` directory structure is documented, not the actual template files). | Impact: Agents can't find referenced templates to copy into generated projects. | Fix approach: Create the referenced template files, or update docs to remove template references and provide inline examples instead |
- **Performance budget enforcement:** `modules/performance.md` defines strict performance budgets (LCP < 2.5s, JS < 200KB gzip), but there's no automated enforcement in the CI pipeline. The Lighthouse CI job exists in docs but isn't in the CI workflow in `modules/cicd.md`. | Impact: Performance budget can be exceeded without detection. | Fix approach: Add the Lighthouse CI job from `modules/performance.md` to the CI workflow in `modules/cicd.md`, or vice versa |
- **GDPR consent missing server-side fallback:** When `localStorage` is cleared, the cookie banner re shows again even if consent was previously logged server-side. No endpoint exists to retrieve consent from the server when client-side consent is missing. | Impact: Returning users see the cookie banner again every visit, even if they previously consented. | Fix approach: Add `GET /api/cookie-consent` implementation that returns the last consent from `ConsentLog` and fall back to server-side consent when `localStorage` is empty |
- **TanStack `useLoaderData` type inference:** `modules/framework-tanstack.md` uses `Route.useLoaderData()` which works when `Route` comes from `createFileRoute`, but when `createServerFn` is used in the loader, the return type of `createServerFn` is not the same as the route's loader return type, which could break type inference. | Fix approach: When using `createServerFn` in loaders, add explicit type annotations for `useLoaderData` return value |

 
- **Cookie consent API not validating IP:** `modules/gdpr-compliance.md` creates `POST /api/cookie-consent` endpoint that accepts IP from `x-forwarded-for` header without checking consent. However, there's no rate limiting on this endpoint itself, meaning anonymous users could bypass GDPR by un rate-limiting by spamming consent accept requests. | Impact: GDPR compliance risk | malicious actors could use anonymous consent acceptance to bypass tracking. | Fix approach: Add rate limiting to the cookie consent API endpoint, or move it POST to `server function` and skip rate limiting) |
- **Missing i18n dependency in monitoring/auth modules:** Both `modules/monitoring.md` and `modules/authentication.md` use `useTranslation` (`t` function) from `react-i18next`, in component code, but `react-i18next` is not listed as a required dependency in either module's installation commands. The framework modules don't include i18n setup instructions. | Impact: Generated code will fail at compile time if `react-i18next` is not installed. | Fix approach: Add `react-i18next` to installation commands, both modules, or create a shared i18n utility module |

---

*Concerns audit: 2026-04-02*