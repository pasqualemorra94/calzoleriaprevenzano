---
phase: quick-260529-mn1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/shared/GoogleAnalytics.tsx
autonomous: true
requirements: [GA4-FIX-01]

must_haves:
  truths:
    - "Caricato gtag.js dopo consenso analytics, GA4 invia almeno una richiesta /collect a www.google-analytics.com con tid=G-VBM6E09LBH"
    - "I comandi js e config vengono processati da gtag.js (non restano array inerti nel dataLayer)"
    - "Tutto il comportamento esistente (consent gating, logging [GA4], idempotenza injection, config con anonymize_ip/allow_google_signals false, listener revoke) resta invariato"
  artifacts:
    - path: "src/components/shared/GoogleAnalytics.tsx"
      provides: "Stub gtag che spinge l'oggetto arguments (pattern Google ufficiale)"
      contains: "dataLayer.push(arguments)"
  key_links:
    - from: "src/components/shared/GoogleAnalytics.tsx (stub gtag)"
      to: "window.dataLayer"
      via: "push(arguments) invece di push(args)"
      pattern: "dataLayer.*push\\(arguments\\)"
---

<objective>
Correggere lo stub di `gtag` nel componente `GoogleAnalytics.tsx`: attualmente spinge un array (`args`) nel dataLayer invece dell'oggetto `arguments`, quindi gtag.js non riconosce i comandi `js`/`config`, non processa il config e non invia mai la richiesta `/collect` a GA4 (zero dati in Google Analytics).

La diagnosi è già confermata empiricamente (test Playwright sul sito live): con `push(args)` → 0 richieste `/collect`; con il pattern ufficiale Google `function gtag(){ dataLayer.push(arguments); }` → 2 richieste `/collect` con `tid=G-VBM6E09LBH`.

Purpose: ripristinare l'invio dei dati di analytics a GA4 (ID misurazione G-VBM6E09LBH, proprietà 273888156).
Output: stub gtag corretto, build verde (typecheck + Biome), comportamento circostante invariato.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@./CLAUDE.md
@src/components/shared/GoogleAnalytics.tsx

<interfaces>
<!-- Estratto dal file da modificare. L'executor usa questo direttamente, niente esplorazione. -->

Stub ATTUALE (errato) — src/components/shared/GoogleAnalytics.tsx:45-49:
```typescript
window.gtag =
  window.gtag ||
  function gtag(...args: unknown[]): void {
    (window.dataLayer as unknown[]).push(args);
  };
```

Chiamate a valle (devono continuare a funzionare con la stessa firma `(...args: unknown[]) => void`):
```typescript
window.gtag("js", new Date());
window.gtag("config", MEASUREMENT_ID, { anonymize_ip: true, allow_google_signals: false, allow_ad_personalization_signals: false });
```

Tipo globale (NON cambiare):
```typescript
declare global {
  interface Window {
    gtag?: (...args: unknown[]) => void;
    dataLayer?: unknown[];
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correggi lo stub gtag per spingere arguments invece di args</name>
  <files>src/components/shared/GoogleAnalytics.tsx</files>
  <action>
Nella funzione `loadGtag` (righe ~43-49), modificare SOLO lo stub di `window.gtag` affinché spinga l'oggetto `arguments` nel dataLayer, replicando il pattern Google ufficiale `function gtag(){ dataLayer.push(arguments); }`. Questo è l'unico vero fix: gtag.js a valle ispeziona gli oggetti `arguments` (array-like con proprietà `callee`) per riconoscere i comandi `js`/`config`; un array normale viene ignorato.

Vincoli di stile del progetto (CLAUDE.md: zero `any`, TypeScript strict, Biome, commenti in italiano):
- Biome attiva le regole `noArguments` / `useRestParameter` (`prefer-rest-params`) sull'uso di `arguments` e dei rest parameter. La funzione DEVE usare `arguments` (non rest), quindi:
  - Dichiarare la funzione SENZA parametri tipizzati: `function gtag(): void { ... }` (oppure `function gtag(..._args: unknown[]): void` se necessario per la firma, ma poi spingere comunque `arguments`).
  - Anteporre un commento `// biome-ignore lint/style/noArguments: gtag.js richiede l'oggetto arguments nativo (pattern ufficiale Google), non un array — vedi diagnosi /collect` sulla riga che usa `arguments`.
- TypeScript strict: `dataLayer.push(arguments)` può richiedere un cast perché `arguments` è di tipo `IArguments` mentre `dataLayer` è `unknown[]`. Usare un cast esplicito SENZA `any`, ad es. `(window.dataLayer as unknown[]).push(arguments as unknown as unknown[])` oppure `(window.dataLayer as unknown[]).push(arguments)` se il compilatore lo accetta (verificare con typecheck). NON introdurre `any` né `as any`.
- Mantenere il pattern idempotente `window.gtag = window.gtag || function gtag()...` invariato nella forma (solo il corpo cambia da `push(args)` a `push(arguments)`).
- Il tipo globale `Window.gtag?: (...args: unknown[]) => void` deve restare compatibile: assegnare una funzione senza parametri a una firma `(...args: unknown[]) => void` è valido in TypeScript (parametri in meno sono assegnabili). Se TS si lamenta, usare un cast sull'assegnazione: `window.gtag || (function gtag(): void { ... } as (...args: unknown[]) => void)`.

NON toccare nient'altro: le chiamate `window.gtag("js", ...)` e `window.gtag("config", ...)`, il config con `anonymize_ip`/`allow_google_signals: false`/`allow_ad_personalization_signals: false`, il logging diagnostico `[GA4]`, l'idempotenza dell'injection dello script, il consent gating via `loadScriptWithConsent`, e il listener `consent-update` di revoke. Solo il corpo dello stub cambia.
  </action>
  <verify>
    <automated>cd /Users/pasqualemorra/Projects/calzoleriaprevenzano && pnpm typecheck 2>&1 | grep -c "GoogleAnalytics.tsx" || true</automated>
  </verify>
  <done>
- Lo stub `gtag` spinge `arguments` (non `args`) nel dataLayer; `grep -n "dataLayer.*push(arguments)" src/components/shared/GoogleAnalytics.tsx` trova la riga.
- `grep -n "push(args)" src/components/shared/GoogleAnalytics.tsx` → 0 match (vecchio pattern rimosso).
- Presente il commento `// biome-ignore lint/style/noArguments` sopra l'uso di `arguments`.
- `pnpm typecheck` non introduce NUOVI errori su `GoogleAnalytics.tsx` (la baseline pre-esistente 26 errori su altri file resta invariata; il file GA4 deve restare a 0 errori).
- Zero `any` / `as any` introdotti nel diff.
- Tutto il resto del file (consent gating, logging [GA4], idempotenza, config, listener revoke) byte-identico tranne il corpo dello stub.
  </done>
</task>

</tasks>

<verification>
- `pnpm typecheck`: nessun nuovo errore su `src/components/shared/GoogleAnalytics.tsx` (baseline 26 errori su file out-of-scope noti — vedi STATE.md — invariata).
- `git diff src/components/shared/GoogleAnalytics.tsx`: il diff netto tocca SOLO il corpo dello stub gtag (+ eventuale commento biome-ignore + eventuale cast). Nessuna altra riga modificata.
- Zero `any`/`as any` nel diff.
- Nota: `pnpm lint` project-wide è rotto (biome.json incompatibile con Biome 2.x, problema PRE-ESISTENTE documentato in STATE.md) → lint deferito al CI Railway. Il commento `biome-ignore` resta comunque corretto per quando il lint verrà ripristinato e per il CI.
- Verifica funzionale post-deploy (Railway auto-deploy del branch site-gen/calzoleria-prevenzano): aprire calzoleriaprevenzano.it, accettare il consenso analytics, e in DevTools → Network filtrare `collect` → deve comparire almeno una richiesta a `google-analytics.com/g/collect` con `tid=G-VBM6E09LBH`. In alternativa GA4 Realtime (proprietà 273888156) deve registrare la sessione.
</verification>

<success_criteria>
- Stub gtag corretto: `dataLayer.push(arguments)` al posto di `push(args)`.
- Build TypeScript verde sul file (zero nuovi errori, zero `any`).
- biome-ignore appropriato per l'uso di `arguments`.
- Comportamento circostante invariato (consent gating, logging, idempotenza, config, revoke).
- Post-deploy: richieste `/collect` con `tid=G-VBM6E09LBH` partono (dati in GA4).
</success_criteria>

<output>
After completion, create `.planning/quick/260529-mn1-fix-ga4-stub-gtag-usa-push-array-invece-/260529-mn1-SUMMARY.md`
</output>
