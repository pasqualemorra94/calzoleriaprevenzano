---
phase: quick/260430-of1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/shared/GoogleAnalytics.tsx
  - src/components/shared/index.ts
  - src/routes/__root.tsx
  - .env.example
  - src/routes/ordine-confermato.tsx
  - src/routes/cookie.tsx
  - src/routes/privacy.tsx
autonomous: true
requirements:
  - WAVE2-GA4-COMPONENT
  - WAVE2-GA4-MOUNT
  - WAVE2-GA4-ENV
  - WAVE2-PURCHASE-EVENT
  - WAVE2-POLICY-ALIGN
must_haves:
  truths:
    - "Visitatore senza consenso analytics non genera alcuna richiesta a googletagmanager.com o google-analytics.com"
    - "Visitatore che accetta analytics carica gtag.js, riceve cookie _ga e invia hit pageview"
    - "All'arrivo su /ordine-confermato dopo Stripe redirect, se analytics consentito, viene sparato evento gtag('purchase', { transaction_id, value, currency: 'EUR', items })"
    - "L'evento purchase NON viene sparato due volte in StrictMode/dev (idempotenza via useRef)"
    - "Se VITE_GA4_MEASUREMENT_ID e' assente o vuoto, <GoogleAnalytics /> e' un no-op (return null) senza side effect"
    - "Revoca successiva di analytics via 'Gestisci preferenze cookie' triggera reload e cookie _ga vengono rimossi al prossimo caricamento (perche' GA non si ricarica senza consenso)"
    - "Le pagine /cookie e /privacy non menzionano piu' Plausible Analytics e descrivono GA4 con disclaimer Schrems II / SCC art. 46 GDPR"
  artifacts:
    - path: "src/components/shared/GoogleAnalytics.tsx"
      provides: "Componente side-effect-only che inietta GA4 gtag.js gated da hasConsent('analytics')"
      contains: "loadScriptWithConsent, VITE_GA4_MEASUREMENT_ID, anonymize_ip, allow_google_signals: false"
    - path: "src/components/shared/index.ts"
      provides: "Barrel export aggiornato con GoogleAnalytics"
      contains: "export { GoogleAnalytics }"
    - path: "src/routes/__root.tsx"
      provides: "Mount <GoogleAnalytics /> dopo <Footer /> nel RootComponent"
      contains: "<GoogleAnalytics />"
    - path: ".env.example"
      provides: "Esempio VITE_GA4_MEASUREMENT_ID con commento esplicito su disable-when-empty"
      contains: "VITE_GA4_MEASUREMENT_ID"
    - path: "src/routes/ordine-confermato.tsx"
      provides: "Purchase conversion event in useEffect post-render dati ordine, idempotente via useRef"
      contains: "gtag('event', 'purchase'"
    - path: "src/routes/cookie.tsx"
      provides: "Sezione 4 (Cookie analitici) allineata a GA4 senza menzioni Plausible"
      contains: "Google Analytics 4"
    - path: "src/routes/privacy.tsx"
      provides: "Sezione E (Analisi) allineata + nuova sezione Schrems II / SCC art. 46 GDPR per GA4"
      contains: "Standard Contractual Clauses"
  key_links:
    - from: "src/components/shared/GoogleAnalytics.tsx"
      to: "src/lib/cookieConsent.ts (loadScriptWithConsent + hasConsent + dispatchConsentUpdate)"
      via: "import + funzioni"
      pattern: "loadScriptWithConsent\\(\"analytics\""
    - from: "src/routes/__root.tsx"
      to: "src/components/shared/GoogleAnalytics.tsx"
      via: "JSX render in RootComponent dopo <Footer />"
      pattern: "<GoogleAnalytics\\s*/>"
    - from: "src/routes/ordine-confermato.tsx"
      to: "src/lib/cookieConsent.ts (hasConsent) + window.gtag"
      via: "useEffect guard + chiamata gtag"
      pattern: "hasConsent\\(\"analytics\"\\)"
    - from: "GoogleAnalytics consent-update listener"
      to: "window.location.reload()"
      via: "addEventListener('consent-update', ...) con detail.category === 'analytics' && !detail.consented"
      pattern: "window\\.location\\.reload"
---

<objective>
Wave 2 GDPR/Cookie compliance — implementare GA4 con Consent Mode v2 "basic" gated dal banner cookie (Wave 1 gia' live), aggiungere conversion event `purchase` in `/ordine-confermato`, allineare i testi delle policy `/cookie` e `/privacy` rimuovendo ogni menzione Plausible Analytics e aggiungendo il disclaimer Schrems II / SCC art. 46 GDPR per il trasferimento dati extra-UE.

Purpose: completare lo strato analytics legalmente compliant — niente GA finche' il consenso `analytics` non e' true (basic mode = no "denied pings"), purchase tracking corretto via gtag con `transaction_id` order number, policy testi coerenti con cio' che il sito effettivamente fa (no Plausible mai installato, GA4 con SCC come base).

Output: 5 commit atomici italiani (uno per task), 7 file impattati (1 nuovo: `src/components/shared/GoogleAnalytics.tsx`), zero `any`, auto-disattivazione safe quando env var mancante (deploy senza setup GA preventivo OK).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@docs/superpowers/specs/2026-04-30-gdpr-cookie-compliance-design.md
@src/lib/cookieConsent.ts
@src/components/shared/CookieBanner.tsx
@src/routes/__root.tsx
@src/routes/ordine-confermato.tsx
@src/routes/cookie.tsx
@src/routes/privacy.tsx
@src/components/shared/index.ts
@src/routes/api/orders.by-session.ts
@.env.example

<interfaces>
<!-- Contratti consumati direttamente. NESSUNA esplorazione del codebase necessaria. -->

From `src/lib/cookieConsent.ts`:
```typescript
export type ConsentCategory = "necessary" | "preferences" | "analytics" | "marketing";

export interface CookieConsent {
  necessary: boolean;
  preferences: boolean;
  analytics: boolean;
  marketing: boolean;
  timestamp: number;
  version: number;
}

// Client-side helpers (used by GoogleAnalytics + ordine-confermato)
export function hasConsent(category: Exclude<ConsentCategory, "necessary">): boolean;
export function loadScriptWithConsent(
  category: Exclude<ConsentCategory, "necessary">,
  scriptLoader: () => void,
): void;
export function dispatchConsentUpdate(category: string, consented: boolean): void;
```

The "consent-update" custom event has shape:
```typescript
new CustomEvent("consent-update", { detail: { category: string, consented: boolean } })
```
Dispatched by `CookieBanner` whenever the user saves preferences (lines 43-44 of CookieBanner.tsx — both for analytics and marketing categories, every save).

From `src/routes/api/orders.by-session.ts` (response shape consumed by `/ordine-confermato`):
```typescript
{
  orderNumber: string;        // "PRV-20260430-XXXX" — used as transaction_id
  status: string;
  total: number;              // EUR units (NOT cents — it's Number(order.total) from Prisma Decimal)
  items: Array<{
    name: string;
    quantity: number;
    price: number;            // EUR units per single item (NOT cents)
  }>;
  createdAt: string;
}
```
NOTE: `total` and `price` are gia' in EUR (unita' decimali), non in cents. Quindi gtag value: usa `order.total` direttamente. Items price per gtag: `item.price` direttamente.

From `src/routes/__root.tsx` (RootComponent structure):
```tsx
return (
  <MotionProvider>
    {/* skip link, StructuredData, MegaMenu */}
    <main>...</main>
    {!isAdmin && <Footer />}
    {!isAdmin && <CookieBanner />}    {/* mounted in Wave 1 */}
    {!isAdmin && !isAccount && <MobileBottomNav />}
    {!isAdmin && <MobileSearchOverlay />}
    <Toaster ... />
  </MotionProvider>
);
```
Mount point per `<GoogleAnalytics />`: subito dopo `<CookieBanner />`, SENZA gating `!isAdmin` (GA puo' tracciare anche le route admin se l'admin ha consentito — il componente e' comunque no-op senza consenso). Pero' per coerenza con Wave 1 e per evitare tracking di sezioni private, gate `!isAdmin` come gli altri.
</interfaces>

<constraints_recap>
- Zero `any`. gtag types via `declare global { interface Window { gtag?: (...args: unknown[]) => void; dataLayer?: unknown[]; } }` o equivalente.
- Component auto-disattivante: env var mancante o vuota → return null.
- Consent Mode v2 BASIC: GA NON si carica finche' analytics false. Niente `gtag('consent', 'default', { analytics_storage: 'denied' })` PRIMA del consenso (questo sarebbe advanced mode con denied pings).
- Listener `consent-update`: se `detail.category === "analytics" && detail.consented === false` E gtag e' gia' caricato → `window.location.reload()` (pulisce cookie _ga al prossimo paint dato che GA non si ricaricha senza consenso).
- Idempotenza purchase event: useRef boolean guard per StrictMode dev double-fire.
- Allineamento policy: rimuovere "Plausible Analytics" da cookie.tsx e privacy.tsx. Aggiungere paragrafo Schrems II in privacy.tsx.
- .env.example: aggiungere `VITE_GA4_MEASUREMENT_ID="G-XXXXXXXX"` con commento "empty = analytics disabled".
- Pattern commit messages italiani, scope feat()/chore()/docs() come da spec.
</constraints_recap>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Componente GoogleAnalytics con loader gated da consenso</name>
  <files>src/components/shared/GoogleAnalytics.tsx (NEW), src/components/shared/index.ts</files>
  <behavior>
    - Render: sempre `return null` (puro side-effect component, nessun JSX visibile).
    - useEffect on mount:
      1. Read `import.meta.env.VITE_GA4_MEASUREMENT_ID` (string | undefined). Se vuota o assente → no-op (return).
      2. Definire `loadGtag()` (closure):
         - Inietta `<script async src="https://www.googletagmanager.com/gtag/js?id=${MEASUREMENT_ID}">` nel `document.head` se non gia' presente (check via `document.querySelector('script[src*="googletagmanager.com/gtag/js"]')`).
         - Init `window.dataLayer = window.dataLayer || []`.
         - Definisce `window.gtag = function gtag(){ window.dataLayer!.push(arguments); }` se non gia' definita. (Usa rest args: `function gtag(...args: unknown[]) { window.dataLayer!.push(args); }`.)
         - Chiama: `window.gtag('js', new Date()); window.gtag('config', MEASUREMENT_ID, { anonymize_ip: true, allow_google_signals: false, allow_ad_personalization_signals: false });`
      3. Chiama `loadScriptWithConsent("analytics", loadGtag)` — il loader si aggancia all'evento `consent-update` se consenso non ancora dato.
    - useEffect listener separato (montato in parallelo sullo stesso useEffect o in un secondo useEffect — preferire SECONDO useEffect dedicato per chiarezza):
      1. Listener `(e: Event) => { ... }` su `window.addEventListener("consent-update", ...)`.
      2. Type narrowing: cast a `CustomEvent<{ category: string; consented: boolean }>` solo dopo `e instanceof CustomEvent`. Niente `any`.
      3. Se `detail.category === "analytics" && detail.consented === false && typeof window.gtag === "function"` → `window.location.reload()`.
      4. Cleanup: `removeEventListener` nel return del useEffect.
    - Type augmentation per `window.gtag` e `window.dataLayer`: usare `declare global { interface Window { gtag?: (...args: unknown[]) => void; dataLayer?: unknown[]; } }` in cima al file (dopo gli import). Zero `any`.
    - Export named: `export function GoogleAnalytics(): null { ... }` (return type esplicito `null`, niente `ReactNode` ambiguo).
    - Aggiornare `src/components/shared/index.ts` aggiungendo `export { GoogleAnalytics } from "./GoogleAnalytics";` (alla fine, dopo `ErrorBoundary`).
    - Commit message: `feat(analytics): GoogleAnalytics component gated by consent`.
  </behavior>
  <action>
    1. Creare `src/components/shared/GoogleAnalytics.tsx` (~80 LOC max):
       - Imports: `useEffect` da react, `loadScriptWithConsent`, `hasConsent` (opzionale, per early-exit) da `~/lib/cookieConsent`.
       - `declare global { interface Window { gtag?: (...args: unknown[]) => void; dataLayer?: unknown[]; } }` subito dopo gli import.
       - Costante `MEASUREMENT_ID = import.meta.env.VITE_GA4_MEASUREMENT_ID as string | undefined` (cast esplicito a `string | undefined` perche' `import.meta.env` ha `Record<string, any>` di default — pero' verificare se vite/client types fornisce gia' typing — preferire cast esplicito per sicurezza).
       - Funzione `GoogleAnalytics(): null`:
         - Early return null se `!MEASUREMENT_ID` (componente diventa no-op a livello di render — ma vogliamo SKIP anche gli useEffect, quindi mettere la guard PRIMA degli hook? NO — regole React vietano hook condizionali. Soluzione: chiama gli hook sempre, ma gli hook stessi early-return se `!MEASUREMENT_ID`).
         - useEffect 1 — loader:
           ```ts
           useEffect(() => {
             if (!MEASUREMENT_ID) return;
             const loadGtag = () => {
               // idempotenza inject script
               if (document.querySelector(`script[src*="googletagmanager.com/gtag/js"]`)) return;
               const script = document.createElement("script");
               script.async = true;
               script.src = `https://www.googletagmanager.com/gtag/js?id=${MEASUREMENT_ID}`;
               document.head.appendChild(script);
               window.dataLayer = window.dataLayer || [];
               window.gtag = window.gtag || function gtag(...args: unknown[]) {
                 (window.dataLayer as unknown[]).push(args);
               };
               window.gtag("js", new Date());
               window.gtag("config", MEASUREMENT_ID, {
                 anonymize_ip: true,
                 allow_google_signals: false,
                 allow_ad_personalization_signals: false,
               });
             };
             loadScriptWithConsent("analytics", loadGtag);
           }, []);
           ```
         - useEffect 2 — revoke listener:
           ```ts
           useEffect(() => {
             if (!MEASUREMENT_ID) return;
             const handler = (e: Event) => {
               if (!(e instanceof CustomEvent)) return;
               const detail = e.detail as { category?: string; consented?: boolean } | null;
               if (
                 detail?.category === "analytics" &&
                 detail.consented === false &&
                 typeof window.gtag === "function"
               ) {
                 window.location.reload();
               }
             };
             window.addEventListener("consent-update", handler);
             return () => window.removeEventListener("consent-update", handler);
           }, []);
           ```
         - `return null;`
    2. Aggiornare `src/components/shared/index.ts` — aggiungere riga `export { GoogleAnalytics } from "./GoogleAnalytics";` dopo `export { ErrorBoundary } from "./ErrorBoundary";`.
    3. Verificare typecheck del solo nuovo file con `pnpm typecheck` e confermare baseline `25` errori (NON aumenta — file nuovo zero errori).
    4. Git commit: `feat(analytics): GoogleAnalytics component gated by consent`.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 && grep -c "GoogleAnalytics" src/components/shared/index.ts && test -f src/components/shared/GoogleAnalytics.tsx && grep -E "loadScriptWithConsent\\(\"analytics\"" src/components/shared/GoogleAnalytics.tsx && grep -E "anonymize_ip:\\s*true" src/components/shared/GoogleAnalytics.tsx && ! grep -E ":\\s*any\\b|as any\\b" src/components/shared/GoogleAnalytics.tsx</automated>
  </verify>
  <done>
    - File `src/components/shared/GoogleAnalytics.tsx` esiste, ~80 LOC, esporta `GoogleAnalytics(): null`.
    - Barrel `src/components/shared/index.ts` esporta `GoogleAnalytics`.
    - Zero `any` (grep negativo).
    - `pnpm typecheck` baseline 25 → 25 (nessun nuovo errore introdotto dal file).
    - Commit atomico singolo creato.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Mount GoogleAnalytics in __root.tsx</name>
  <files>src/routes/__root.tsx</files>
  <behavior>
    - Importare `GoogleAnalytics` dal barrel `~/components/shared` (estendere import esistente che gia' contiene `Footer, CookieBanner`).
    - Renderizzare `<GoogleAnalytics />` nel RootComponent subito DOPO `{!isAdmin && <CookieBanner />}` (e prima di `<MobileBottomNav />`).
    - Gating: `{!isAdmin && <GoogleAnalytics />}` — coerente con Footer/CookieBanner/MegaMenu (no tracking sezioni admin).
    - Componente e' comunque no-op se env var assente o consenso analytics false, il gate `!isAdmin` e' belt-and-suspenders.
    - Commit message: `feat(legal): mount GA4 in root layout`.
  </behavior>
  <action>
    1. Editare `src/routes/__root.tsx`:
       - Riga 14 attuale: `import { Footer, CookieBanner } from "~/components/shared";` → diventa: `import { Footer, CookieBanner, GoogleAnalytics } from "~/components/shared";`.
       - Riga 158 attuale: `{!isAdmin && <CookieBanner />}` → aggiungere subito sotto: `{!isAdmin && <GoogleAnalytics />}`.
    2. `pnpm typecheck` per verificare baseline 25 → 25.
    3. Git commit: `feat(legal): mount GA4 in root layout`.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 && grep -E "GoogleAnalytics" src/routes/__root.tsx | wc -l | grep -E "^\\s*2\\s*$" && grep -E "!isAdmin && <GoogleAnalytics" src/routes/__root.tsx</automated>
  </verify>
  <done>
    - `src/routes/__root.tsx` importa `GoogleAnalytics` dal barrel (1 occorrenza nell'import) e lo renderizza con gate `!isAdmin` (1 occorrenza nel JSX) — totale 2 grep match.
    - `pnpm typecheck` baseline preservata.
    - Commit atomico singolo.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Aggiungere VITE_GA4_MEASUREMENT_ID a .env.example</name>
  <files>.env.example</files>
  <behavior>
    - Aggiungere alla fine di `.env.example` una sezione "Analytics" con la riga `VITE_GA4_MEASUREMENT_ID="G-XXXXXXXX"` e un commento esplicativo.
    - Il commento DEVE chiarire: "vuoto/assente = analytics disabilitato (no-op)".
    - Posizionamento: dopo la sezione `# AI — fal.ai / Fashn.ai` (riga 37 attuale).
    - NON modificare nessun'altra riga del file.
    - Commit message: `chore(env): add VITE_GA4_MEASUREMENT_ID example`.
  </behavior>
  <action>
    1. Editare `.env.example` aggiungendo in fondo (dopo riga 37):
       ```
       
       # Analytics — Google Analytics 4 (Consent Mode v2 basic, gated da consenso cookie)
       VITE_GA4_MEASUREMENT_ID="G-XXXXXXXX"  # vuoto o assente = analytics disabilitato (componente <GoogleAnalytics /> diventa no-op)
       ```
    2. Git commit: `chore(env): add VITE_GA4_MEASUREMENT_ID example`.
  </action>
  <verify>
    <automated>grep -E "VITE_GA4_MEASUREMENT_ID" .env.example && grep -E "Analytics.*Google Analytics 4" .env.example && grep -E "no-op" .env.example</automated>
  </verify>
  <done>
    - `.env.example` contiene `VITE_GA4_MEASUREMENT_ID` con commento che spiega il fallback no-op.
    - Commit atomico singolo.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Purchase conversion event in /ordine-confermato.tsx</name>
  <files>src/routes/ordine-confermato.tsx</files>
  <behavior>
    - Aggiungere useEffect dedicato dopo l'useEffect esistente di fetch dati ordine (post riga 45).
    - Trigger: ogni volta che `order` diventa non-null E `hasConsent("analytics")` ritorna true E `window.gtag` e' definita.
    - Idempotenza: usare `useRef<boolean>(false)` (init false) come guard. Al primo fire, set `firedRef.current = true`. Skip se gia' true. Questo previene double-fire in StrictMode dev (dove l'useEffect viene smontato e rimontato due volte).
    - Tipizzazione `window.gtag`: usare il tipo gia' augmentato in Task 1 via `declare global` (il file lo eredita perche' i `declare global` sono module-augmentation cross-file).
    - Payload `gtag('event', 'purchase', {...})`:
      - `transaction_id: order.orderNumber` (es. "PRV-20260430-XXXX")
      - `value: order.total` — gia' in EUR (NOT cents — vedi shape API by-session.ts: `total: Number(order.total)` da Prisma Decimal)
      - `currency: "EUR"`
      - `items: order.items.map((item, idx) => ({ item_id: \`${order.orderNumber}-${idx}\`, item_name: item.name, price: item.price, quantity: item.quantity }))` — `item_id` derivato perche' la shape attuale dell'API non espone product id (l'aggiunta sarebbe un'estensione fuori scope).
    - NIENTE try/catch attorno a gtag — se gtag throw, vogliamo vederlo in dev. In prod gtag non throw mai (Google garantisce no-throw).
    - Commit message: `feat(analytics): purchase conversion event in ordine-confermato`.
  </behavior>
  <action>
    1. Editare `src/routes/ordine-confermato.tsx`:
       - Aggiungere `useRef` all'import esistente da react: `import { useState, useEffect, useRef } from "react";`.
       - Aggiungere import: `import { hasConsent } from "~/lib/cookieConsent";`.
       - Dentro `OrderConfirmedPage()`, dopo la dichiarazione di `error` state (riga ~26), aggiungere: `const purchaseFiredRef = useRef<boolean>(false);`.
       - Aggiungere un useEffect SEPARATO dopo l'useEffect esistente (post riga 45) — NON unire al primo per chiarezza:
         ```ts
         useEffect(() => {
           if (!order) return;
           if (purchaseFiredRef.current) return;
           if (!hasConsent("analytics")) return;
           if (typeof window.gtag !== "function") return;
           purchaseFiredRef.current = true;
           window.gtag("event", "purchase", {
             transaction_id: order.orderNumber,
             value: order.total,
             currency: "EUR",
             items: order.items.map((item, idx) => ({
               item_id: `${order.orderNumber}-${idx}`,
               item_name: item.name,
               price: item.price,
               quantity: item.quantity,
             })),
           });
         }, [order]);
         ```
       - NOTA: `window.gtag` e' tipizzato grazie a `declare global` in `GoogleAnalytics.tsx` (Task 1). Se TypeScript si lamenta perche' `ordine-confermato.tsx` non importa `GoogleAnalytics`, il `declare global` viene comunque visto perche' incluso nel project tsconfig — ma per essere safe, se serve, aggiungere `import "~/components/shared/GoogleAnalytics";` (side-effect import che assicura il caricamento del modulo per types). PREFERIRE non aggiungere side-effect import se typecheck passa senza.
    2. `pnpm typecheck` — baseline 25 → 25.
    3. Git commit: `feat(analytics): purchase conversion event in ordine-confermato`.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 && grep -E "gtag\\(\"event\", \"purchase\"" src/routes/ordine-confermato.tsx && grep -E "purchaseFiredRef" src/routes/ordine-confermato.tsx && grep -E "hasConsent\\(\"analytics\"\\)" src/routes/ordine-confermato.tsx && grep -E "currency: \"EUR\"" src/routes/ordine-confermato.tsx && ! grep -E ":\\s*any\\b|as any\\b" src/routes/ordine-confermato.tsx</automated>
  </verify>
  <done>
    - `src/routes/ordine-confermato.tsx` contiene useEffect con guard (`!order`, `purchaseFiredRef.current`, `!hasConsent("analytics")`, `typeof window.gtag !== "function"`).
    - Payload purchase corretto: `transaction_id`, `value: order.total` (EUR), `currency: "EUR"`, `items` mappati con item_id derivato.
    - `useRef` guard per idempotenza StrictMode.
    - Zero `any`.
    - `pnpm typecheck` baseline preservata.
    - Commit atomico singolo.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Allineare cookie.tsx + privacy.tsx a GA4 (rimuovi Plausible, aggiungi Schrems II)</name>
  <files>src/routes/cookie.tsx, src/routes/privacy.tsx</files>
  <behavior>
    - **cookie.tsx** (sezione 4 "Cookie Analitici", righe ~272-277):
      - Sostituire il paragrafo che menziona "e/o Plausible Analytics" con un testo che descrive SOLO GA4 con anonimizzazione IP built-in (GA4 anonimizza by design — non serve flag `anonymizeIp`, e' UA-only).
      - Riformulare il riferimento al codice `anonymizeIp` (riga 273-274) — in GA4 e' built-in. Sostituire con menzione `anonymize_ip: true` come parametro `gtag('config')` per chiarezza tecnica, MA spiegare che in GA4 e' default.
      - La tabella `COOKIE_TABLE_ANALYTICS` (righe 53-75) e' gia' OK: elenca `_ga`, `_ga_*`, `_gid` con provider Google Analytics, durate corrette. NON modificare la tabella.
    - **privacy.tsx** (sezione E "Analisi e miglioramento del servizio", riga 123):
      - Rimuovere "e/o Plausible Analytics" dalla riga 123.
      - Sostituire con: "Utilizziamo Google Analytics 4 (GA4) con anonimizzazione dell'IP attiva (built-in nella versione 4)."
    - **privacy.tsx** — sezione 8 "Trasferimento dei Dati all'Estero" (righe 258-275): AGGIUNGERE in coda a questa sezione (prima del `</section>` chiusura) un nuovo paragrafo dedicato a GA4/SCC:
      ```
      <p className="mt-4">
        <strong className="text-[var(--color-text)]">Google Analytics 4:</strong> il servizio e' fornito da Google Ireland Limited (titolare europeo, con sede in Irlanda). Il trattamento puo' comportare potenziali trasferimenti di dati negli Stati Uniti d&apos;America, regolati da Standard Contractual Clauses (SCC) approvate dalla Commissione Europea ai sensi dell&apos;art. 46 del GDPR. La base giuridica del trattamento e' il consenso dell&apos;interessato (art. 6, par. 1, lett. a) GDPR), revocabile in qualsiasi momento dalle preferenze cookie. La sentenza Schrems II (CGUE C-311/18, 16 luglio 2020) ha sottolineato la necessita' di valutazioni caso per caso sui trasferimenti extra-UE: il Titolare ha valutato che le SCC adottate da Google offrono garanzie adeguate per la finalita' statistica anonimizzata.
      </p>
      ```
    - **NESSUNA** modifica a `COOKIE_TABLE_TECHNICAL`, `COOKIE_TABLE_MARKETING`, o ad altre sezioni di cookie.tsx/privacy.tsx.
    - Verifica finale: `grep -i "plausible" src/routes/cookie.tsx src/routes/privacy.tsx` deve ritornare ZERO match.
    - Commit message: `docs(legal): align cookie/privacy policy to GA4 + Schrems II disclaimer`.
  </behavior>
  <action>
    1. **cookie.tsx** — modificare il paragrafo alla riga ~272-277 (sezione 4):
       - Testo attuale (righe 272-277):
         ```
         Utilizziamo Google Analytics con l&apos;anonimizzazione dell&apos;indirizzo IP (tramite la funzione
         <code className="mx-1 rounded bg-[var(--color-surface)] px-1.5 py-0.5 text-xs font-mono">anonymizeIp</code>)
         e/o Plausible Analytics, un servizio di analisi privacy-oriented che non utilizza cookie e
         rispetta pienamente il GDPR. Questi cookie vengono installati solo previo tuo consenso esplicito.
         ```
       - Nuovo testo:
         ```
         Utilizziamo Google Analytics 4 (GA4), il quale anonimizza by design l&apos;indirizzo IP degli utenti
         (l&apos;anonimizzazione e&apos; integrata nel prodotto e non richiede flag aggiuntivi, a differenza della
         versione Universal Analytics). Disabilitiamo inoltre i segnali pubblicitari Google
         (<code className="mx-1 rounded bg-[var(--color-surface)] px-1.5 py-0.5 text-xs font-mono">allow_google_signals: false</code>)
         e la personalizzazione degli annunci
         (<code className="mx-1 rounded bg-[var(--color-surface)] px-1.5 py-0.5 text-xs font-mono">allow_ad_personalization_signals: false</code>).
         Questi cookie vengono installati solo previo tuo consenso esplicito.
         ```
    2. **privacy.tsx** — modificare riga 123 (sezione E):
       - Testo attuale: `Analisi statistiche anonimizzate o aggregate sulla navigazione e sull&apos;utilizzo del sito, per migliorare l&apos;esperienza utente, ottimizzare le performance del sito e comprendere le preferenze della clientela. Utilizziamo Google Analytics in versione anonimizzata e/o Plausible Analytics.`
       - Nuovo testo: `Analisi statistiche anonimizzate o aggregate sulla navigazione e sull&apos;utilizzo del sito, per migliorare l&apos;esperienza utente, ottimizzare le performance del sito e comprendere le preferenze della clientela. Utilizziamo Google Analytics 4 (GA4) con anonimizzazione dell&apos;IP attiva by design e segnali pubblicitari/personalizzazione annunci disabilitati.`
    3. **privacy.tsx** — aggiungere paragrafo Schrems II/SCC alla fine della sezione 8 "Trasferimento dei Dati all'Estero" (subito prima del `</section>` di chiusura, ~riga 275):
       ```jsx
       <p className="mt-4">
         <strong className="text-[var(--color-text)]">Google Analytics 4:</strong> il servizio &egrave; fornito da Google Ireland Limited (titolare europeo, con sede in Irlanda). Il trattamento pu&ograve; comportare potenziali trasferimenti di dati negli Stati Uniti d&apos;America, regolati da Standard Contractual Clauses (SCC) approvate dalla Commissione Europea ai sensi dell&apos;art. 46 del GDPR. La base giuridica del trattamento &egrave; il consenso dell&apos;interessato (art. 6, par. 1, lett. a) GDPR), revocabile in qualsiasi momento dalle preferenze cookie. La sentenza Schrems II (CGUE C-311/18, 16 luglio 2020) ha sottolineato la necessit&agrave; di valutazioni caso per caso sui trasferimenti extra-UE: il Titolare ha valutato che le SCC adottate da Google offrono garanzie adeguate per la finalit&agrave; statistica anonimizzata qui descritta.
       </p>
       ```
    4. Verifica anti-Plausible: `grep -i "plausible" src/routes/cookie.tsx src/routes/privacy.tsx` deve essere VUOTO.
    5. `pnpm typecheck` — baseline 25 → 25 (modifiche solo a JSX text content, zero impatto types).
    6. Git commit: `docs(legal): align cookie/privacy policy to GA4 + Schrems II disclaimer`.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 && test -z "$(grep -i plausible src/routes/cookie.tsx src/routes/privacy.tsx)" && grep -E "Google Analytics 4 \\(GA4\\)" src/routes/cookie.tsx && grep -E "Standard Contractual Clauses" src/routes/privacy.tsx && grep -E "Schrems II" src/routes/privacy.tsx && grep -E "Google Ireland" src/routes/privacy.tsx && grep -E "art\\. 46.*GDPR" src/routes/privacy.tsx</automated>
  </verify>
  <done>
    - Zero menzioni "Plausible" in cookie.tsx e privacy.tsx (grep -i vuoto).
    - cookie.tsx sezione 4 menziona "Google Analytics 4 (GA4)" e i flag `allow_google_signals: false` / `allow_ad_personalization_signals: false`.
    - privacy.tsx sezione E menziona "Google Analytics 4 (GA4) con anonimizzazione dell'IP attiva by design".
    - privacy.tsx sezione 8 contiene il paragrafo Schrems II con: Google Ireland Limited, Standard Contractual Clauses, art. 46 GDPR, art. 6.1.a GDPR, sentenza CGUE C-311/18.
    - `pnpm typecheck` baseline preservata.
    - Commit atomico singolo.
  </done>
</task>

</tasks>

<verification>
**Smoke verification post-task5 (NON blocking — manuale, opzionale):**

Lo spec dichiara che la verifica end-to-end di GA4 richiede browser DevTools + GA4 Realtime dashboard, e necessita che l'utente abbia gia' creato la property GA4 e settato `VITE_GA4_MEASUREMENT_ID` su Railway (out of scope codice). Quindi:

1. **Senza setup utente** (env var assente in `.env` locale):
   - `pnpm dev` → aprire `http://localhost:3000` in incognito.
   - DevTools → Network → filtrare "googletagmanager" — ZERO richieste sia prima che dopo aver accettato cookie analytics. Componente e' no-op.
   - Console: nessun errore relativo a `gtag` o `dataLayer`.

2. **Con setup utente** (Railway con `VITE_GA4_MEASUREMENT_ID=G-...`):
   - Aprire sito prod in incognito → "Rifiuta tutto" sul banner → DevTools Network: ZERO richieste googletagmanager.
   - Aprire sito prod in incognito → "Accetta tutti" → reload → Network mostra `gtag/js?id=G-...` + `g/collect` parte. Cookie `_ga` e `_ga_<container>` presenti.
   - GA4 Realtime dashboard mostra il device entro 30s.
   - Acquistare prodotto test → su `/ordine-confermato` → GA4 Realtime → tab "Eventi" mostra `purchase` con `transaction_id` corretto.
   - Footer "Gestisci preferenze cookie" → rifiuta analytics → reload → cookie `_ga` rimossi al primo render senza GA.

3. **Typecheck globale** (blocking):
   - `pnpm typecheck` baseline 25 → 25. Zero nuovi errori.

4. **Anti-regressione typescript zero-any**:
   - `grep -rE ":\\s*any\\b|as any\\b" src/components/shared/GoogleAnalytics.tsx src/routes/ordine-confermato.tsx` → 0 match (su parti modificate; il file ordine-confermato puo' avere `any` pre-esistenti — verificare solo che le righe NUOVE siano clean).

**Verifica wave-level (auto, blocking):**
- 5 commit atomici creati nell'ordine spec, ognuno tocca solo i file dichiarati nel rispettivo task.
- 7 file impattati totali, 1 nuovo (`src/components/shared/GoogleAnalytics.tsx`).
- Branch `site-gen/calzoleria-prevenzano` avanza di 5 commit.
</verification>

<success_criteria>
- [ ] `src/components/shared/GoogleAnalytics.tsx` creato (~80 LOC, zero `any`, no-op se env var assente)
- [ ] `src/components/shared/index.ts` esporta `GoogleAnalytics`
- [ ] `src/routes/__root.tsx` monta `<GoogleAnalytics />` dopo `<CookieBanner />` con gate `!isAdmin`
- [ ] `.env.example` contiene `VITE_GA4_MEASUREMENT_ID="G-XXXXXXXX"` con commento no-op
- [ ] `src/routes/ordine-confermato.tsx` spara `gtag('event', 'purchase', {...})` con guard hasConsent + useRef idempotency
- [ ] `src/routes/cookie.tsx` e `src/routes/privacy.tsx` non menzionano piu' Plausible
- [ ] `src/routes/privacy.tsx` contiene paragrafo Schrems II / SCC art. 46 GDPR
- [ ] `pnpm typecheck` baseline 25 → 25 (zero regressioni)
- [ ] 5 commit atomici italiani con messaggi spec-aligned
- [ ] Push NON eseguito (constraint utente standard, decisione Railway pending)
</success_criteria>

<output>
After completion, create `.planning/quick/260430-of1-wave-2-gdpr-cookie-compliance-ga4-con-co/260430-of1-SUMMARY.md` documentando:
- I 5 commit (hash + message)
- File impattati con LOC delta
- Risultato typecheck baseline
- Stato push (eseguito o pending utente)
- Cosa serve all'utente per attivare GA4 in produzione (creare property GA4 + settare `VITE_GA4_MEASUREMENT_ID` su Railway env vars)
- Wave 3 status (DEFERRED come da spec — diritti GDPR export/delete, newsletter double opt-in, admin /consensi, signup ConsentLog)
</output>
