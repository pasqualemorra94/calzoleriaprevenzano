---
phase: quick/260501-h9l
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/shared/CookieBanner.tsx
  - src/components/product/ProductGallery.tsx
  - src/components/product/MobileImageCarousel.tsx
  - src/components/product/MobileImageZoomModal.tsx
  - src/components/product/MobileStickyAddToCart.tsx
  - src/components/product/VariantSelector.tsx
  - src/components/product/index.ts
  - src/routes/prodotti.$slug.tsx
autonomous: true
requirements:
  - MUX-01  # Cookie banner compact mobile (testo + padding + layout bottoni)
  - MUX-02  # Cookie preferences modal full-screen su mobile
  - MUX-03  # MobileImageCarousel con scroll-snap nativo + dots
  - MUX-04  # MobileImageZoomModal con pinch-zoom nativo
  - MUX-05  # Refactor ProductGallery split desktop/mobile
  - MUX-06  # MobileStickyAddToCart sticky bottom bar
  - MUX-07  # Mount sticky bar + nascondi CTA inline + body padding
must_haves:
  truths:
    - "Su mobile (≤768px) il banner cookie occupa <30% viewport (≤220px altezza)"
    - "Su mobile il pannello preferenze cookie si apre come modal full-screen, non accordion inline"
    - "Su mobile la galleria prodotto è un carousel swipe full-width con dots indicator"
    - "Tap su foto galleria mobile apre modal zoom con pinch-zoom nativo browser"
    - "Su mobile il CTA Aggiungi-al-carrello è sticky in fondo allo schermo, sempre visibile"
    - "Su mobile NON appare il CTA inline duplicato dentro VariantSelector"
    - "Il footer non viene coperto dalla sticky bar (padding-bottom 96px sul wrapper)"
    - "Su desktop (≥768px) il layout esistente è invariato in tutti e 3 i sotto-feature"
    - "pnpm typecheck passa senza nuovi errori (baseline 25 preservata)"
  artifacts:
    - path: "src/components/shared/CookieBanner.tsx"
      provides: "Banner cookie con layout responsive mobile/desktop + modal preferences mobile"
      contains: "md:hidden, hidden md:block, fixed inset-0"
    - path: "src/components/product/MobileImageCarousel.tsx"
      provides: "Carousel mobile con scroll-snap CSS + IntersectionObserver per sync indice"
      min_lines: 100
    - path: "src/components/product/MobileImageZoomModal.tsx"
      provides: "Modal zoom mobile con touch-action: pinch-zoom"
      min_lines: 60
    - path: "src/components/product/MobileStickyAddToCart.tsx"
      provides: "Sticky bottom bar con prezzo + quantity stepper + CTA dinamico 5 stati"
      min_lines: 90
    - path: "src/components/product/ProductGallery.tsx"
      provides: "Wrapper che renderizza DesktopGallery (md:block) + MobileImageCarousel (md:hidden)"
      contains: "hidden md:block, md:hidden"
    - path: "src/components/product/VariantSelector.tsx"
      provides: "Blocco add-to-cart riga 142 con classe hidden md:flex"
      contains: "hidden md:flex"
    - path: "src/routes/prodotti.$slug.tsx"
      provides: "Mount MobileStickyAddToCart + wrapper esterno con pb-24 md:pb-0"
      contains: "MobileStickyAddToCart, pb-24"
    - path: "src/components/product/index.ts"
      provides: "Barrel export aggiornato con 3 nuovi componenti mobile"
      contains: "MobileImageCarousel, MobileImageZoomModal, MobileStickyAddToCart"
  key_links:
    - from: "src/components/product/ProductGallery.tsx"
      to: "src/components/product/MobileImageCarousel.tsx"
      via: "import + render condizionale Tailwind md:hidden"
      pattern: "MobileImageCarousel.*className.*md:hidden"
    - from: "src/components/product/MobileImageCarousel.tsx"
      to: "src/components/product/MobileImageZoomModal.tsx"
      via: "useState zoomOpen + click handler su zoom icon"
      pattern: "MobileImageZoomModal"
    - from: "src/routes/prodotti.$slug.tsx"
      to: "src/components/product/MobileStickyAddToCart.tsx"
      via: "import + mount sotto RelatedProducts con stesse props del VariantSelector"
      pattern: "MobileStickyAddToCart.*onAddToCart=\\{handleAddToCart\\}"
    - from: "src/components/shared/CookieBanner.tsx"
      to: "src/components/shared/CookiePreferencesPanel.tsx"
      via: "import + render dentro modal mobile (fixed inset-0) E inline desktop"
      pattern: "<CookiePreferencesPanel"
---

<objective>
Mobile UX redesign della pagina prodotto + cookie banner. Tre sotto-feature indipendenti tematicamente unite (mobile-only): (1) compattare il cookie banner per ridurlo da 50-60% viewport a <30%, (2) sostituire la galleria thumbnails con un carousel swipe nativo edge-to-edge + tap-to-zoom modal, (3) introdurre una sticky add-to-cart bar in fondo allo schermo per pattern Zalando/Asos sempre raggiungibile durante esplorazione varianti.

Purpose: Le pagine prodotto sono il cuore della conversion (unico modo di acquistare). Pattern attuali su mobile sono percepiti come "veramente brutti" dall'utente: thumbnails impilate scrollano la foto principale fuori viewport, long-press magnifier crea popup confondenti `top: -136px`, CTA `flex-col` impilato scrolla via mentre l'utente esplora le varianti. Cookie banner copre l'above-the-fold della home.

Output: 7 commit atomici italiani, 3 file nuovi (`MobileImageCarousel.tsx`, `MobileImageZoomModal.tsx`, `MobileStickyAddToCart.tsx`), 5 file modificati (CookieBanner, ProductGallery, VariantSelector, prodotti.$slug, index.ts barrel). Mobile detection esclusivamente via classi Tailwind `md:hidden` / `hidden md:block` (no JS, no SSR mismatch). Carousel con `scroll-snap-type: x mandatory` CSS nativo (no librerie esterne). Zoom con `touch-action: pinch-zoom` CSS nativo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@docs/superpowers/specs/2026-04-30-mobile-ux-product-page-design.md
@./CLAUDE.md
@src/components/shared/CookieBanner.tsx
@src/components/shared/CookiePreferencesPanel.tsx
@src/components/product/ProductGallery.tsx
@src/components/product/VariantSelector.tsx
@src/components/product/index.ts
@src/routes/prodotti.$slug.tsx
@src/styles/design-tokens.css

<interfaces>
<!-- Z-index variables verificate in src/styles/design-tokens.css — TUTTE PRESENTI -->
<!-- Nessuna variable da aggiungere, usare quelle esistenti -->

```css
--z-base: 0;
--z-above: 10;
--z-sticky: 40;    /* MobileStickyAddToCart bottom bar */
--z-navbar: 50;
--z-overlay: 60;   /* Mega menu mobile drawer */
--z-modal: 70;     /* MobileImageZoomModal + Cookie preferences modal mobile */
--z-toast: 80;     /* Cookie banner (più alto, sopra sticky bar quando visibile) */
```

Gerarchia z-index attesa runtime (dal basso verso alto):
- Contenuto pagina: 0
- Sticky add-to-cart bar mobile: 40 (--z-sticky)
- Mega menu mobile drawer: 60 (--z-overlay)
- Cookie preferences modal mobile: 70 (--z-modal)
- Mobile zoom foto modal: 70 (--z-modal)
- Cookie banner bottom: 80 (--z-toast) — copre la sticky bar finché utente non sceglie

<!-- Props/types principali da rispettare in tutti i nuovi componenti -->

From src/components/product/ProductGallery.tsx:
```typescript
export interface ProductImage {
  id: string;
  url: string;
  alt: string | null;
  width: number | null;
  height: number | null;
}

interface ProductGalleryProps {
  images: ProductImage[];
  selectedIndex: number;
  onSelect: (index: number) => void;
  productName: string;
}
```

From src/components/product/VariantSelector.tsx (handler signature passati come props):
```typescript
quantity: number;
cartStatus: "idle" | "loading" | "success";
canAddToCart: boolean;
onSetQuantity: (q: number) => number;
onAddToCart: () => void;
priceBreakdown: { base: number; optionsTotal: number; total: number };
effectiveStock: number;
allVisibleGroupsSelected: boolean;
```

From src/components/product/index.ts (barrel da estendere):
```typescript
export { ProductGallery } from "./ProductGallery";
export type { ProductImage } from "./ProductGallery";
export { VariantSelector } from "./VariantSelector";
export type { ProductVariant, OptionGroup } from "./VariantSelector";
export { RelatedProducts } from "./RelatedProducts";
export type { ProductListItem } from "./RelatedProducts";
```

Convenzioni progetto da rispettare (CLAUDE.md):
- Zero `any`, zero `as any`, zero `Record<string, any>`
- shadcn/ui components dove possibile (NO raw HTML form)
- Lingua italiana per UI/commenti, English per identifier
- CSS custom properties (NO hex values hardcoded)
- Components max 200 LOC (eccezione tollerata per MobileImageCarousel ~150 e MobileStickyAddToCart ~120)
- Commit messages: italiani con prefix `feat(scope):` o `refactor(scope):`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Cookie banner compact mobile (testo + padding + layout bottoni)</name>
  <files>src/components/shared/CookieBanner.tsx</files>
  <action>
Modificare CookieBanner.tsx per renderlo compact su mobile mantenendo desktop invariato. Implementa MUX-01.

Modifiche puntuali (dentro l'unico return della funzione `CookieBanner`):

1. Riga 81 — wrapper esterno: cambiare `p-4 md:p-6` → `p-3 md:p-6`.

2. Riga 83 — card interna: cambiare `p-6` → `p-4 md:p-6`.

3. Riga 84-90 — bottone X chiudi: aggiungere classi `hidden md:block` (su mobile il bottone "Rifiuta" assolve la stessa funzione semantica). Risultato: `className="absolute right-4 top-4 hidden md:block rounded-md p-1 text-[var(--color-text-muted)] transition-colors hover:text-[var(--color-text)]"`.

4. Riga 92-105 — testo: SOSTITUIRE l'intero blocco `<div className="flex-1 mb-5 pr-8">` con DUE blocchi mutuamente esclusivi:

   ```jsx
   {/* Mobile: testo conciso, no h3 */}
   <div className="flex-1 mb-3 md:hidden">
     <p className="text-sm text-[var(--color-text-secondary)] leading-relaxed">
       Usiamo cookie tecnici e, con il tuo consenso, analitici.{" "}
       <a href="/cookie" className="text-[var(--color-primary)] underline underline-offset-2 hover:text-[var(--color-primary-dark)]">
         Cookie Policy
       </a>
     </p>
   </div>

   {/* Desktop: layout completo invariato */}
   <div className="hidden md:block flex-1 mb-5 pr-8">
     <h3 className="font-display text-base font-semibold text-[var(--color-text)] mb-2">
       Rispettiamo la tua privacy
     </h3>
     <p className="text-sm text-[var(--color-text-secondary)] leading-relaxed">
       Utilizziamo cookie tecnici necessari al funzionamento del sito e, previo tuo consenso,
       cookie analitici e di marketing per migliorare la tua esperienza.
       Per saperne di più consulta la nostra{" "}
       <a href="/cookie" className="text-[var(--color-primary)] underline underline-offset-2 hover:text-[var(--color-primary-dark)]">
         Cookie Policy
       </a>
       .
     </p>
   </div>
   ```

5. Riga 117-160 — blocco bottoni: SOSTITUIRE l'intero `<div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-end">` con DUE blocchi separati per mobile/desktop:

   ```jsx
   {/* MOBILE: 2 bottoni primari + link Personalizza sotto */}
   {!showPreferences && (
     <div className="md:hidden">
       <div className="flex gap-2">
         <button
           onClick={handleRejectAll}
           className="flex-1 h-10 rounded-md border border-[var(--color-border)] bg-transparent text-sm font-medium text-[var(--color-text)] transition-colors hover:bg-[var(--color-muted)]"
         >
           Rifiuta
         </button>
         <button
           onClick={handleAcceptAll}
           className="flex-1 h-10 rounded-md bg-[var(--color-primary)] text-sm font-medium text-[var(--color-primary-foreground)] transition-colors hover:bg-[var(--color-primary-dark)]"
         >
           Accetta
         </button>
       </div>
       <div className="mt-2 text-center">
         <button
           onClick={() => setShowPreferences(true)}
           className="text-xs underline text-[var(--color-text-muted)] hover:text-[var(--color-text)]"
         >
           Personalizza preferenze
         </button>
       </div>
     </div>
   )}

   {/* DESKTOP: layout esistente invariato (3 bottoni in fila) */}
   <div className="hidden md:flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-end">
     {/* INCOLLA QUI il blocco identico riga 117-160 ORIGINALE (3 bottoni desktop + showPreferences logic) */}
   </div>
   ```

6. Il rendering del `<CookiePreferencesPanel />` inline (riga 107-114) per ora resta INVARIATO — il modal mobile lo gestiamo nel Task 2 con un blocco condizionale aggiuntivo. Su mobile, finché non implementi Task 2, il pannello inline NON deve apparire perché il bottone "Personalizza preferenze" mobile nel blocco aggiunto sopra setta `showPreferences=true` e il pannello inline viene renderizzato. ACCETTABILE come stato intermedio in Task 1, sarà sostituito dal modal in Task 2.

Per regola Rule 1 zero-any: nessun `any` in questo task (solo modifiche JSX).

NON toccare: `useState`, `useCallback`, `persistAndClose`, gestione consent, analytics dispatch.

Commit message: `feat(banner): cookie banner compact mobile (testo, padding, layout bottoni)`
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>
- CookieBanner.tsx compila senza errori (zero new TS errors)
- Su viewport iPhone 12 (390x844): banner mostra paragrafo conciso (no h3), 2 bottoni "Rifiuta"/"Accetta" side-by-side full-width, link "Personalizza preferenze" sotto centrato, no bottone X
- Su viewport ≥768px: layout originale identico (h3 "Rispettiamo la tua privacy", paragrafo lungo, 3 bottoni in fila + Personalizza inline + bottone X)
- Altezza banner mobile misurata in DevTools ≤220px
- Click "Personalizza preferenze" mobile setta showPreferences=true (pannello inline appare provvisoriamente — sarà sostituito da modal in Task 2)
- Commit `feat(banner): cookie banner compact mobile (testo, padding, layout bottoni)` creato
  </done>
</task>

<task type="auto">
  <name>Task 2: Cookie preferences modal full-screen su mobile</name>
  <files>src/components/shared/CookieBanner.tsx</files>
  <action>
Trasformare il pannello preferenze in modal full-screen su mobile, mantenendo desktop inline. Implementa MUX-02.

Modifiche dentro `CookieBanner.tsx`:

1. Aggiungere body scroll lock effect quando `showPreferences && viewport mobile`. NOTA: viewport detection NO JS (decision spec). Soluzione: applicare body lock SEMPRE quando showPreferences è true, perché su desktop il pannello è inline (non blocca nulla critico) e su mobile il modal lo richiede. Usa `useEffect`:

   ```typescript
   useEffect(() => {
     if (!showPreferences) return;
     const original = document.body.style.overflow;
     // Solo mobile (md breakpoint = 768px). Verifica via matchMedia.
     const mq = window.matchMedia("(max-width: 767px)");
     if (mq.matches) {
       document.body.style.overflow = "hidden";
     }
     return () => {
       document.body.style.overflow = original;
     };
   }, [showPreferences]);
   ```

   `matchMedia` è OK qui perché è dentro `useEffect` (client-only, no SSR mismatch).

2. Modificare il rendering del pannello preferences (riga 107-114 originale, già intermedio post-Task 1). SOSTITUIRE il blocco `{showPreferences && (<CookiePreferencesPanel ... />)}` con DUE rendering condizionali:

   ```jsx
   {/* DESKTOP: pannello inline (≥md) */}
   {showPreferences && (
     <div className="hidden md:block">
       <CookiePreferencesPanel
         analyticsEnabled={analyticsEnabled}
         marketingEnabled={marketingEnabled}
         onAnalyticsChange={setAnalyticsEnabled}
         onMarketingChange={setMarketingEnabled}
       />
     </div>
   )}

   {/* MOBILE: modal full-screen (<md) */}
   {showPreferences && (
     <div
       className="md:hidden fixed inset-0 z-[var(--z-modal)] bg-black/50 flex items-center justify-center p-4"
       role="dialog"
       aria-modal="true"
       aria-label="Preferenze cookie"
       onClick={(e) => {
         // Click sul backdrop chiude
         if (e.target === e.currentTarget) setShowPreferences(false);
       }}
     >
       <div className="bg-[var(--color-surface)] rounded-lg w-[90vw] max-w-md max-h-[80vh] overflow-y-auto p-6 relative">
         <div className="flex items-center justify-between mb-4">
           <h3 className="font-display text-base font-semibold text-[var(--color-text)]">
             Preferenze cookie
           </h3>
           <button
             type="button"
             onClick={() => setShowPreferences(false)}
             aria-label="Chiudi preferenze"
             className="rounded-md p-1 text-[var(--color-text-muted)] hover:text-[var(--color-text)]"
           >
             <X className="h-4 w-4" />
           </button>
         </div>
         <CookiePreferencesPanel
           analyticsEnabled={analyticsEnabled}
           marketingEnabled={marketingEnabled}
           onAnalyticsChange={setAnalyticsEnabled}
           onMarketingChange={setMarketingEnabled}
         />
         <div className="mt-4 flex gap-2">
           <button
             type="button"
             onClick={() => setShowPreferences(false)}
             className="flex-1 h-10 rounded-md border border-[var(--color-border)] bg-transparent text-sm font-medium text-[var(--color-text)] hover:bg-[var(--color-muted)]"
           >
             Annulla
           </button>
           <button
             type="button"
             onClick={handleSavePreferences}
             className="flex-1 h-10 rounded-md bg-[var(--color-primary)] text-sm font-medium text-[var(--color-primary-foreground)] hover:bg-[var(--color-primary-dark)]"
           >
             Salva
           </button>
         </div>
       </div>
     </div>
   )}
   ```

3. Aggiungere ESC key handler globale per chiudere modal mobile:

   ```typescript
   useEffect(() => {
     if (!showPreferences) return;
     const onKey = (e: KeyboardEvent) => {
       if (e.key === "Escape") setShowPreferences(false);
     };
     document.addEventListener("keydown", onKey);
     return () => document.removeEventListener("keydown", onKey);
   }, [showPreferences]);
   ```

NON modificare: handler `handleSavePreferences`, persistenza, analytics dispatch — riutilizzati così come sono. NON modificare il blocco bottoni mobile creato in Task 1 (Personalizza preferenze già aperto correttamente).

Per Rule 1 zero-any: tipo `KeyboardEvent` esplicito su `onKey`, nessun `any`.

Commit message: `feat(banner): cookie preferences modal full-screen su mobile`
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>
- Su viewport iPhone 12: click "Personalizza preferenze" apre modal centrato fixed inset-0 z-modal con backdrop nero opaco; body NON scrolla (overflow:hidden); click fuori dal card chiude; tasto ESC chiude
- Su viewport ≥768px: click "Personalizza" mostra pannello inline come prima (NON modal); body scrolla normalmente
- Toggle analytics/marketing dentro modal mobile funzionano
- "Salva" persiste consent + chiude banner
- "Annulla" chiude modal mantenendo banner aperto in fase iniziale (rifiuta/accetta/personalizza)
- pnpm typecheck passa
- Commit `feat(banner): cookie preferences modal full-screen su mobile` creato
  </done>
</task>

<task type="auto">
  <name>Task 3: MobileImageCarousel con scroll-snap nativo + dots</name>
  <files>src/components/product/MobileImageCarousel.tsx, src/components/product/index.ts</files>
  <action>
Creare nuovo componente `MobileImageCarousel.tsx` con scroll-snap CSS nativo + IntersectionObserver per sync indice + dots cliccabili. Implementa MUX-03.

File NEW `src/components/product/MobileImageCarousel.tsx` (~150 LOC max):

```typescript
import type { ReactNode } from "react";
import { useEffect, useRef, useCallback, useState } from "react";
import { ZoomIn, ShoppingBag } from "lucide-react";
import { cn } from "~/lib/utils/cn";
import type { ProductImage } from "./ProductGallery";
import { MobileImageZoomModal } from "./MobileImageZoomModal";

interface MobileImageCarouselProps {
  images: ProductImage[];
  selectedIndex: number;
  onSelect: (index: number) => void;
  productName: string;
  className?: string;
}

export function MobileImageCarousel({
  images, selectedIndex, onSelect, productName, className,
}: MobileImageCarouselProps): ReactNode {
  const containerRef = useRef<HTMLDivElement>(null);
  const slidesRef = useRef<Array<HTMLImageElement | null>>([]);
  const [zoomOpen, setZoomOpen] = useState(false);
  const programmaticScrollRef = useRef(false);

  // Sync selectedIndex parent state via IntersectionObserver (threshold 0.6)
  useEffect(() => {
    if (!containerRef.current) return;
    const observer = new IntersectionObserver(
      (entries) => {
        if (programmaticScrollRef.current) return; // ignore during scrollIntoView
        const visible = entries.find((e) => e.intersectionRatio > 0.6);
        if (visible) {
          const idxAttr = visible.target.getAttribute("data-index");
          if (idxAttr === null) return;
          const index = Number(idxAttr);
          if (!Number.isNaN(index) && index !== selectedIndex) {
            onSelect(index);
          }
        }
      },
      { root: containerRef.current, threshold: [0.6] },
    );
    for (const slide of slidesRef.current) {
      if (slide) observer.observe(slide);
    }
    return () => observer.disconnect();
  }, [selectedIndex, onSelect, images.length]);

  // Programmatic scroll quando selectedIndex cambia da esterno (es. variant selection)
  useEffect(() => {
    const target = slidesRef.current[selectedIndex];
    if (!target) return;
    programmaticScrollRef.current = true;
    target.scrollIntoView({ behavior: "smooth", inline: "center", block: "nearest" });
    const t = setTimeout(() => { programmaticScrollRef.current = false; }, 600);
    return () => clearTimeout(t);
  }, [selectedIndex]);

  const handleDotClick = useCallback((i: number) => {
    onSelect(i);
  }, [onSelect]);

  if (!images.length) {
    return (
      <div className={cn("relative aspect-square overflow-hidden bg-[var(--color-muted)]", className)}>
        <div className="flex h-full w-full items-center justify-center text-[var(--color-text-muted)]">
          <ShoppingBag className="h-16 w-16" />
        </div>
      </div>
    );
  }

  const current = images[selectedIndex];

  return (
    <div className={cn("relative", className)}>
      {/* Carousel scroll container (edge-to-edge negative margin) */}
      <div
        ref={containerRef}
        className="relative -mx-[var(--page-padding-x)] flex overflow-x-auto"
        style={{
          scrollSnapType: "x mandatory",
          scrollSnapStop: "always",
          scrollbarWidth: "none",
          WebkitOverflowScrolling: "touch",
        }}
      >
        <style>{`
          .mob-carousel-scroll::-webkit-scrollbar { display: none; }
        `}</style>
        {images.map((img, i) => (
          <img
            key={img.id}
            ref={(el) => { slidesRef.current[i] = el; }}
            data-index={i}
            src={img.url}
            alt={img.alt ?? `${productName} ${i + 1}`}
            className="aspect-square w-full flex-shrink-0 object-cover"
            style={{ scrollSnapAlign: "center" }}
            draggable={false}
            loading={i === 0 ? "eager" : "lazy"}
          />
        ))}

        {/* Zoom icon overlay */}
        <button
          type="button"
          onClick={() => setZoomOpen(true)}
          aria-label="Ingrandisci immagine"
          className="absolute bottom-3 right-3 flex h-9 w-9 items-center justify-center rounded-full bg-black/40 text-white backdrop-blur-sm hover:bg-black/60"
        >
          <ZoomIn className="h-4 w-4" />
        </button>
      </div>

      {/* Dots + counter sotto */}
      {images.length > 1 && (
        <div className="mt-3 flex flex-col items-center gap-1.5">
          <div className="flex gap-1.5">
            {images.map((img, i) => (
              <button
                key={img.id}
                type="button"
                onClick={() => handleDotClick(i)}
                aria-label={`Vai a immagine ${i + 1}`}
                aria-current={i === selectedIndex}
                className={cn(
                  "h-2 w-2 rounded-full transition-all",
                  i === selectedIndex
                    ? "bg-[var(--color-primary)] w-4"
                    : "bg-[var(--color-border)] hover:bg-[var(--color-text-muted)]",
                )}
              />
            ))}
          </div>
          <span className="text-xs text-[var(--color-text-muted)]">
            {selectedIndex + 1} / {images.length}
          </span>
        </div>
      )}

      {/* Zoom modal */}
      {zoomOpen && current && (
        <MobileImageZoomModal
          src={current.url}
          alt={current.alt ?? productName}
          onClose={() => setZoomOpen(false)}
        />
      )}
    </div>
  );
}
```

NOTA: Importa `MobileImageZoomModal` da `./MobileImageZoomModal` — verrà creato in Task 4. Il typecheck di Task 3 fallirà finché Task 4 non è completato. È accettato perché il commit di Task 3 è atomico e logicamente "introduce il carousel". In alternativa puoi unire questo task con Task 4 in un singolo commit ma la spec richiede commit separati (vedi spec § "Atomic commits previsti" punti 3 e 4).

PROCEDURA SUGGERITA: Crea PRIMA il file `MobileImageZoomModal.tsx` come stub minimale (solo signature corretta) per evitare typecheck failure intermedio, POI completa carousel, POI commit Task 3, POI completa zoom in Task 4 e commit Task 4. Lo stub iniziale di MobileImageZoomModal:

```typescript
// stub temporaneo, verrà completato in Task 4
import type { ReactNode } from "react";
interface Props { src: string; alt: string; onClose: () => void; }
export function MobileImageZoomModal({ onClose: _onClose }: Props): ReactNode {
  return null;
}
```

Aggiorna anche `src/components/product/index.ts`:

```typescript
export { ProductGallery } from "./ProductGallery";
export type { ProductImage } from "./ProductGallery";
export { VariantSelector } from "./VariantSelector";
export type { ProductVariant, OptionGroup } from "./VariantSelector";
export { RelatedProducts } from "./RelatedProducts";
export type { ProductListItem } from "./RelatedProducts";
export { MobileImageCarousel } from "./MobileImageCarousel";
```

(MobileImageZoomModal verrà aggiunto al barrel in Task 4, MobileStickyAddToCart in Task 6.)

Per Rule 1 zero-any: tutte le ref tipizzate (`HTMLDivElement`, `HTMLImageElement | null`), `Number()` con `Number.isNaN` guard.

Commit message: `feat(product): MobileImageCarousel con scroll-snap nativo + dots`
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>
- File `src/components/product/MobileImageCarousel.tsx` esiste, ~120-150 LOC
- File `src/components/product/MobileImageZoomModal.tsx` esiste (stub minimale che ritorna null)
- Barrel `index.ts` esporta `MobileImageCarousel`
- pnpm typecheck passa (zero new errors)
- Componente non ancora montato — render condizionale verrà aggiunto in Task 5
- Commit `feat(product): MobileImageCarousel con scroll-snap nativo + dots` creato
  </done>
</task>

<task type="auto">
  <name>Task 4: MobileImageZoomModal con pinch-zoom nativo</name>
  <files>src/components/product/MobileImageZoomModal.tsx, src/components/product/index.ts</files>
  <action>
Sostituire lo stub creato in Task 3 con l'implementazione completa del modal zoom mobile con `touch-action: pinch-zoom` nativo browser. Implementa MUX-04.

File EDIT `src/components/product/MobileImageZoomModal.tsx` (~80 LOC):

```typescript
import type { ReactNode } from "react";
import { useEffect, useCallback } from "react";
import { X } from "lucide-react";

interface MobileImageZoomModalProps {
  src: string;
  alt: string;
  onClose: () => void;
}

/**
 * Mobile-only image zoom modal con pinch-zoom nativo browser
 * (touch-action: pinch-zoom + max-w/max-h object-contain).
 * Tap fuori dall'immagine, tasto X o tasto ESC chiudono.
 */
export function MobileImageZoomModal({
  src, alt, onClose,
}: MobileImageZoomModalProps): ReactNode {
  // Body scroll lock + ESC handler
  useEffect(() => {
    const original = document.body.style.overflow;
    document.body.style.overflow = "hidden";
    const onKey = (e: KeyboardEvent) => {
      if (e.key === "Escape") onClose();
    };
    document.addEventListener("keydown", onKey);
    return () => {
      document.body.style.overflow = original;
      document.removeEventListener("keydown", onKey);
    };
  }, [onClose]);

  const handleBackdropClick = useCallback(
    (e: React.MouseEvent<HTMLDivElement>) => {
      // Click solo sul backdrop chiude (non sull'immagine stessa)
      if (e.target === e.currentTarget) onClose();
    },
    [onClose],
  );

  return (
    <div
      className="fixed inset-0 z-[var(--z-modal)] flex items-center justify-center bg-black/95 p-4"
      role="dialog"
      aria-modal="true"
      aria-label="Immagine ingrandita"
      onClick={handleBackdropClick}
    >
      <button
        type="button"
        onClick={onClose}
        aria-label="Chiudi zoom"
        className="absolute right-4 top-4 flex h-10 w-10 items-center justify-center rounded-full bg-white/10 text-white backdrop-blur-sm hover:bg-white/20"
      >
        <X className="h-5 w-5" />
      </button>
      <img
        src={src}
        alt={alt}
        className="max-h-full max-w-full object-contain"
        style={{ touchAction: "pinch-zoom" }}
        draggable={false}
      />
    </div>
  );
}
```

Aggiornare `src/components/product/index.ts` aggiungendo l'export:

```typescript
export { MobileImageZoomModal } from "./MobileImageZoomModal";
```

Per Rule 1 zero-any: handler tipizzato `React.MouseEvent<HTMLDivElement>`, `KeyboardEvent` standard. Note: `touch-action: pinch-zoom` è una proprietà CSS valida (CSS Touch Events spec); React `style` prop accetta `touchAction` come camelCase typed string in CSSProperties.

Commit message: `feat(product): MobileImageZoomModal con pinch-zoom`
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>
- File `src/components/product/MobileImageZoomModal.tsx` ~70-80 LOC, completo
- Barrel `index.ts` esporta `MobileImageZoomModal`
- pnpm typecheck passa
- Apertura modal: body scroll bloccato, backdrop nero opaco, immagine centrata, X in alto a destra
- Tasto X / ESC / click backdrop chiudono modal e ripristinano body scroll
- Pinch-zoom nativo (verifica reale possibile solo su touch device, manuale)
- Commit `feat(product): MobileImageZoomModal con pinch-zoom` creato
  </done>
</task>

<task type="auto">
  <name>Task 5: Refactor ProductGallery split desktop/mobile</name>
  <files>src/components/product/ProductGallery.tsx</files>
  <action>
Refactorare `ProductGallery.tsx` per renderizzare DUE versioni mutuamente esclusive via classi Tailwind: desktop esistente (con ZoomableImage + MagnifiableThumbnail + ZoomLensPanel) `hidden md:block` E nuovo MobileImageCarousel `block md:hidden`. Implementa MUX-05.

Modifiche puntuali in `src/components/product/ProductGallery.tsx`:

1. Aggiungere import in cima al file (dopo gli altri import):

   ```typescript
   import { MobileImageCarousel } from "./MobileImageCarousel";
   ```

2. Modificare la funzione esportata `ProductGallery` (riga 28-55) sostituendo il return body. Rinominare il body interno in una sub-funzione `DesktopGallery` (estraendo il return originale) E aggiungere il render condizionale:

   ```typescript
   export function ProductGallery({ images, selectedIndex, onSelect, productName }: ProductGalleryProps): ReactNode {
     return (
       <>
         {/* Desktop: ZoomableImage + MagnifiableThumbnail (md+) */}
         <div className="hidden md:block">
           <DesktopGallery
             images={images}
             selectedIndex={selectedIndex}
             onSelect={onSelect}
             productName={productName}
           />
         </div>

         {/* Mobile: scroll-snap carousel + zoom modal (<md) */}
         <MobileImageCarousel
           className="block md:hidden"
           images={images}
           selectedIndex={selectedIndex}
           onSelect={onSelect}
           productName={productName}
         />
       </>
     );
   }

   // Estratto: codice originale del return di ProductGallery
   function DesktopGallery({ images, selectedIndex, onSelect, productName }: ProductGalleryProps): ReactNode {
     return (
       <div>
         <ZoomableImage
           src={images[selectedIndex]?.url}
           alt={images[selectedIndex]?.alt ?? productName}
           naturalWidth={images[selectedIndex]?.width ?? 800}
           naturalHeight={images[selectedIndex]?.height ?? 800}
           empty={!images.length}
         />
         {images.length > 1 && (
           <div className="mt-4 flex gap-3 overflow-x-auto pb-1">
             {images.map((img, i) => (
               <MagnifiableThumbnail
                 key={img.id}
                 src={img.url}
                 alt={img.alt ?? `${productName} ${i + 1}`}
                 isActive={i === selectedIndex}
                 onClick={() => onSelect(i)}
                 label={img.alt ?? `${productName} ${i + 1}`}
               />
             ))}
           </div>
         )}
       </div>
     );
   }
   ```

3. NON toccare `ZoomableImage`, `ZoomLensPanel`, `MagnifiableThumbnail` — restano come sono. Su mobile non vengono mai renderizzati grazie a `hidden md:block` sul wrapper.

4. NON modificare l'export di tipo `ProductImage` (resta nel file).

Cleanup: nessuno richiesto. `MagnifiableThumbnail` con il long-press magnifier confondente non viene più montato su mobile (decision spec § 1.3 "Long-press magnifier su mobile: Rimosso") perché il wrapper è `hidden md:block`.

Per Rule 1 zero-any: nessun nuovo `any`. Le firme delle funzioni desktop sono identiche all'originale.

Commit message: `refactor(product): split ProductGallery desktop/mobile, cleanup magnifier su mobile`
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>
- ProductGallery.tsx contiene wrapper esterno con `<DesktopGallery className="hidden md:block" />` e `<MobileImageCarousel className="block md:hidden" />`
- Il body originale di ProductGallery è stato estratto in `DesktopGallery` con stessa logica (no comportamento cambiato su desktop)
- pnpm typecheck passa
- Smoke DevTools viewport iPhone 12: appare il carousel scroll-snap (no thumbnails impilate, no MagnifiableThumbnail popup)
- Smoke DevTools viewport iPad (768x1024): appare layout desktop esistente (ZoomableImage + thumbnails sotto)
- Tap zoom icon mobile apre il MobileImageZoomModal
- Swipe orizzontale mobile cambia immagine + dots si aggiornano + counter "X / N" si aggiorna
- Commit `refactor(product): split ProductGallery desktop/mobile, cleanup magnifier su mobile` creato
  </done>
</task>

<task type="auto">
  <name>Task 6: MobileStickyAddToCart sticky bottom bar</name>
  <files>src/components/product/MobileStickyAddToCart.tsx, src/components/product/index.ts</files>
  <action>
Creare nuovo componente `MobileStickyAddToCart.tsx` con sticky bottom bar mobile-only contenente prezzo + quantity stepper + CTA dinamico 5 stati. Implementa MUX-06.

File NEW `src/components/product/MobileStickyAddToCart.tsx` (~120 LOC):

```typescript
import type { ReactNode } from "react";
import { Minus, Plus, ShoppingBag, Loader2, Check } from "lucide-react";
import { cn } from "~/lib/utils/cn";

interface MobileStickyAddToCartProps {
  price: number;
  quantity: number;
  onSetQuantity: (q: number) => void;
  cartStatus: "idle" | "loading" | "success";
  canAddToCart: boolean;
  onAddToCart: () => void;
  effectiveStock: number;
  allOptionsSelected: boolean;
}

/**
 * Sticky bottom bar mobile-only (md:hidden) per pagina prodotto.
 * Layout: prezzo + quantity stepper + CTA dinamico (5 stati).
 * z-index: var(--z-sticky) — sotto cookie banner (--z-toast) e modal (--z-modal),
 * sopra contenuto pagina.
 */
export function MobileStickyAddToCart({
  price, quantity, onSetQuantity, cartStatus, canAddToCart,
  onAddToCart, effectiveStock, allOptionsSelected,
}: MobileStickyAddToCartProps): ReactNode {
  const isOutOfStock = effectiveStock === 0;
  const isLoading = cartStatus === "loading";
  const isSuccess = cartStatus === "success";
  const isDisabled = !canAddToCart || isLoading || isOutOfStock;

  // Determina label + icon CTA secondo i 5 stati documentati in spec § 3.2
  let ctaLabel: string;
  let CtaIcon: typeof ShoppingBag | typeof Loader2 | typeof Check | null;
  let ctaBg: string;
  if (isOutOfStock) {
    ctaLabel = "Esaurito";
    CtaIcon = null;
    ctaBg = "bg-[var(--color-text-muted)]";
  } else if (!allOptionsSelected) {
    ctaLabel = "Seleziona opzioni";
    CtaIcon = null;
    ctaBg = "bg-[var(--color-text-muted)]";
  } else if (isLoading) {
    ctaLabel = "Aggiungo...";
    CtaIcon = Loader2;
    ctaBg = "bg-[var(--color-primary)]";
  } else if (isSuccess) {
    ctaLabel = "Aggiunto!";
    CtaIcon = Check;
    ctaBg = "bg-green-600";
  } else {
    ctaLabel = "Aggiungi al carrello";
    CtaIcon = ShoppingBag;
    ctaBg = "bg-[var(--color-primary)]";
  }

  return (
    <div
      className="md:hidden fixed bottom-0 left-0 right-0 z-[var(--z-sticky)] border-t border-[var(--color-border)] bg-white/95 px-4 py-3 backdrop-blur"
      style={{ boxShadow: "0 -4px 12px rgba(0,0,0,0.06)" }}
      role="region"
      aria-label="Aggiungi al carrello"
    >
      <div className="flex items-center gap-3">
        {/* Prezzo */}
        <div className="flex-shrink-0">
          <p className="text-base font-semibold text-[var(--color-primary)]">
            EUR {price.toFixed(2)}
          </p>
        </div>

        {/* Quantity stepper */}
        <div className="flex flex-shrink-0 items-center rounded-md border border-[var(--color-border)]">
          <button
            type="button"
            onClick={() => onSetQuantity(Math.max(1, quantity - 1))}
            disabled={quantity <= 1 || isOutOfStock}
            aria-label="Diminuisci quantità"
            className="flex h-10 w-10 items-center justify-center text-[var(--color-text-muted)] disabled:opacity-40"
          >
            <Minus className="h-3 w-3" />
          </button>
          <span className="flex h-10 w-8 items-center justify-center text-sm font-semibold">
            {quantity}
          </span>
          <button
            type="button"
            onClick={() => onSetQuantity(Math.min(10, quantity + 1))}
            disabled={quantity >= 10 || isOutOfStock}
            aria-label="Aumenta quantità"
            className="flex h-10 w-10 items-center justify-center text-[var(--color-text-muted)] disabled:opacity-40"
          >
            <Plus className="h-3 w-3" />
          </button>
        </div>

        {/* CTA dinamico */}
        <button
          type="button"
          onClick={onAddToCart}
          disabled={isDisabled}
          className={cn(
            "flex flex-1 h-11 items-center justify-center gap-1.5 rounded-md text-sm font-medium text-white transition-colors disabled:cursor-not-allowed disabled:opacity-60",
            ctaBg,
          )}
        >
          {CtaIcon && (
            <CtaIcon className={cn("h-4 w-4", isLoading && "animate-spin")} />
          )}
          <span>{ctaLabel}</span>
        </button>
      </div>
    </div>
  );
}
```

Aggiornare `src/components/product/index.ts`:

```typescript
export { MobileStickyAddToCart } from "./MobileStickyAddToCart";
```

NOTA stati CTA (spec § 3.2):
| Condizione | Label | Icon | Bg |
|-----------|-------|------|----|
| `effectiveStock === 0` | Esaurito | — | text-muted (disabled) |
| `!allOptionsSelected` | Seleziona opzioni | — | text-muted (disabled) |
| `cartStatus === "loading"` | Aggiungo... | Loader2 spin | primary |
| `cartStatus === "success"` | Aggiunto! | Check | green-600 |
| Default | Aggiungi al carrello | ShoppingBag | primary |

Per Rule 1 zero-any: prop interface fully typed, `CtaIcon: typeof ShoppingBag | typeof Loader2 | typeof Check | null` (discriminated by component reference).

Commit message: `feat(product): MobileStickyAddToCart sticky bottom bar`
  </action>
  <verify>
    <automated>pnpm typecheck</automated>
  </verify>
  <done>
- File `src/components/product/MobileStickyAddToCart.tsx` ~110-130 LOC
- Barrel `index.ts` esporta `MobileStickyAddToCart`
- pnpm typecheck passa
- Componente non ancora montato — sarà mountato in Task 7
- Commit `feat(product): MobileStickyAddToCart sticky bottom bar` creato
  </done>
</task>

<task type="auto">
  <name>Task 7: Mount sticky bar + nascondi CTA inline + body padding compensativo</name>
  <files>src/components/product/VariantSelector.tsx, src/routes/prodotti.$slug.tsx</files>
  <action>
Wirare la sticky bar nella pagina prodotto, nascondere il CTA inline su mobile dentro VariantSelector, aggiungere padding compensativo al wrapper esterno della pagina. Implementa MUX-07.

### 7a) Modificare `src/components/product/VariantSelector.tsx`

Riga 142 — wrapper del blocco quantità + CTA inline. Cambiare:

DA:
```jsx
<div className="mt-8 flex flex-col gap-4 sm:flex-row sm:items-center">
```

A:
```jsx
<div className="mt-8 hidden md:flex flex-col gap-4 sm:flex-row sm:items-center">
```

Su mobile (`<md`) il blocco quantità + CTA non viene renderizzato (il sticky bar lo sostituisce). Su desktop il layout è invariato.

Nessun'altra modifica al file.

### 7b) Modificare `src/routes/prodotti.$slug.tsx`

1. Aggiungere import in cima al file (dopo gli import esistenti dei product components):

   ```typescript
   import { MobileStickyAddToCart } from "~/components/product/MobileStickyAddToCart";
   ```

2. Wrappare il return della funzione `ProdottoPage` con padding-bottom compensativo. Cambiare il fragment esterno `<>...</>` (riga 192-282) in un `<div>` wrapper:

   DA:
   ```jsx
   return (
     <>
       <div className="bg-[var(--color-surface)] py-4">
         ...
       </div>
       <section className="bg-[var(--color-background)] py-[var(--section-padding-y)]">
         ...
       </section>
       <RelatedProducts products={relatedProducts} currentProductId={product.id} />
     </>
   );
   ```

   A:
   ```jsx
   return (
     <div className="pb-24 md:pb-0">
       <div className="bg-[var(--color-surface)] py-4">
         ...
       </div>
       <section className="bg-[var(--color-background)] py-[var(--section-padding-y)]">
         ...
       </section>
       <RelatedProducts products={relatedProducts} currentProductId={product.id} />

       {/* Mobile sticky add-to-cart bar (md:hidden interno) */}
       <MobileStickyAddToCart
         price={priceBreakdown.total}
         quantity={quantity}
         onSetQuantity={(q) => setQuantity(q)}
         cartStatus={cartStatus}
         canAddToCart={!!canAddToCart}
         onAddToCart={handleAddToCart}
         effectiveStock={effectiveStock}
         allOptionsSelected={allVisibleGroupsSelected}
       />
     </div>
   );
   ```

3. Verificare che le variabili passate a `MobileStickyAddToCart` esistano nello scope (sono già definite riga 130-179):
   - `priceBreakdown` ✓ (riga 141-149)
   - `quantity` ✓ (riga 91)
   - `setQuantity` ✓ (riga 91)
   - `cartStatus` ✓ (riga 92)
   - `canAddToCart` ✓ (riga 153)
   - `handleAddToCart` ✓ (riga 179)
   - `effectiveStock` ✓ (riga 151)
   - `allVisibleGroupsSelected` ✓ (riga 130)

   Tutto nello scope → no props drilling addizionale. Riutilizzo identico a quanto già passato a `<VariantSelector />`.

NOTA padding 96px: `pb-24` Tailwind = 24*4 = 96px (76px sticky bar height + 20px buffer). Su `md:pb-0` desktop torna a zero.

Per Rule 1 zero-any: nessun `any`, tipi già esistenti riutilizzati.

Commit message: `feat(product): mount sticky bar mobile + nascondi CTA inline + body padding compensativo`

NOTA commit: la spec § "Atomic commits previsti" elenca 7 commit ma divide il "mount sticky" e "body padding compensativo" come commit 7. Implementati insieme qui (singolo commit) perché tecnicamente inseparabili: senza padding il bottone copre il footer; senza mount lo sticky bar non esiste. Documentare in commit body se serve.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>
- VariantSelector.tsx riga 142: wrapper ha `hidden md:flex` (verificabile via grep)
- prodotti.$slug.tsx: import `MobileStickyAddToCart` aggiunto
- prodotti.$slug.tsx: return wrapped in `<div className="pb-24 md:pb-0">`
- prodotti.$slug.tsx: `<MobileStickyAddToCart .../>` mountato dopo `<RelatedProducts />`
- pnpm typecheck passa (baseline 25 → 25 preservata, no nuovi errori)
- pnpm lint passa (Biome conforme)
- Smoke DevTools viewport iPhone 12 su una pagina prodotto reale (es. /prodotti/sandalo-classico):
  - Sticky bar visibile sempre in basso (z-sticky)
  - NESSUN CTA inline visibile dentro VariantSelector
  - Footer non coperto: scroll fino in fondo, RelatedProducts visibili sopra alla sticky bar grazie al pb-24
  - Click "Aggiungi al carrello" → loading spinner → success "Aggiunto!" verde 3s → torna a "Aggiungi al carrello"
  - Stato "Esaurito" se variant out of stock
  - Stato "Seleziona opzioni" se non tutte le opzioni richieste sono selezionate
- Smoke DevTools viewport iPad (768+): sticky bar NON visibile (md:hidden), CTA inline visibile come prima dentro VariantSelector
- Z-index check: cookie banner (se ancora presente) appare SOPRA sticky bar (z-toast 80 > z-sticky 40) ✓
- Commit `feat(product): mount sticky bar mobile + nascondi CTA inline + body padding compensativo` creato
  </done>
</task>

</tasks>

<verification>
Dopo l'esecuzione di tutti i 7 task:

**Smoke browser DevTools — viewport iPhone 12 (390x844):**
- Banner cookie altezza ≤220px (misurabile con DevTools layout panel)
- Bottoni "Rifiuta" + "Accetta" side-by-side full-width, link "Personalizza preferenze" sotto centrato
- Click "Personalizza preferenze" → modal full-screen apre con backdrop nero, body NON scrolla
- ESC chiude modal; click backdrop chiude modal; click X chiude modal

**Smoke browser DevTools — viewport iPad (768x1024):**
- Layout desktop si attiva: cookie banner con h3 + paragrafo lungo + 3 bottoni in fila
- Pannello preferenze cookie inline (NO modal)
- Galleria con ZoomableImage + thumbnails sotto (no carousel)
- CTA inline visibile dentro VariantSelector (NO sticky bar)

**Smoke gallery mobile (viewport iPhone su una pagina prodotto reale):**
- Carousel edge-to-edge (no padding sui lati)
- Swipe left/right cambia immagine smooth con scroll-snap
- Dots indicator si aggiorna (pallino attivo allungato + colore primary)
- Counter "X / N" corretto
- Tap zoom icon → modal nera apre full-screen
- Pinch-zoom nativo sull'immagine modal funziona (verifica device reale)
- Tap close X / ESC / tap fuori chiudono modal

**Smoke add-to-cart mobile:**
- Sticky bar sempre visibile in basso (z-sticky 40)
- Nessun CTA inline visibile su mobile (`hidden md:flex` verificabile su VariantSelector)
- Click bottone: loading → success → idle funzionano (5 stati)
- Footer pagina + RelatedProducts non coperti (padding-bottom 96px verificabile)

**Z-index conflict check:**
- Cookie banner z-toast (80) sopra MobileStickyAddToCart z-sticky (40) ✓
- MobileImageZoomModal z-modal (70) sopra sticky bar z-sticky (40) ✓
- Cookie preferences modal z-modal (70) sopra sticky bar ✓
- Mega menu mobile drawer z-overlay (60) sotto modal e cookie banner ✓

**TypeScript baseline:**
- `pnpm typecheck`: errori totali = 25 (baseline preservata, no nuovi)

**Build:**
- `pnpm build`: passa senza errori

**Commit history finale:**
- 7 commit atomici sequenziali con prefix italiano `feat(banner):`, `feat(product):`, `refactor(product):`
- Ordine: 1.banner-compact, 2.banner-modal, 3.carousel, 4.zoom-modal, 5.gallery-split, 6.sticky-bar, 7.mount+padding
</verification>

<success_criteria>
- 7 commit atomici creati su branch corrente
- 3 file nuovi: `MobileImageCarousel.tsx`, `MobileImageZoomModal.tsx`, `MobileStickyAddToCart.tsx`
- 5 file modificati: `CookieBanner.tsx`, `ProductGallery.tsx`, `VariantSelector.tsx`, `prodotti.$slug.tsx`, `product/index.ts`
- pnpm typecheck baseline 25 errori preservata (no nuovi)
- pnpm lint Biome conforme
- Cookie banner mobile ≤220px altezza (smoke DevTools)
- Galleria mobile è carousel swipe + dots + tap-to-zoom (no thumbnails impilate)
- Sticky add-to-cart mobile sempre visibile, CTA inline nascosto su mobile, footer non coperto
- Desktop ≥768px completamente invariato in tutti e 3 i sotto-feature
- Tutti i must_haves truths sopra verificati a vista
</success_criteria>

<output>
After completion, create `.planning/quick/260501-h9l-mobile-ux-cookie-banner-compact-productg/260501-h9l-SUMMARY.md` con:
- Riassunto delle 7 modifiche (1 sezione per task)
- Lista commit hash con messaggi
- File creati/modificati con LOC
- Risultati smoke test (mobile + desktop)
- Eventuali deviazioni dalla spec con motivazione
- Z-index hierarchy verificata
- TypeScript baseline check
</output>
