---
phase: quick-260601-q8r
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [scripts/remove-bruciato-option.ts]
autonomous: true
requirements: [Q8R-01]
must_haves:
  truths:
    - "Lo script esiste in scripts/remove-bruciato-option.ts ed è eseguibile con npx tsx"
    - "In dry-run (default) NON scrive mai sul DB; con --execute applica gli update"
    - "Rimuove SOLO l'opzione con label esattamente 'bruciato' (case-insensitive), MAI 'marrone bruciato'"
    - "Processa sia Product.variantConfig (nullable) sia VariantTemplate.config (non nullable)"
    - "È idempotente: una seconda esecuzione --execute rimuove 0 opzioni"
    - "npx tsc --noEmit non produce NUOVI errori sul file (zero any, zero as any)"
  artifacts:
    - path: "scripts/remove-bruciato-option.ts"
      provides: "Script di migrazione CLI dry-run/execute per rimuovere l'opzione variante Bruciato"
      min_lines: 120
      exports: []
  key_links:
    - from: "scripts/remove-bruciato-option.ts"
      to: "prisma.product.findMany / prisma.product.update"
      via: "where variantConfig not null + update data variantConfig"
      pattern: "prisma\\.product\\.(findMany|update)"
    - from: "scripts/remove-bruciato-option.ts"
      to: "prisma.variantTemplate.findMany / prisma.variantTemplate.update"
      via: "findMany su tutti + update data config"
      pattern: "prisma\\.variantTemplate\\.(findMany|update)"
---

<objective>
Creare lo script di migrazione dati `scripts/remove-bruciato-option.ts` che rimuove l'opzione di variante colore "Bruciato" da TUTTI i prodotti (`Product.variantConfig` JSON) e da tutti i variant template (`VariantTemplate.config` JSON) nel database.

Purpose: l'opzione colore "Bruciato" va eliminata dal catalogo. Serve uno script ripetibile, sicuro (dry-run di default), eseguibile dall'utente sia in locale sia su Railway, che pulisca il JSON delle varianti senza toccare gli ordini/carrelli storici.

Output: un singolo file TypeScript `scripts/remove-bruciato-option.ts`, SOLO scritto + typecheck. Lo script NON va eseguito dall'executor contro alcun DB — l'orchestratore/utente lo lancia manualmente dopo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

# File da riusare come pattern (NON modificare nessuno di questi)
@scripts/populate-and-cleanup-prod-images.ts
@scripts/fix-elisa-no-variants.ts
@src/lib/types/variant-config.ts

<interfaces>
<!-- Forma canonica dei tipi variante (da src/lib/types/variant-config.ts). -->
<!-- NB: gli script in scripts/ NON importano da src/ — definiscono i tipi localmente -->
<!-- (vedi scripts/create-product-templates.ts:12). tsx NON risolve i path alias "~/" -->
<!-- del tsconfig di default. Quindi RIDEFINISCI i tipi minimi nello script, NON importarli. -->

VariantConfig (root JSON):
```typescript
interface VariantOption {
  value: string;
  label: string;
  color?: string;
  priceModifier?: number;
  imageUrl?: string;
}
interface VariantGroup {
  id: string;
  label: string;
  type: "button" | "select" | "color-swatch";
  required: boolean;
  options: VariantOption[];
  dependsOn?: { groupId: string; optionValue: string };
}
interface VariantConfig {
  groups: VariantGroup[];
}
```

Schema Prisma (confermato in prisma/schema.prisma):
- `Product.variantConfig  Json?`   → NULLABLE (riga 256)
- `VariantTemplate.config Json`    → NON nullable (riga 536)
- Prisma ritorna i campi Json come `Prisma.JsonValue` (importabile da `@prisma/client`).

Pattern -prod confermati (da populate-and-cleanup-prod-images.ts):
- `import { PrismaClient } from "@prisma/client"; const prisma = new PrismaClient();`
- guard `if (!process.env.DATABASE_URL) { console.error(...); process.exit(1); }`
- `const isExecute = new Set(process.argv.slice(2)).has("--execute");`
- `try { ... } finally { await prisma.$disconnect(); }`
- `main().catch((err) => { console.error(err); process.exit(1); });`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scrivere scripts/remove-bruciato-option.ts (dry-run/execute, type-safe)</name>
  <files>scripts/remove-bruciato-option.ts</files>
  <action>
Crea il file `scripts/remove-bruciato-option.ts`. Commenti in italiano. Zero `any` / zero `as any`. NON eseguire lo script contro nessun DB — solo scriverlo e fare typecheck.

**1. Header doc-comment** con descrizione + esempi d'uso:
```
/**
 * remove-bruciato-option.ts
 *
 * Rimuove l'opzione di variante colore "Bruciato" da:
 *   - Product.variantConfig  (Json?, nullable)
 *   - VariantTemplate.config (Json, non nullable)
 * Match ESATTO su label === "bruciato" (case-insensitive): NON tocca "marrone bruciato".
 * Dry-run di default (nessuna scrittura). Flag --execute per applicare.
 * Idempotente: una seconda esecuzione rimuove 0 opzioni.
 *
 * NON tocca OrderItem/CartItem.selectedOptions (dati storici).
 *
 * Uso LOCALE (legge DATABASE_URL dal .env):
 *   npx tsx scripts/remove-bruciato-option.ts            # dry-run
 *   npx tsx scripts/remove-bruciato-option.ts --execute  # applica
 *
 * Uso RAILWAY:
 *   railway run --service Postgres -- bash -c 'DATABASE_URL=$DATABASE_PUBLIC_URL npx tsx scripts/remove-bruciato-option.ts'
 *   railway run --service Postgres -- bash -c 'DATABASE_URL=$DATABASE_PUBLIC_URL npx tsx scripts/remove-bruciato-option.ts --execute'
 */
```

**2. Import + tipi locali.** Importa SOLO `import { PrismaClient } from "@prisma/client";` (NON importare i tipi da `src/` né da `~/` — tsx non risolve l'alias; gli altri script ridefiniscono i tipi localmente). Definisci localmente le interface `VariantOption`, `VariantGroup`, `VariantConfig` esattamente come nel blocco `<interfaces>` sopra (groups → options con value/label/color?/priceModifier?/imageUrl?, group con id/label/type/required/options/dependsOn?). `const prisma = new PrismaClient();`

**3. Costante target:** `const TARGET_LABEL = "bruciato";`

**4. Type guard difensivo `isVariantConfig(value: unknown): value is VariantConfig`.** Verifica runtime, ZERO cast ciechi:
   - `typeof value === "object" && value !== null`
   - la proprietà `groups` esiste ed è un array: usa un cast intermedio sicuro `const obj = value as { groups?: unknown };` poi `Array.isArray(obj.groups)`
   - per ogni elemento di groups verifica che sia object non-null con `Array.isArray(group.options)` (controllo difensivo minimo; se vuoi, accetta groups con options array di object). Ritorna `false` su qualsiasi forma inattesa.
   Non serve validare ogni singolo campo option: basta garantire `groups: array` e `options: array` per poter iterare in sicurezza, leggendo `option.label` come `unknown` e facendo `typeof label === "string"` prima del confronto.

**5. Funzione PURA `stripBruciato(config: VariantConfig)`** che ritorna:
```
{ config: VariantConfig; removed: number; affectedGroups: string[]; emptiedGroups: string[] }
```
   - NON muta l'input: costruisci un nuovo `VariantConfig`.
   - Per ogni group: filtra le `options` tenendo quelle per cui NON vale `typeof option.label === "string" && option.label.trim().toLowerCase() === TARGET_LABEL`. (Solo "bruciato" esatto: "marrone bruciato".trim().toLowerCase() !== "bruciato" → resta.)
   - `removed` = somma delle opzioni rimosse su tutti i gruppi.
   - `affectedGroups` = label (o id) dei gruppi da cui è stata rimossa ≥1 opzione.
   - `emptiedGroups` = label (o id) dei gruppi rimasti con `options.length === 0` DOPO il filtro. **NON eliminare il group** (lascialo con options vuoto) — solo segnalalo come warning.
   - Ritorna il nuovo config con tutti i gruppi (anche quelli svuotati) e le options filtrate.

**6. Helper `maskDbHost(url: string): string`** che estrae SOLO l'hostname dall'URL (es. `new URL(url).hostname`) per stamparlo senza mai esporre user/password. Avvolgi in try/catch (`catch (e: unknown)`) → ritorna `"(host non parsabile)"` su URL malformato.

**7. `async function main()`:**
   - `const isExecute = new Set(process.argv.slice(2)).has("--execute");`
   - `const dbUrl = process.env.DATABASE_URL;` → se assente: `console.error("❌ DATABASE_URL non settato.")` + `process.exit(1)`.
   - Stampa header: modalità (`"DRY-RUN (nessuna scrittura)"` se !isExecute, altrimenti `"EXECUTE (scritture attive)"`) e `console.log(\`DB host: ${maskDbHost(dbUrl)}\`)`.
   - `const prisma = new PrismaClient();` dentro `try { ... } finally { await prisma.$disconnect(); }`.
   - **PRODOTTI:** `const products = await prisma.product.findMany({ where: { variantConfig: { not: null } }, select: { id: true, slug: true, variantConfig: true } });`
     - Per ognuno: `if (!isVariantConfig(product.variantConfig)) { console.warn(...skip slug...); continue; }`
     - `const result = stripBruciato(product.variantConfig);`
     - se `result.removed > 0`: logga dettaglio (slug, affectedGroups, `removed`, eventuali emptiedGroups come `⚠️`); incrementa contatori; se `isExecute` → `await prisma.product.update({ where: { id: product.id }, data: { variantConfig: JSON.parse(JSON.stringify(result.config)) } });` (il `JSON.parse(JSON.stringify(...))` produce un valore compatibile con `Prisma.InputJsonValue`, stesso pattern di fix-elisa-no-variants.ts:51).
   - **TEMPLATE:** `const templates = await prisma.variantTemplate.findMany({ select: { id: true, slug: true, config: true } });` (config NON nullable → nessun filtro `not: null`).
     - Stessa logica: type guard `isVariantConfig(template.config)` → skip+warn su forma inattesa; `stripBruciato`; se removed>0 logga + (se execute) `await prisma.variantTemplate.update({ where: { id: template.id }, data: { config: JSON.parse(JSON.stringify(result.config)) } });`
   - **Riepilogo finale:** N prodotti interessati, N template interessati, N opzioni totali rimosse, e — se non vuota — la lista aggregata di emptiedGroups (warning). Se `!isExecute`: chiudi con `console.log("ℹ️  Rilancia con --execute per applicare.")`.

**8. `main().catch((err) => { console.error("❌ Errore:", err); process.exit(1); });`** in coda.

**Vincoli da rispettare nel codice:**
   - Zero `any` / zero `as any`: usa `unknown` + type guard + cast intermedi tipizzati (`as { groups?: unknown }`). `catch (e: unknown)`.
   - In dry-run NON deve MAI chiamare `prisma.*.update` (gating su `isExecute`).
   - Idempotente: dopo un run --execute, la label "bruciato" non esiste più → secondo run `removed === 0`.
   - NON toccare OrderItem/CartItem.selectedOptions, NON modificare schema.prisma, NON modificare scripts/apply-variant-configs.ts, NON modificare package.json.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | grep -E "scripts/remove-bruciato-option\.ts" ; echo "EXIT_NO_MATCH=$?"</automated>
  </verify>
  <done>
Il file `scripts/remove-bruciato-option.ts` esiste (≥120 LOC). `npx tsc --noEmit` non riporta ALCUN errore sulla riga del nuovo file (il grep sopra deve dare 0 match → `EXIT_NO_MATCH=1`). Gli errori d'ambiente pre-esistenti del worktree fresco (routeTree.gen.ts non generato, @prisma/client non generato, @playwright/test mancante) sono accettabili e NON riguardano questo file. `grep -nE ": any|as any" scripts/remove-bruciato-option.ts` → 0 match. Sono presenti: import `PrismaClient`, tipi locali `VariantConfig`/`VariantGroup`/`VariantOption`, type guard `isVariantConfig`, funzione pura `stripBruciato`, gating `--execute`, guard `DATABASE_URL`, `maskDbHost`, loop su `prisma.product.findMany`/`prisma.variantTemplate.findMany`, `prisma.$disconnect()` in `finally`. Lo script NON è stato eseguito contro alcun DB.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` → zero NUOVI errori imputabili a `scripts/remove-bruciato-option.ts` (gli errori d'ambiente noti su routeTree.gen.ts/@prisma/@playwright in worktree fresco sono pre-esistenti e accettabili).
- `grep -nE ": any|as any" scripts/remove-bruciato-option.ts` → 0 match (zero-any policy).
- `grep -c "prisma.product.update\|prisma.variantTemplate.update" scripts/remove-bruciato-option.ts` → ≥2 (entrambe le mutazioni presenti, gated su --execute).
- Lo script NON è stato eseguito (nessun comando `tsx scripts/remove-bruciato-option.ts` lanciato dall'executor).
</verification>

<success_criteria>
- Esiste `scripts/remove-bruciato-option.ts`, type-safe, zero any, commenti italiani.
- Dry-run di default (nessuna scrittura), `--execute` applica gli update a Product.variantConfig e VariantTemplate.config.
- Match esatto label "bruciato" case-insensitive, esclude "marrone bruciato".
- Type guard difensivo prima di ogni cast a VariantConfig; gruppi svuotati segnalati ma non eliminati.
- Idempotente; `$disconnect()` in finally; guard su DATABASE_URL; host DB mascherato in stampa.
- `npx tsc --noEmit` senza nuovi errori sul file.
</success_criteria>

<output>
After completion, create `.planning/quick/260601-q8r-script-migrazione-rimozione-opzione-vari/260601-q8r-SUMMARY.md`
</output>
