---
quick_id: 260430-ov8
mode: quick
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/routes/account.tsx
  - src/routes/account/privacy.tsx
  - src/routes/api/user/export.ts
  - src/routes/api/user/delete.ts
  - src/routes/api/user/consents.ts
  - src/components/auth/RegisterForm.tsx
  - src/routes/api/newsletter.ts
  - src/routes/api/newsletter/confirm.ts
  - src/routes/api/newsletter/unsubscribe.ts
  - src/routes/newsletter.conferma.tsx
  - src/routes/newsletter.disiscriviti.tsx
  - src/routes/admin.consensi.tsx
  - src/lib/admin/admin-consents.server.ts
  - src/lib/validators/admin.ts
autonomous: true
requirements:
  - GDPR-W3-PRIVACY-PAGE
  - GDPR-W3-EXPORT
  - GDPR-W3-DELETE
  - GDPR-W3-SIGNUP-CONSENT
  - GDPR-W3-NEWSLETTER-DOI
  - GDPR-W3-ADMIN-CONSENT-LOG

must_haves:
  truths:
    - "Utente loggato vede /account/privacy con sezioni dati/cancella/storico consensi"
    - "Utente loggato scarica il proprio dump JSON (profilo+ordini+items+wishlist+addresses+consentLogs+auditLogs) con Content-Disposition attachment"
    - "Utente loggato può anonimizzare il proprio account: email/name/image rimpiazzati, account/session/address/cart/wishlist/review cancellati, ORDINI E PAYMENTS PRESERVATI con userId valorizzato (legge fiscale)"
    - "Signup tramite Better Auth + checkbox marketing inserisce 1 row ConsentLog type=privacy granted=true e (se opt-in) 1 row ConsentLog type=marketing granted=true con userId del nuovo utente"
    - "POST /api/newsletter con email valida crea NewsletterSubscription pending + invia email Resend con link conferma; NON inserisce ConsentLog finché non c'è conferma"
    - "Click link /newsletter/conferma?token=XXX → NewsletterSubscription confirmed + ConsentLog type=marketing granted=true (audit-by-presence)"
    - "Click link /newsletter/disiscriviti?token=XXX → NewsletterSubscription unsubscribed + ConsentLog type=marketing granted=false"
    - "Admin /admin/consensi mostra tabella read-only ultime 100 ConsentLog ordinate per createdAt desc, paginazione 12/pagina, filtri type/date range"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "model NewsletterSubscription"
      contains: "model NewsletterSubscription"
    - path: "src/routes/account/privacy.tsx"
      provides: "GDPR rights UI page for logged-in users"
      min_lines: 120
    - path: "src/routes/api/user/export.ts"
      provides: "JSON dump endpoint with rate limit 3/day per userId"
      min_lines: 60
    - path: "src/routes/api/user/delete.ts"
      provides: "Anonymization transaction (NOT delete user, preserve Order)"
      min_lines: 80
    - path: "src/routes/api/user/consents.ts"
      provides: "Post-signup ConsentLog logging endpoint"
      min_lines: 40
    - path: "src/routes/api/newsletter/confirm.ts"
      provides: "Token-based double opt-in confirm endpoint"
      min_lines: 40
    - path: "src/routes/api/newsletter/unsubscribe.ts"
      provides: "Token-based unsubscribe endpoint"
      min_lines: 35
    - path: "src/routes/newsletter.conferma.tsx"
      provides: "Confirm landing page with loader calling confirm endpoint"
      min_lines: 30
    - path: "src/routes/newsletter.disiscriviti.tsx"
      provides: "Unsubscribe landing page with loader"
      min_lines: 30
    - path: "src/routes/admin.consensi.tsx"
      provides: "Admin consent log read-only panel"
      min_lines: 100
    - path: "src/lib/admin/admin-consents.server.ts"
      provides: "Server-side consent log query with filters/pagination"
      min_lines: 50
  key_links:
    - from: "src/routes/account/privacy.tsx"
      to: "/api/user/export"
      via: "fetch POST con credentials"
      pattern: "fetch.*api/user/export"
    - from: "src/routes/account/privacy.tsx"
      to: "/api/user/delete"
      via: "fetch POST con TypeConfirmDialog"
      pattern: "fetch.*api/user/delete"
    - from: "src/routes/api/user/delete.ts"
      to: "prisma.$transaction"
      via: "anonymize user.update + cleanup deleteMany"
      pattern: "prisma\\.\\$transaction"
    - from: "src/components/auth/RegisterForm.tsx"
      to: "/api/user/consents"
      via: "fetch POST dopo authClient.signUp.email() success"
      pattern: "fetch.*api/user/consents"
    - from: "src/routes/api/newsletter.ts"
      to: "sendEmail (Resend)"
      via: "import + chiamata con HTML inline confirm link"
      pattern: "sendEmail\\(\\{"
    - from: "src/routes/admin.consensi.tsx"
      to: "src/lib/admin/admin-consents.server.ts"
      via: "createServerFn import"
      pattern: "listConsentLogs"
---

<objective>
Wave 3 GDPR — chiudere il cerchio compliance: pagina /account/privacy con export+delete dati, double opt-in newsletter, audit ConsentLog signup, pannello admin /consensi.

Purpose: Wave 1 (cookie banner vivo) e Wave 2 (GA4 con consent) sono in produzione. Questa wave implementa Art. 15/17/20 GDPR (accesso/cancellazione/portabilità), il double opt-in newsletter obbligatorio in IT (Codice Privacy + Provv. Garante), l'audit trail consensi al signup, e il pannello admin per consultare il registro consensi. Vincolo legale critico: NON cancellare gli ordini al delete account (DPR 633/72 + art. 2220 c.c. → conservazione fiscale 10 anni) → anonimizzazione del user, ordini intatti con shippingAddress snapshot già immutabile.

Output:
- 1 nuovo modello Prisma: NewsletterSubscription + migration
- 7 nuovi file: src/routes/account/privacy.tsx, src/routes/api/user/{export,delete,consents}.ts, src/routes/api/newsletter/{confirm,unsubscribe}.ts, src/routes/newsletter.{conferma,disiscriviti}.tsx, src/routes/admin.consensi.tsx, src/lib/admin/admin-consents.server.ts
- 4 file modificati: prisma/schema.prisma, src/routes/account.tsx (sidebar link), src/components/auth/RegisterForm.tsx (post-signup consents call), src/routes/api/newsletter.ts (rewrite double opt-in), src/lib/validators/admin.ts (listConsentLogsSchema)
- 8 commit atomici italiani
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@.planning/STATE.md
@docs/superpowers/specs/2026-04-30-gdpr-cookie-compliance-design.md

@prisma/schema.prisma
@src/components/auth/GdprConsentFields.tsx
@src/components/auth/RegisterForm.tsx
@src/routes/account.tsx
@src/routes/account/profilo.tsx
@src/routes/api/newsletter.ts
@src/routes/api/cookie-consent.ts
@src/lib/email.server.ts
@src/lib/auth.ts
@src/lib/auth.server.ts
@src/lib/auth-functions.ts
@src/lib/sdk-auth.server.ts
@src/lib/api-response.ts
@src/lib/rate-limit.server.ts
@src/lib/admin/admin-orders.server.ts
@src/lib/validators/admin.ts
@src/components/admin/TypeConfirmDialog.tsx
@src/routes/admin.tsx
@src/routes/admin.ordini.tsx

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. Use these directly — no exploration needed. -->

From src/lib/sdk-auth.server.ts:
```typescript
interface AuthUser {
  id: string;
  email: string;
  name: string;
  role: string;
  emailVerified: boolean;
}
export async function requireUser(request: Request): Promise<AuthUser>;  // throws redirect if no session
export async function requireAdmin(request: Request): Promise<AuthUser>; // throws redirect if not admin
export async function getUser(request: Request): Promise<AuthUser | null>; // optional auth
```

From src/lib/api-response.ts:
```typescript
export function apiSuccess<T>(data: T, status?: number, headers?: Record<string, string>): Response;
export function apiError(code: string, message: string, status: number, details?: Array<{field: string; message: string}>, headers?: Record<string, string>): Response;
```

From src/lib/rate-limit.server.ts:
```typescript
export function checkRateLimit(identifier: string, preset: "AUTH"|"FORM"|"API"|"WEBHOOK"): { success: boolean; retryAfterMs: number };
export function getClientIp(request: Request): string;
```

From src/lib/email.server.ts:
```typescript
interface SendEmailOptions { to: string; subject: string; html: string; replyTo?: string; }
export async function sendEmail(opts: SendEmailOptions): Promise<{ ok: boolean; id?: string }>;
```

From src/lib/auth.server.ts:
```typescript
export async function auditLog(data: { userId?: string; event: string; ip?: string|null; userAgent?: string|null; metadata?: unknown }): Promise<void>;
```

Better Auth signup flow (CRITICAL for Task 5):
- RegisterForm.tsx chiama `authClient.signUp.email({ name, email, password })` lato CLIENT
- I checkbox `privacyPolicy`, `ageConfirmation`, `marketingConsent` NON arrivano al server in nessun modo
- Strategia: dopo che `signUp.email()` ritorna senza error, il client chiama POST `/api/user/consents` con `{ marketing: boolean }` (privacy è sempre true per definizione, validata client-side)
- L'endpoint usa la sessione Better Auth appena creata (autoSignIn: true in src/lib/auth.ts:45) per ricavare userId

Prisma schema rilevanti (verificati):
- `user` (mappato a tabella `users`): id, name, email @unique, emailVerified, image?, createdAt, updatedAt, role, banned?
- `account`: id, userId, providerId, accountId, password? (passwordHash è qui, NON sul user!), accessToken? — onDelete: Cascade verso user
- `session`: id, userId, token @unique, expiresAt, ipAddress?, userAgent? — onDelete: Cascade verso user
- `Order`: id, userId? (NULLABLE già!), orderNumber @unique, ... — onDelete: Cascade verso user
- `Address`, `Cart`, `Wishlist`, `Review`: tutti con onDelete: Cascade verso user
- `ConsentLog`: id, userId? (nullable), type ("cookie"|"privacy"|"marketing"), granted, ip?, userAgent?, createdAt
- `DataRequest`: id, userId, type ("export"|"delete"|"rectify"), status ("pending"|"processing"|"completed"), createdAt, completedAt?
- `AuditLog`: id, userId?, event, ip?, userAgent?, metadata Json?, createdAt

Order.userId è OPZIONALE nello schema (`userId String?`). Quindi anche dopo anonymize possiamo lasciarlo valorizzato — ottimo, l'utente anonimo resta linkabile per audit interno ma il PII è sparito.

NewsletterSubscription model da aggiungere (Task 1):
```prisma
model NewsletterSubscription {
  id          String    @id @default(cuid())
  email       String    @unique
  status      String    @default("pending")   // "pending" | "confirmed" | "unsubscribed"
  token       String    @unique               // confirm + unsubscribe usano lo stesso token
  confirmedAt DateTime?
  createdAt   DateTime  @default(now())

  @@index([status])
}
```

Validator da aggiungere a src/lib/validators/admin.ts (Task 8):
```typescript
export const listConsentLogsSchema = z.object({
  page: z.coerce.number().int().positive().default(1),
  perPage: z.coerce.number().int().positive().max(50).default(12),
  type: z.enum(["cookie","preferences","privacy","analytics","marketing"]).optional(),
  createdFrom: z.coerce.date().optional(),
  createdTo: z.coerce.date().optional(),
});
export type ListConsentLogsInput = z.infer<typeof listConsentLogsSchema>;
```
</interfaces>
</context>

<critical_constraints>
**SCHEMA SAFETY (NON NEGOZIABILE)**: NON chiamare `prisma.user.delete()`. Order ha onDelete:Cascade su user → cancellerebbe gli ordini violando obblighi fiscali (DPR 633/72, art. 2220 c.c., conservazione 10 anni). USARE SEMPRE `prisma.user.update()` con anonymize fields.

**Better Auth signup**: i checkbox GDPR (privacyPolicy/marketingConsent) sono client-only, non passano dal server. Strategia: client chiama POST /api/user/consents subito dopo `authClient.signUp.email()` success, l'endpoint usa la sessione fresca per ricavare userId.

**passwordHash NON ESISTE su user model** (Better Auth pattern: credenziali sono in `account.password`, vedi schema:86). Quindi NESSUNA modifica schema su user — basta `account.deleteMany({ userId })` per disabilitare login.

**Audit by presence per ConsentLog**: per cookie/privacy/marketing inseriamo row solo per consensi accordati (granted=true). Per unsubscribe newsletter, eccezione: inseriamo granted=false (è una revoca esplicita di consenso pregresso, va tracciata).

**Newsletter idempotency**: se email già `confirmed`, POST /api/newsletter ritorna 200 senza inviare email (no spam, no nuova subscription).

**Rate limit export**: 3/giorno per userId, NON per IP (famiglia condivisa). Usare `prisma.dataRequest.count({ where: { userId, type: "export", createdAt: { gte: 24h ago } } })`.
</critical_constraints>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Schema NewsletterSubscription + migration</name>
  <files>prisma/schema.prisma</files>
  <action>
Aggiungere a `prisma/schema.prisma` il modello `NewsletterSubscription` nella sezione GDPR (dopo `DataRequest`, prima di `StripeEvent`):

```prisma
// ─── NewsletterSubscription (double opt-in) ──────────────────────
// Stato: pending → confirmed (via email link) | unsubscribed (via token link)
// Token unico usato sia per confirm che per unsubscribe (cuid).
// ConsentLog viene loggato SOLO al passaggio pending→confirmed (audit-by-presence).

model NewsletterSubscription {
  id          String    @id @default(cuid())
  email       String    @unique
  status      String    @default("pending")
  token       String    @unique
  confirmedAt DateTime?
  createdAt   DateTime  @default(now())

  @@index([status])
}
```

Poi:
1. Eseguire `pnpm db:migrate -- --name add_newsletter_subscription` (lo script `db:migrate` in package.json:22 esegue `prisma migrate dev`, il flag dopo `--` viene passato a prisma).
2. Verificare che venga creato `prisma/migrations/YYYYMMDD_add_newsletter_subscription/migration.sql` e che il client venga rigenerato.
3. NESSUNA altra modifica allo schema (passwordHash NON esiste su user — è in account.password — verificato in schema:86).

ATTENZIONE: `db:migrate` è collegato a un DB live (DATABASE_URL Railway probabilmente). Se il task fallisce per database remoto inaccessibile o policy migration, fallback: `pnpm db:push` per sync diretto dello schema (acceptable in dev/staging come da pattern repo). Documentare nel commit message quale percorso scelto.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -10 && grep -A 12 "model NewsletterSubscription" prisma/schema.prisma && ls prisma/migrations/ 2>&1 | tail -3</automated>
  </verify>
  <done>Modello NewsletterSubscription presente nello schema, migration applicata (o db:push eseguito), client Prisma rigenerato (`prisma.newsletterSubscription` accessibile), typecheck baseline 25→25.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Endpoint POST /api/user/export</name>
  <files>src/routes/api/user/export.ts</files>
  <action>
Creare endpoint nuovo `src/routes/api/user/export.ts` seguendo pattern di `src/routes/api/cookie-consent.ts`:

1. Auth required: usa `await getUser(request)` da `~/lib/sdk-auth.server`. Se null → `apiError("UNAUTHORIZED", "Devi essere autenticato", 401)`.
2. Rate limit custom (NON via checkRateLimit, perché serve per-userId per-day):
   ```typescript
   const oneDayAgo = new Date(Date.now() - 24 * 60 * 60 * 1000);
   const recentExports = await prisma.dataRequest.count({
     where: { userId: user.id, type: "export", createdAt: { gte: oneDayAgo } },
   });
   if (recentExports >= 3) {
     return apiError("RATE_LIMITED", "Hai raggiunto il limite di 3 export/giorno. Riprova domani.", 429);
   }
   ```
3. Query parallele in `Promise.all`:
   - `prisma.user.findUnique({ where: { id }, select: { id, name, email, emailVerified, image, createdAt, updatedAt, role } })` (NO password/banReason)
   - `prisma.address.findMany({ where: { userId } })`
   - `prisma.order.findMany({ where: { userId }, include: { items: true, payments: { select: { id, amount, currency, status, method, createdAt } } } })` (NO Stripe IDs nel dump)
   - `prisma.wishlist.findMany({ where: { userId }, include: { product: { select: { id, name, slug } } } })`
   - `prisma.consentLog.findMany({ where: { userId }, orderBy: { createdAt: "desc" } })`
   - `prisma.auditLog.findMany({ where: { userId }, orderBy: { createdAt: "desc" }, take: 500 })` (cap a 500 per dimensione)
   - `prisma.review.findMany({ where: { userId } })`
4. Costruisci payload `{ exportedAt: new Date().toISOString(), user, addresses, orders, wishlist, consentLogs, auditLogs, reviews }`. Convertire Decimal a Number con helper inline o `JSON.stringify(payload, (k,v) => typeof v === "object" && v?.constructor?.name === "Decimal" ? Number(v) : v)`.
5. Insert `prisma.dataRequest.create({ data: { userId: user.id, type: "export", status: "completed", completedAt: new Date() } })` (in parallelo o subito dopo, non blocca la risposta).
6. Audit log: `await auditLog({ userId: user.id, event: "data_exported", ip: getClientIp(request), userAgent: request.headers.get("user-agent") })`.
7. Risposta: usare `Response` raw (NON apiSuccess perché serve Content-Disposition):
   ```typescript
   const today = new Date().toISOString().slice(0, 10);
   return new Response(JSON.stringify(payload, replacer, 2), {
     status: 200,
     headers: {
       "Content-Type": "application/json",
       "Content-Disposition": `attachment; filename="prevenzano-dati-${user.id}-${today}.json"`,
     },
   });
   ```
8. Try/catch globale con `apiError("INTERNAL_ERROR", "Errore durante l'esportazione", 500)`, log via `createLogger("user-export")`.

Pattern frontmatter file: `import { createFileRoute } from "@tanstack/react-router"` + `Route = createFileRoute("/api/user/export")({ server: { handlers: { POST: async ({ request }) => { ... } } } })`.

Zero `any`. Catch `(e: unknown)` + `instanceof Error`.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 && test -f src/routes/api/user/export.ts && grep -c "Content-Disposition" src/routes/api/user/export.ts</automated>
  </verify>
  <done>File esiste, typecheck baseline preservata, endpoint risponde 401 senza auth, 200 con dump JSON downloadable per utente loggato, DataRequest e AuditLog inseriti, rate limit 3/24h via DataRequest count.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Endpoint POST /api/user/delete (anonymize)</name>
  <files>src/routes/api/user/delete.ts</files>
  <action>
Creare endpoint nuovo `src/routes/api/user/delete.ts`. **CRITICO**: NON `prisma.user.delete()` mai (cancellerebbe ordini via cascade).

1. Auth required: `await getUser(request)`, 401 se null.
2. Body validation Zod:
   ```typescript
   const deleteSchema = z.object({ confirm: z.literal("CANCELLA") });
   const parsed = deleteSchema.safeParse(await request.json());
   if (!parsed.success) return apiError("VALIDATION_ERROR", "Conferma richiesta", 422);
   ```
3. Rate limit AUTH preset per IP (5/15min) + userId (1 delete attivo/giorno via DataRequest):
   ```typescript
   const ip = getClientIp(request);
   const limit = checkRateLimit(`delete-${user.id}`, "AUTH");
   if (!limit.success) return apiError("RATE_LIMITED", "Troppi tentativi. Riprova più tardi.", 429, undefined, { "Retry-After": String(Math.ceil(limit.retryAfterMs / 1000)) });
   ```
4. Generare email anonimizzata: `import { createId } from "@paralleldrive/cuid2"` se presente, altrimenti usare `crypto.randomUUID()`. Pattern: `const anonEmail = \`deleted-\${crypto.randomUUID()}@deleted.local\`;`
5. **Transazione atomica obbligatoria** (NESSUNA `prisma.user.delete`):
   ```typescript
   const userAgent = request.headers.get("user-agent");
   await prisma.$transaction([
     // Anonimizza user (preserva id per FK Order.userId, ma scarnifica PII)
     prisma.user.update({
       where: { id: user.id },
       data: {
         email: anonEmail,
         name: "Utente cancellato",
         image: null,
         emailVerified: false,
       },
     }),
     // Cancella credenziali (passwordHash è in account.password — schema:86)
     prisma.account.deleteMany({ where: { userId: user.id } }),
     // Cancella sessioni (logout forzato globale)
     prisma.session.deleteMany({ where: { userId: user.id } }),
     // Cancella PII collaterale (review anonime sono incoerenti, cancellate)
     prisma.address.deleteMany({ where: { userId: user.id } }),
     prisma.cart.deleteMany({ where: { userId: user.id } }),
     prisma.wishlist.deleteMany({ where: { userId: user.id } }),
     prisma.review.deleteMany({ where: { userId: user.id } }),
     // Audit trail
     prisma.auditLog.create({
       data: { userId: user.id, event: "account_deleted", ip, userAgent, metadata: { anonymizedEmail: anonEmail } },
     }),
     prisma.dataRequest.create({
       data: { userId: user.id, type: "delete", status: "completed", completedAt: new Date() },
     }),
   ]);
   ```
6. **NON toccare**: Order, Payment, OrderItem (cascade da Order). Restano in DB con `userId` valorizzato (utente ora anonimo) e `Order.shippingAddress` JSON snapshot ha già nome/indirizzo storico (immutabili per legge fiscale).
7. Invalidare la sessione corrente: dopo la transazione `await auth.api.signOut({ headers: request.headers })` (import `auth` da `~/lib/auth`). Forwardare i Set-Cookie clear nella risposta:
   ```typescript
   const signOutResult = await auth.api.signOut({ headers: request.headers });
   const setCookies = (signOutResult as { headers?: { getSetCookie?: () => string[] } }).headers?.getSetCookie?.() ?? [];
   const headers: Record<string, string> = {};
   if (setCookies.length > 0) headers["Set-Cookie"] = setCookies.join(", ");
   return apiSuccess({ deleted: true }, 200, headers);
   ```
   (Note: se `Set-Cookie` multipli causano problemi col helper apiSuccess che accetta `Record<string,string>`, in alternativa costruire `Response.json(...)` manualmente con `headers: new Headers()` e `appendHeader` per ogni cookie.)
8. Try/catch globale → `apiError("INTERNAL_ERROR", "Errore durante la cancellazione", 500)`. Log via `createLogger("user-delete")`.

Zero `any`. Catch `(e: unknown)`.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 && test -f src/routes/api/user/delete.ts && grep -c "user.update\|account.deleteMany\|session.deleteMany" src/routes/api/user/delete.ts && ! grep -q "user.delete\b" src/routes/api/user/delete.ts</automated>
  </verify>
  <done>File esiste, NON contiene `user.delete` (solo `user.update`), tutti i deleteMany sui modelli PII presenti, transazione atomica, sessione invalidata, AuditLog + DataRequest inseriti, typecheck baseline preservata.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: /account/privacy page + sidebar link</name>
  <files>src/routes/account/privacy.tsx, src/routes/account.tsx</files>
  <action>
**File 1 — NEW `src/routes/account/privacy.tsx`** (~150 LOC, sotto soglia 200):

Pattern: `createFileRoute("/account/privacy")` con beforeLoad che carica `consentLogs` recenti via createServerFn. Layout in 3 sezioni stacked.

```typescript
// Server function per fetch consent history (ultimi 20 per userId corrente)
import { createServerFn } from "@tanstack/react-start";
import { getRequest } from "@tanstack/react-start/server";

const $getConsentHistory = createServerFn({ method: "GET" }).handler(async () => {
  const request = getRequest();
  const { getUser } = await import("~/lib/sdk-auth.server");
  const user = await getUser(request);
  if (!user) return [];
  const { prisma } = await import("~/lib/db.server");
  const logs = await prisma.consentLog.findMany({
    where: { userId: user.id },
    orderBy: { createdAt: "desc" },
    take: 20,
  });
  return logs.map((l) => ({
    id: l.id,
    type: l.type,
    granted: l.granted,
    createdAt: l.createdAt.toISOString(),
  }));
});

export const Route = createFileRoute("/account/privacy")({
  beforeLoad: async () => {
    const consentHistory = await $getConsentHistory();
    return { consentHistory };
  },
  component: PrivacyPage,
});
```

UI sections (use design tokens `var(--color-text)` etc., shadcn `Button` se disponibile altrimenti `<button>` con classi coerenti a `account/profilo.tsx`):

**Sezione 1 "I tuoi dati"**:
- Card con descrizione "Esercita il tuo diritto di accesso e portabilità (Art. 15/20 GDPR). Scarica un dump JSON di tutti i tuoi dati."
- Bottone "Scarica i miei dati (JSON)" → `onClick`:
  ```typescript
  setExporting(true);
  try {
    const res = await fetch("/api/user/export", { method: "POST", credentials: "same-origin" });
    if (!res.ok) {
      const err = await res.json().catch(() => ({}));
      toast.error(err?.error?.message ?? "Errore durante l'export");
      return;
    }
    // Trigger download dal blob
    const blob = await res.blob();
    const url = URL.createObjectURL(blob);
    const a = document.createElement("a");
    a.href = url;
    a.download = `prevenzano-dati-${new Date().toISOString().slice(0,10)}.json`;
    document.body.appendChild(a);
    a.click();
    a.remove();
    URL.revokeObjectURL(url);
    toast.success("Download avviato");
  } catch { toast.error("Errore di connessione"); }
  finally { setExporting(false); }
  ```

**Sezione 2 "Cancella account"**:
- Card destructive (border red-200 bg-red-50/30 sottile)
- Testo: "Questa azione è irreversibile. I tuoi dati personali verranno anonimizzati. Gli ordini esistenti restano in archivio per obblighi di legge fiscale (10 anni, DPR 633/72)."
- Bottone destructive "Cancella definitivamente account" → apre `<TypeConfirmDialog>` (import da `~/components/admin/TypeConfirmDialog`) con:
  - title: "Conferma cancellazione account"
  - message: "Email, nome, indirizzi, wishlist, carrello e recensioni saranno cancellati. Gli ordini restano (anonimi) per obblighi fiscali."
  - confirmText: "CANCELLA"
  - confirmLabel: "Cancella account"
  - onConfirm: async → POST /api/user/delete con `{ confirm: "CANCELLA" }`, su success `toast.success("Account cancellato. Sarai reindirizzato.")` poi `setTimeout(() => { window.location.href = "/"; }, 1500)`.

**Sezione 3 "Storico consensi"**:
- Tabella read-only (max-w-2xl) con colonne: Data | Tipo | Stato.
- Render `consentHistory.map((log) => <tr>...)`.
- Type label mapping: `cookie`/`preferences`/`analytics`/`marketing`/`privacy` → label IT ("Cookie", "Preferenze", "Analytics", "Marketing", "Privacy").
- Granted: badge verde "Concesso" / badge rosso "Revocato".
- Stato vuoto: "Nessun consenso registrato finora."

Aggiungere h1 page `Privacy e dati`, sotto-sezioni con h2 della categoria.

Componenti usati: `motion/react m.div` per fade-in (pattern da profilo.tsx), `lucide-react` icons (Download, Trash2, FileText), `sonner` toast. NO shadcn radix per dialog (TypeConfirmDialog è handcrafted già).

**File 2 — MODIFICA `src/routes/account.tsx`**:

Aggiungere a `NAV_ITEMS` (line 22-29) una nuova voce DOPO "Password":
```typescript
{ label: "Privacy e dati", href: "/account/privacy", icon: Shield, matchPath: "/account/privacy" as const },
```
Importare `Shield` da `lucide-react` (riga 3, append all'import existente).

NESSUN altro cambio a account.tsx.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 && test -f src/routes/account/privacy.tsx && grep -c "TypeConfirmDialog\|consentHistory" src/routes/account/privacy.tsx && grep -c "Privacy e dati" src/routes/account.tsx</automated>
  </verify>
  <done>Pagina /account/privacy esiste con 3 sezioni (export/delete/history), TypeConfirmDialog usato per delete, link "Privacy e dati" presente in sidebar account, typecheck baseline preservata.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 5: Endpoint POST /api/user/consents + RegisterForm hook</name>
  <files>src/routes/api/user/consents.ts, src/components/auth/RegisterForm.tsx</files>
  <action>
**File 1 — NEW `src/routes/api/user/consents.ts`** (~60 LOC):

Endpoint che riceve i consensi GDPR del signup (privacy obbligatorio, marketing opzionale) DOPO che `authClient.signUp.email()` ha creato user + sessione (autoSignIn: true).

```typescript
import { createFileRoute } from "@tanstack/react-router";
import { z } from "zod";
import { apiSuccess, apiError } from "~/lib/api-response";
import { getUser } from "~/lib/sdk-auth.server";
import { getClientIp, checkRateLimit } from "~/lib/rate-limit.server";
import { prisma } from "~/lib/db.server";
import { createLogger } from "~/lib/logger.server";

const log = createLogger("user-consents");

const schema = z.object({
  privacy: z.literal(true),    // obbligatorio per signup
  marketing: z.boolean(),      // opzionale
});

export const Route = createFileRoute("/api/user/consents")({
  server: {
    handlers: {
      POST: async ({ request }) => {
        const ip = getClientIp(request);
        const limit = checkRateLimit(ip, "FORM");
        if (!limit.success) return apiError("RATE_LIMITED", "Troppe richieste", 429);

        const user = await getUser(request);
        if (!user) return apiError("UNAUTHORIZED", "Devi essere autenticato", 401);

        const parsed = schema.safeParse(await request.json());
        if (!parsed.success) {
          return apiError("VALIDATION_ERROR", parsed.error.issues[0]?.message ?? "Payload non valido", 422);
        }
        const { privacy, marketing } = parsed.data;
        const userAgent = request.headers.get("user-agent");

        try {
          const rows: Array<{ userId: string; type: string; granted: boolean; ip: string; userAgent: string | null }> = [
            { userId: user.id, type: "privacy", granted: privacy, ip, userAgent },
          ];
          if (marketing) rows.push({ userId: user.id, type: "marketing", granted: true, ip, userAgent });
          await prisma.consentLog.createMany({ data: rows });
        } catch (e: unknown) {
          log.error("ConsentLog insert failed", { error: e instanceof Error ? e.message : "unknown" });
          return apiError("INTERNAL_ERROR", "Errore salvataggio consensi", 500);
        }

        return apiSuccess({ logged: true });
      },
    },
  },
});
```

**File 2 — MODIFICA `src/components/auth/RegisterForm.tsx`** (linee 38-61, dentro `onSubmit`):

DOPO il check `if (error) { ... return; }` e PRIMA di `setSuccess(true)`, aggiungere:

```typescript
// Log GDPR consents server-side (audit-trail by-presence per privacy + marketing opt-in)
// Nota: failure here non blocca il signup (consenso esplicito è già stato dato client-side)
try {
  await fetch("/api/user/consents", {
    method: "POST",
    headers: { "Content-Type": "application/json" },
    credentials: "same-origin",
    body: JSON.stringify({
      privacy: true,                            // sempre true per signup (validato client-side)
      marketing: value.marketingConsent,        // opzionale
    }),
  });
} catch {
  // Silent fail: audit log non blocca user experience
}
```

Zero `any`. NESSUN'altra modifica a RegisterForm o GdprConsentFields.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 && test -f src/routes/api/user/consents.ts && grep -c "api/user/consents" src/components/auth/RegisterForm.tsx</automated>
  </verify>
  <done>Endpoint /api/user/consents esiste, RegisterForm chiama l'endpoint dopo signup success con privacy:true + marketing dal checkbox, typecheck baseline preservata. Smoke: nuovo signup → query `SELECT * FROM "ConsentLog" WHERE "userId" = X` mostra 1 row privacy + (se opt-in) 1 row marketing.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 6: Newsletter rewrite double opt-in (subscribe)</name>
  <files>src/routes/api/newsletter.ts</files>
  <action>
Riscrivere completamente `src/routes/api/newsletter.ts` (attualmente è un TODO log+success). Pattern double opt-in:

1. Mantenere rate limit FORM esistente.
2. Schema esistente già valido (`{ email }`), preservare.
3. Logica:
   ```typescript
   import { createId } from "@paralleldrive/cuid2"; // o crypto.randomUUID() se cuid2 non installato (verificare package.json)
   import { prisma } from "~/lib/db.server";
   import { sendEmail } from "~/lib/email.server";
   
   // Trova existing
   const existing = await prisma.newsletterSubscription.findUnique({ where: { email: parsed.data.email } });
   
   // Idempotency: se già confirmed → 200 senza inviare email
   if (existing?.status === "confirmed") {
     return apiSuccess({ message: "Sei già iscritto alla newsletter" });
   }
   
   // Genera nuovo token (re-issue su pending o unsubscribed per resubscribe)
   const token = createId(); // o crypto.randomUUID()
   const subscription = await prisma.newsletterSubscription.upsert({
     where: { email: parsed.data.email },
     create: { email: parsed.data.email, token, status: "pending" },
     update: { token, status: "pending", confirmedAt: null }, // reset su resubscribe
   });
   
   // Invia email confirm
   const baseUrl = process.env.BETTER_AUTH_URL ?? "https://calzoleriaprevenzano.it";
   const confirmUrl = `${baseUrl}/newsletter/conferma?token=${token}`;
   const unsubscribeUrl = `${baseUrl}/newsletter/disiscriviti?token=${token}`;
   
   const html = `<!doctype html>
<html lang="it"><head><meta charset="utf-8"><title>Conferma iscrizione</title></head>
<body style="margin:0;padding:0;background:#f5f3ef;font-family:Georgia,serif;">
  <table role="presentation" width="100%" cellpadding="0" cellspacing="0" style="background:#f5f3ef;padding:32px 16px;">
    <tr><td align="center">
      <table role="presentation" width="560" cellpadding="0" cellspacing="0" style="background:#ffffff;border-radius:8px;padding:40px 32px;max-width:560px;">
        <tr><td>
          <h1 style="margin:0 0 16px;font-size:22px;color:#2d2419;font-weight:600;">Conferma la tua iscrizione</h1>
          <p style="margin:0 0 24px;font-size:15px;line-height:1.6;color:#5a4d3f;">
            Grazie per esserti iscritto alla newsletter di Calzoleria Prevenzano. Per completare l'iscrizione, conferma il tuo indirizzo email cliccando sul pulsante qui sotto.
          </p>
          <table role="presentation" cellpadding="0" cellspacing="0"><tr><td style="border-radius:4px;background:#8b6f47;">
            <a href="${confirmUrl}" style="display:inline-block;padding:14px 28px;color:#ffffff;text-decoration:none;font-size:14px;font-weight:600;letter-spacing:0.5px;">Conferma iscrizione</a>
          </td></tr></table>
          <p style="margin:32px 0 0;font-size:13px;line-height:1.5;color:#8a7d6b;">
            Se non riesci a cliccare il pulsante, copia e incolla questo link nel browser:<br/>
            <a href="${confirmUrl}" style="color:#8b6f47;word-break:break-all;">${confirmUrl}</a>
          </p>
          <p style="margin:24px 0 0;font-size:12px;color:#a89c8a;">
            Se non hai richiesto questa iscrizione, ignora questa email. Non riceverai altri messaggi da noi.
          </p>
        </td></tr>
      </table>
      <p style="margin:16px 0 0;font-size:11px;color:#a89c8a;">
        Calzoleria Prevenzano · <a href="${unsubscribeUrl}" style="color:#a89c8a;">Disiscriviti</a>
      </p>
    </td></tr>
  </table>
</body></html>`;
   
   const result = await sendEmail({
     to: parsed.data.email,
     subject: "Conferma la tua iscrizione alla newsletter",
     html,
   });
   
   if (!result.ok) {
     log.error("Newsletter confirm email failed", { email: parsed.data.email });
     // NON 500: la subscription è creata, l'utente può richiedere reinvio
     return apiSuccess({
       message: "Iscrizione registrata. Se non ricevi l'email entro qualche minuto, riprova.",
     }, 201);
   }
   
   return apiSuccess({
     message: "Ti abbiamo inviato un'email di conferma. Clicca sul link per completare l'iscrizione.",
   }, 201);
   ```

4. **NON** inserire `ConsentLog` qui — viene loggato SOLO in /api/newsletter/confirm dopo conferma effettiva (audit-by-presence: il consenso vale solo se confermato).
5. Verificare se `@paralleldrive/cuid2` è in `package.json`. Se NON c'è, usare `crypto.randomUUID()` (Node 22 native, no dep aggiunta). Documentare nel commit la scelta.
6. Mantenere log esistente `log.info("New subscription", { email })` ma aggiungere `, { status: existing?.status ?? "new" }`.

Zero `any`.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 && grep -c "newsletterSubscription\|sendEmail\|conferma" src/routes/api/newsletter.ts</automated>
  </verify>
  <done>Endpoint /api/newsletter ora upserta NewsletterSubscription, invia email confirm via sendEmail, NON logga ConsentLog (sposta a /confirm), idempotente su email già confirmed, typecheck preservata.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 7: Newsletter confirm + unsubscribe (API + page routes)</name>
  <files>src/routes/api/newsletter/confirm.ts, src/routes/api/newsletter/unsubscribe.ts, src/routes/newsletter.conferma.tsx, src/routes/newsletter.disiscriviti.tsx</files>
  <action>
**File 1 — NEW `src/routes/api/newsletter/confirm.ts`** (~50 LOC):

```typescript
import { createFileRoute } from "@tanstack/react-router";
import { z } from "zod";
import { apiSuccess, apiError } from "~/lib/api-response";
import { prisma } from "~/lib/db.server";
import { getClientIp, checkRateLimit } from "~/lib/rate-limit.server";
import { createLogger } from "~/lib/logger.server";

const log = createLogger("newsletter-confirm");
const schema = z.object({ token: z.string().min(1) });

export const Route = createFileRoute("/api/newsletter/confirm")({
  server: {
    handlers: {
      POST: async ({ request }) => {
        const ip = getClientIp(request);
        const limit = checkRateLimit(ip, "FORM");
        if (!limit.success) return apiError("RATE_LIMITED", "Troppe richieste", 429);

        const parsed = schema.safeParse(await request.json());
        if (!parsed.success) return apiError("VALIDATION_ERROR", "Token mancante", 422);

        try {
          const sub = await prisma.newsletterSubscription.findUnique({ where: { token: parsed.data.token } });
          if (!sub) return apiError("NOT_FOUND", "Token non valido o già usato", 404);
          if (sub.status === "confirmed") return apiSuccess({ message: "Iscrizione già confermata" });
          if (sub.status === "unsubscribed") return apiError("INVALID_STATE", "Subscription disiscritta. Iscriviti di nuovo dal sito.", 410);

          await prisma.$transaction([
            prisma.newsletterSubscription.update({
              where: { id: sub.id },
              data: { status: "confirmed", confirmedAt: new Date() },
            }),
            // Audit-by-presence: il consenso marketing vale solo dopo conferma double opt-in
            prisma.consentLog.create({
              data: {
                userId: null, // newsletter può essere indipendente da account
                type: "marketing",
                granted: true,
                ip,
                userAgent: request.headers.get("user-agent"),
              },
            }),
          ]);
          return apiSuccess({ message: "Iscrizione confermata" });
        } catch (e: unknown) {
          log.error("Confirm failed", { error: e instanceof Error ? e.message : "unknown" });
          return apiError("INTERNAL_ERROR", "Errore conferma", 500);
        }
      },
    },
  },
});
```

**File 2 — NEW `src/routes/api/newsletter/unsubscribe.ts`** (~45 LOC):

Pattern identico a confirm ma:
- `if (sub.status === "unsubscribed")` → `apiSuccess({ message: "Già disiscritto" })`
- update: `{ status: "unsubscribed" }`
- ConsentLog: `{ type: "marketing", granted: false, ... }` (eccezione audit-by-presence: revoca esplicita va tracciata)
- response: `{ message: "Disiscrizione completata" }`

**File 3 — NEW `src/routes/newsletter.conferma.tsx`** (~50 LOC):

Page con loader che chiama il confirm endpoint. TanStack Router file-based: `newsletter.conferma.tsx` → URL `/newsletter/conferma`.

```typescript
import { createFileRoute } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { CheckCircle2, XCircle, Loader2 } from "lucide-react";

const confirmSearchSchema = (search: Record<string, unknown>): { token: string } => ({
  token: typeof search.token === "string" ? search.token : "",
});

export const Route = createFileRoute("/newsletter/conferma")({
  validateSearch: confirmSearchSchema,
  component: ConfirmPage,
});

function ConfirmPage() {
  const { token } = Route.useSearch();
  const [state, setState] = useState<"loading" | "success" | "error">("loading");
  const [message, setMessage] = useState("");

  useEffect(() => {
    if (!token) { setState("error"); setMessage("Link non valido: token mancante."); return; }
    fetch("/api/newsletter/confirm", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ token }),
    })
      .then(async (res) => {
        const json = await res.json().catch(() => ({}));
        if (res.ok) { setState("success"); setMessage(json?.data?.message ?? "Iscrizione confermata!"); }
        else { setState("error"); setMessage(json?.error?.message ?? "Errore durante la conferma"); }
      })
      .catch(() => { setState("error"); setMessage("Errore di connessione"); });
  }, [token]);

  return (
    <div className="mx-auto max-w-lg px-[var(--page-padding-x)] py-24 text-center">
      {state === "loading" && <Loader2 className="mx-auto h-12 w-12 animate-spin text-[var(--color-primary)]" />}
      {state === "success" && <CheckCircle2 className="mx-auto h-16 w-16 text-green-600" />}
      {state === "error" && <XCircle className="mx-auto h-16 w-16 text-red-600" />}
      <h1 className="mt-6 font-display text-2xl font-semibold text-[var(--color-text)]">
        {state === "loading" && "Confermando..."}
        {state === "success" && "Iscrizione confermata"}
        {state === "error" && "Conferma non riuscita"}
      </h1>
      <p className="mt-3 text-sm text-[var(--color-text-secondary)]">{message}</p>
      {state !== "loading" && (
        <a href="/" className="mt-8 inline-block text-sm font-medium text-[var(--color-primary)] underline-offset-4 hover:underline">
          Torna alla home
        </a>
      )}
    </div>
  );
}
```

**File 4 — NEW `src/routes/newsletter.disiscriviti.tsx`** (~45 LOC):

Pattern identico a conferma.tsx ma:
- URL `/newsletter/disiscriviti`
- POST `/api/newsletter/unsubscribe`
- testi: "Disiscrizione...", "Disiscrizione completata", "Disiscrizione non riuscita"
- success message: "Sei stato disiscritto dalla newsletter. Ci dispiace vederti andare."

NOTA TanStack Router file convention: il dot `.` nel filename è interpretato come path separator. Quindi `newsletter.conferma.tsx` → `/newsletter/conferma` correttamente. NON usare `[token].tsx` o segmenti dynamic — il token viaggia in querystring (`?token=XXX`) come da spec.

Zero `any`.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 && test -f src/routes/api/newsletter/confirm.ts && test -f src/routes/api/newsletter/unsubscribe.ts && test -f src/routes/newsletter.conferma.tsx && test -f src/routes/newsletter.disiscriviti.tsx</automated>
  </verify>
  <done>4 file creati, confirm valida token + setta status + logga ConsentLog granted=true, unsubscribe simmetrico con granted=false, page routes mostrano loading/success/error states con auto-fetch su mount, typecheck baseline preservata.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 8: Admin /admin/consensi panel (server fn + page + validator)</name>
  <files>src/lib/admin/admin-consents.server.ts, src/routes/admin.consensi.tsx, src/lib/validators/admin.ts</files>
  <action>
**File 1 — MODIFICA `src/lib/validators/admin.ts`** (append):

```typescript
export const listConsentLogsSchema = z.object({
  page: z.coerce.number().int().positive().default(1),
  perPage: z.coerce.number().int().positive().max(50).default(12),
  type: z.enum(["cookie", "preferences", "analytics", "marketing", "privacy"]).optional(),
  createdFrom: z.coerce.date().optional(),
  createdTo: z.coerce.date().optional(),
});
export type ListConsentLogsInput = z.infer<typeof listConsentLogsSchema>;
```

**File 2 — NEW `src/lib/admin/admin-consents.server.ts`** (~70 LOC, pattern mirror di `admin-orders.server.ts:14-100`):

```typescript
import { prisma } from "~/lib/db.server";
import type { PaginatedData } from "~/lib/types/api";
import type { ListConsentLogsInput } from "~/lib/validators/admin";

export interface AdminConsentLogItem {
  id: string;
  userId: string | null;
  userEmail: string | null;   // join to user.email se userId presente
  type: string;
  granted: boolean;
  ip: string | null;
  userAgent: string | null;
  createdAt: string;
}

export async function listConsentLogs(
  input: ListConsentLogsInput,
): Promise<PaginatedData<AdminConsentLogItem>> {
  const { page, perPage, type, createdFrom, createdTo } = input;
  const skip = (page - 1) * perPage;

  const conditions: Array<Record<string, unknown>> = [];
  if (type) conditions.push({ type });
  if (createdFrom || createdTo) {
    const range: Record<string, Date> = {};
    if (createdFrom) range.gte = createdFrom;
    if (createdTo) range.lte = createdTo;
    conditions.push({ createdAt: range });
  }
  const where = conditions.length > 0 ? { AND: conditions } : {};

  const [items, total] = await Promise.all([
    prisma.consentLog.findMany({
      where,
      orderBy: { createdAt: "desc" },
      skip,
      take: perPage,
    }),
    prisma.consentLog.count({ where }),
  ]);

  // Lookup batch emails (avoid N+1)
  const userIds = Array.from(new Set(items.map((i) => i.userId).filter((id): id is string => id !== null)));
  const users = userIds.length > 0
    ? await prisma.user.findMany({ where: { id: { in: userIds } }, select: { id: true, email: true } })
    : [];
  const emailMap = new Map(users.map((u) => [u.id, u.email]));

  return {
    items: items.map((l): AdminConsentLogItem => ({
      id: l.id,
      userId: l.userId,
      userEmail: l.userId ? emailMap.get(l.userId) ?? null : null,
      type: l.type,
      granted: l.granted,
      ip: l.ip,
      userAgent: l.userAgent ? l.userAgent.slice(0, 80) : null,  // truncate UA
      createdAt: l.createdAt.toISOString(),
    })),
    total,
    page,
    totalPages: Math.ceil(total / perPage),
  };
}
```

Server function wrapper (in stesso file o in `src/lib/admin-functions.ts` se è la convenzione — verificare). Pattern da `admin-functions.ts`:

```typescript
import { createServerFn } from "@tanstack/react-start";
import { getRequest } from "@tanstack/react-start/server";
import { listConsentLogsSchema } from "~/lib/validators/admin";

export const $listConsentLogs = createServerFn({ method: "GET" })
  .validator((data: unknown) => listConsentLogsSchema.parse(data))
  .handler(async ({ data }) => {
    const request = getRequest();
    const { requireAdmin } = await import("~/lib/sdk-auth.server");
    await requireAdmin(request);
    const { listConsentLogs } = await import("~/lib/admin/admin-consents.server");
    return listConsentLogs(data);
  });
```

(Posizionare `$listConsentLogs` in `src/lib/admin-functions.ts` se esiste, altrimenti dentro lo stesso file `.server.ts` non funziona per createServerFn — verificare il pattern usato per `$getAdminOrders`.)

**File 3 — NEW `src/routes/admin.consensi.tsx`** (~150 LOC):

Pattern semplificato di `admin.ordini.tsx:1-160` ma read-only (no bulk actions, no row navigation, no soft-delete). Sezioni:

1. `createFileRoute("/admin/consensi")` con `beforeLoad` che fetcha pagina 1 di consent logs.
2. Voce nav "Consensi" da aggiungere a `src/routes/admin.tsx` NAV_ITEMS dopo "Ordini" — usare icon `FileText` o `Shield` da lucide-react.
3. Component:
   - State: `page`, `type` filter (select con `<option value="">Tutti</option>` + 5 categorie), `createdFrom`, `createdTo` (date inputs)
   - `useEffect` su [page, type, createdFrom, createdTo] → call `$listConsentLogs({ data: {...} })`
   - Tabella: Data | Utente (email link `mailto:` o testo "—" se userId null) | Tipo (badge) | Stato (Concesso/Revocato badge) | IP | UA truncated
   - Paginazione: bottoni Prev/Next + indicatore "Pagina X di Y" + count totale
   - Stato vuoto: "Nessun consenso registrato per i filtri selezionati."
   - Loading state: `<Loader2 className="animate-spin" />` durante fetch

Tipo label mapping IT (mirror della pagina /account/privacy):
- `cookie` → "Cookie"
- `preferences` → "Preferenze"
- `analytics` → "Analytics"
- `marketing` → "Marketing"
- `privacy` → "Privacy"

Granted badge:
- true: `bg-green-100 text-green-800` "Concesso"
- false: `bg-red-100 text-red-800` "Revocato"

**MODIFICA `src/routes/admin.tsx`** linee 22-29 NAV_ITEMS, aggiungere DOPO "AI Advisor":
```typescript
{ label: "Consensi", href: "/admin/consensi", icon: Shield, matchPath: "/admin/consensi" as const },
```
+ import `Shield` da lucide-react (riga 3).

Zero `any`.
  </action>
  <verify>
    <automated>pnpm typecheck 2>&1 | tail -5 && test -f src/routes/admin.consensi.tsx && test -f src/lib/admin/admin-consents.server.ts && grep -c "listConsentLogsSchema" src/lib/validators/admin.ts && grep -c "Consensi" src/routes/admin.tsx</automated>
  </verify>
  <done>Pagina admin /admin/consensi accessibile solo ad admin, mostra ultime ConsentLog con filtri type+date+pagination 12/page, link "Consensi" presente in admin sidebar, validator + server fn + page tutti tipizzati zero-any, typecheck baseline preservata.</done>
</task>

</tasks>

<verification>
**End-to-end manual smoke (post-deploy)**:

1. **Privacy page accessibile**: login → `/account/privacy` mostra 3 sezioni + link "Privacy e dati" in sidebar.
2. **Export funziona**: click "Scarica i miei dati" → file `prevenzano-dati-{userId}-{date}.json` scaricato, contiene user+orders+items+wishlist+addresses+consentLogs+auditLogs+reviews.
3. **Rate limit export**: chiamare 4 volte di fila → 4ª risponde 429 "limite 3/giorno".
4. **Delete account**: click "Cancella account" → TypeConfirmDialog appare → digitare "CANCELLA" → bottone si abilita → conferma → toast success → redirect /. Post-delete: query `SELECT email, name FROM users WHERE id = X` mostra `deleted-{uuid}@deleted.local` e `Utente cancellato`. Query `SELECT * FROM "Order" WHERE "userId" = X` mostra ordini ANCORA presenti (con userId valorizzato). Login fallisce con email originale.
5. **Signup ConsentLog**: registrare nuovo utente con marketing checkbox spuntato → query `SELECT type, granted FROM "ConsentLog" WHERE "userId" = X` ritorna 2 righe (privacy granted=true, marketing granted=true). Senza marketing: 1 riga sola (privacy).
6. **Newsletter double opt-in**: POST /api/newsletter con email valida → email arriva (Resend dashboard delivery), `SELECT status, token FROM "NewsletterSubscription"` mostra `pending`. Click link conferma → page mostra "Iscrizione confermata" → DB mostra `confirmed` + 1 row ConsentLog `marketing/granted=true`.
7. **Newsletter unsubscribe**: click link disiscrizione → page mostra "Disiscrizione completata" → DB mostra `unsubscribed` + 1 row ConsentLog `marketing/granted=false`.
8. **Admin consensi**: login admin → `/admin/consensi` mostra ultime 100 ConsentLog ordinate per createdAt desc, filtri type/date funzionano, paginazione 12/page funziona.

**Automated baseline**:
- `pnpm typecheck` → baseline 25 errors, deve restare 25 (zero regressioni).
- `pnpm lint` se script esiste, no nuove warning.

**Schema invariant** (CRITICAL): post-Task 3, eseguire query manuale:
```sql
-- Cancella user di test, verifica ordini intatti
SELECT id, email FROM users WHERE id = 'test-id'; -- email anonimizzato
SELECT COUNT(*) FROM "Order" WHERE "userId" = 'test-id'; -- > 0
SELECT COUNT(*) FROM accounts WHERE "userId" = 'test-id'; -- 0
SELECT COUNT(*) FROM sessions WHERE "userId" = 'test-id'; -- 0
```
</verification>

<success_criteria>
- [ ] 8 commit atomici italiani (uno per task)
- [ ] 1 nuovo modello Prisma + migration applicata (o db:push) — `NewsletterSubscription`
- [ ] 7 file nuovi creati: `src/routes/account/privacy.tsx`, `src/routes/api/user/{export,delete,consents}.ts`, `src/routes/api/newsletter/{confirm,unsubscribe}.ts`, `src/routes/newsletter.{conferma,disiscriviti}.tsx`, `src/routes/admin.consensi.tsx`, `src/lib/admin/admin-consents.server.ts`
- [ ] 4 file modificati: `prisma/schema.prisma`, `src/routes/account.tsx` (sidebar +1 link), `src/routes/admin.tsx` (sidebar +1 link), `src/components/auth/RegisterForm.tsx` (post-signup hook), `src/routes/api/newsletter.ts` (rewrite), `src/lib/validators/admin.ts` (+listConsentLogsSchema)
- [ ] `pnpm typecheck` baseline 25 → 25 (zero regressioni)
- [ ] Zero `any` esplicito introdotto
- [ ] /api/user/delete NON contiene `prisma.user.delete()` — solo update
- [ ] Order.userId, Payment, OrderItem mai cancellati dal delete account
- [ ] ConsentLog inserito in: signup (privacy + opt marketing), cookie banner (Wave 1 esistente), newsletter confirm (granted=true), newsletter unsubscribe (granted=false). NON inserito in: newsletter subscribe pending.
- [ ] Newsletter idempotency: POST con email già `confirmed` → 200 senza email
- [ ] Rate limits: export 3/giorno per userId, delete AUTH preset, newsletter confirm/unsubscribe FORM preset
</success_criteria>

<output>
After completion:
1. Create `.planning/quick/260430-ov8-wave-3-gdpr-diritti-utente-account-priva/260430-ov8-SUMMARY.md` con: commit list, file list, typecheck delta, smoke test results, eventuali deviazioni Rule 1/2/3.
2. Update `.planning/STATE.md` `last_activity` con riepilogo completo Wave 3.
3. NON pushare a origin se non esplicitamente autorizzato dall'utente.
</output>
