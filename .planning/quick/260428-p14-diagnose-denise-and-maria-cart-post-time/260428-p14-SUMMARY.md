---
quick_id: 260428-p14
type: summary
date: 2026-04-28
mode: quick-task
status: complete
duration_minutes: 18
files_modified:
  - tests/e2e/all-products-purchase.spec.ts
commits:
  - 66470c2  # fix(test): retry-click helper per flake denise/maria
  - f1f6cb6  # docs(quick): SUMMARY 260428-p14
---

# Quick 260428-p14 — SUMMARY

## Esito sintetico

**Risolto.** Il flake `denise`/`maria` è stato neutralizzato con un retry-click helper inline (3 tentativi, 250ms backoff) e un predicato `waitForResponse` rilassato (URL+method, niente più `res.ok()`). Targeted run deterministico 3/3 PASS (denise + maria + raffaella-maria, matchati dal grep). Full parametric: **118/119 PASS al primo passaggio, 0 flaky, 1 hard-fail residuo (`provv`)** con root cause data-quality fuori scope.

## Diff dei 2 fix

File modificato: `tests/e2e/all-products-purchase.spec.ts` (+35, -8).

### Fix A — drop `res.ok()` dal predicato

```diff
-      await Promise.all([
-        page.waitForResponse(
-          (res) => res.url().includes("/api/cart") && res.request().method() === "POST" && res.ok(),
-          { timeout: 15_000 },
-        ),
-        page.getByRole("button", { name: /Aggiungi al carrello/i }).click(),
-      ]);
+      await clickAddToCartAndWaitPost(page);
```

Il predicato vive ora dentro l'helper come URL+method only. L'assertion sullo status è AFTER `await`: ogni 4xx futuro emerge come `POST /api/cart returned <status>: <body>` invece di un timeout opaco a 15s.

### Fix B — retry-click helper inline

```diff
+import { test, expect, request as pwRequest, type Page, type Response } from "@playwright/test";
...
+/**
+ * Click "Aggiungi al carrello" e attendi la POST /api/cart con retry.
+ * Assorbe la flake client-side osservata sotto workers=4 dove il click
+ * atterra ma il fetch non viene dispatchato (cfr. RESEARCH 260428-p14).
+ */
+async function clickAddToCartAndWaitPost(page: Page, maxAttempts = 3): Promise<Response> {
+  for (let i = 0; i < maxAttempts; i++) {
+    try {
+      const [res] = await Promise.all([
+        page.waitForResponse(
+          (r) => r.url().includes("/api/cart") && r.request().method() === "POST",
+          { timeout: 5_000 },
+        ),
+        page.getByRole("button", { name: /Aggiungi al carrello/i }).click(),
+      ]);
+      if (!res.ok()) {
+        const body = await res.text();
+        throw new Error(`POST /api/cart returned ${res.status()}: ${body}`);
+      }
+      return res;
+    } catch (err) {
+      if (i === maxAttempts - 1) throw err;
+      await page.waitForTimeout(250);
+    }
+  }
+  throw new Error("unreachable");
+}
```

- Subscribe a `waitForResponse` PRIMA del click (invariante ordering preservato).
- Per-attempt timeout 5_000ms (era 15_000 monolitico): se la POST non parte, ritentiamo 2 volte invece di aspettare.
- Tipi `Page` e `Response` importati da `@playwright/test` — zero `any` (CLAUDE.md TS strict).
- Inline nel solo `all-products-purchase.spec.ts`. **Non tocca** `helpers/variants.ts` né `smoke-purchase.spec.ts` (D-03, D-04).

## Targeted result (locked verification)

**Comando:**

```bash
pnpm exec playwright test tests/e2e/all-products-purchase.spec.ts \
  --project=all-products --grep "denise|maria" \
  --workers=1 --retries=0 --reporter=list
```

**Output:**

```
Running 3 tests using 1 worker

  ✓  1 [all-products] › ... › compra denise (23.7s)
  ✓  2 [all-products] › ... › compra maria (21.4s)
  ✓  3 [all-products] › ... › compra raffaella-maria (20.0s)

  3 passed (1.2m)
```

3/3 PASS deterministico (`raffaella-maria` matchato dal pattern `maria`). Esito atteso: 2/2 (denise + maria); ottenuto 3/3 grazie al greedy match — zero failure, zero flake.

## Full parametric: before / after

**Comando:**

```bash
pnpm test:e2e:all -- --reporter=list
# = playwright test tests/e2e/all-products-purchase.spec.ts \
#     --project=all-products --workers=4 --retries=1 --timeout=90000 --fully-parallel
```

| Metric | Before (o8a baseline) | After (p14) | Δ |
|---|---|---|---|
| Tests totali | 119 | 119 | — |
| Passed (first-pass) | 108 | **118** | **+10** |
| Flaky (recovered con retries=1) | 8 | **0** | **−8** |
| Effective passed | 116/119 | **118/119** | **+2** |
| Hard fail | 3 (provv, denise, maria) | **1 (provv)** | **−2** |
| Wall time | ~13m | 11.4m | −1.6m |

**Note operative:**
- denise, maria, raffaella-maria sono tutti passati al **primo tentativo** sotto `workers=4` (verificato: nessuna riga `(retry #1)` per questi slug nel log run).
- Il drop dei flaky da 8 → 0 conferma che la stessa race client-side (click landed / fetch non dispatched) coperta in o8a tramite `retries=1` viene ora assorbita dal retry-click in-test, lasciando il budget retry libero.
- Provv fallisce sia al primo tentativo sia al `retry #1` — è una regressione hard reale, non flake.

## Hard fail residuo: `provv`

**Errore:**

```
Error: Timed out 10000ms waiting for expect(locator).toBeEnabled()
  Locator: getByRole('button', { name: /Aggiungi al carrello/i })
  Expected: enabled
  at expectAddToCartEnabled (tests/e2e/helpers/variants.ts:110:24)
  at tests/e2e/all-products-purchase.spec.ts:122:13
```

**Root cause (riportato da RESEARCH §"Out of scope" e o8a-SUMMARY):** `provv` è un placeholder di test in DB con `variantConfig` incompleto o varianti senza stock — il bottone "Aggiungi al carrello" non si abilita mai perché la selezione varianti rimane invalida. Non è un bug funzionale: nessun cliente reale lo vedrebbe perché il prodotto non è destinato al pubblico.

**Conferma scope:** il fallimento avviene su `expectAddToCartEnabled` PRIMA del cart-POST, quindi è completamente a monte rispetto al fix p14. Il retry-click helper non c'entra.

## Next-quick recommendation

Dato che `provv` è l'unico hard-fail residuo e ha root cause data-quality (non funzionale), proporre un next-quick `260428-p15` (o equivalente) con due opzioni:

| Opzione | Pro | Contro |
|---|---|---|
| **A. DB write `UPDATE Product SET isActive = false WHERE slug = 'provv'`** | Rimuove il prodotto dalla discovery `/api/products` → il test parametrico passa a 118/118 e il listing pubblico non lo mostra mai. | Richiede ENV/DB access in produzione (Railway). |
| **B. `test.skip(slug === 'provv', "test placeholder, see 260428-p15")` nel for-loop** | Solo test, no DB write, atomic. | Lascia un dato sporco in DB. |

Raccomandazione: **Opzione A** se l'utente conferma che `provv` è un placeholder genuino (RESEARCH lo classifica così con HIGH confidence). Altrimenti **B** come quick-and-safe.

**Esplicitamente fuori scope per p14** per task constraint.

## Out of scope (carried from research)

- Disattivazione DB di `provv` (vedi sezione precedente).
- Migration delle Postgres sequences sui Order/Customer (incrementi non monotoni osservati in run paralleli — RESEARCH 260428-nd8).
- Cleanup ordini di test (`UPDATE Order ... WHERE email LIKE 'e2e+%@test.calzoleriaprevenzano.it'`).
- Pre-existing TS warnings in `src/routes/prodotti.$slug.tsx` (loader return type non tipato — non emerso da p14 ma confermato durante `tsc --noEmit`; nessuna regressione introdotta dal fix).
- Pre-existing Biome warnings (non eseguito `pnpm lint` perché out of scope per un test-only fix).

## Verifiche (plan-level checks)

- [x] `git log --oneline -2` mostra due commit Italian (fix + docs).
- [x] `git diff HEAD~2 -- tests/e2e/all-products-purchase.spec.ts` mostra solo: import update (1 LOC), helper insertion (~33 LOC), call-site replacement (7 LOC → 1 LOC).
- [x] `git diff HEAD~2 -- tests/e2e/helpers/variants.ts tests/e2e/smoke-purchase.spec.ts src/` è vuoto (zero modifiche fuori scope).
- [x] SUMMARY.md esiste con tutte e 6 le sezioni richieste (diff, targeted, parametric before/after, hard-fail residuo, next-quick, out of scope).
- [x] Targeted denise+maria 3/3 PASS deterministico (workers=1, retries=0).
- [x] Smoke test (Isabella) non regredito: passato in full parametric (`compra isabella ✓ 25.6s`).
- [x] `pnpm exec tsc --noEmit` pulito sul file modificato (errori esistenti in `prodotti.$slug.tsx` sono pre-existing, non correlati).

## Self-Check: PASSED

- File `tests/e2e/all-products-purchase.spec.ts` modificato (+35, -8). Verificato.
- Helper `clickAddToCartAndWaitPost` presente al module scope. Verificato (line 80).
- Call-site singolo `await clickAddToCartAndWaitPost(page);`. Verificato (line 124).
- Predicato senza `res.ok()`. Verificato (line 85).
- Commit `66470c2` esistente. Verificato.
- `helpers/variants.ts` e `smoke-purchase.spec.ts` intoccati. Verificato.
- Targeted run 3/3 PASS catturato. Verificato.
- Full parametric 118/119 PASS catturato. Verificato.
