---
quick_id: 260820-h2j
date: 2026-08-20
mode: quick
status: planned
---

# Quick 260820-h2j — Banner annuncio chiusura estiva in header

## Goal

Striscia di annuncio sopra a tutto l'header (desktop + mobile) con il testo
"Chiusura estiva — gli ordini effettuati ora verranno evasi a partire dal 1° settembre".
Non chiudibile. Si auto-disattiva dal 1 settembre 2026 (Europe/Rome).

## Decisioni bloccate (dall'utente)

- Copy esatto: `Chiusura estiva — gli ordini effettuati ora verranno evasi a partire dal 1° settembre`
- Nessuna X di chiusura, sempre visibile finché attivo
- Auto-scadenza: dal 2026-09-01 (ora di Roma) il banner non viene più renderizzato

## Vincoli tecnici

- `--navbar-height` è anche l'altezza della barra mobile (`MegaMenu.tsx:590`) e del blocco
  desktop (`:653`) → NON modificare quel token.
- L'offset del contenuto (`__root.tsx:108`) e le altezze hero
  (`HeroSection.tsx:71`, `MobileHeroSection.tsx:88`) devono compensare il banner
  SOLO quando è attivo, altrimenti dopo il 1 settembre resterebbe una fascia vuota.
- Header admin già escluso da `isAdmin` in `__root.tsx`.
- Nessun hex hardcoded: usare i design token.

## Tasks

### Task 1 — Flag + token altezza
- files: `src/lib/announcement.ts` (nuovo), `src/styles/design-tokens.css`
- action: helper `isAnnouncementActive(now?)` che confronta la data corrente con
  `2026-09-01T00:00:00+02:00` e costante `ANNOUNCEMENT_TEXT`. Aggiungere token
  `--announcement-height: 2.75rem` e `--announcement-height-md: 2.25rem` in Layout.
- verify: `pnpm typecheck` sui file toccati
- done: helper esporta flag + testo, token presenti

### Task 2 — Componente AnnouncementBar dentro l'header
- files: `src/components/shared/AnnouncementBar.tsx` (nuovo),
  `src/components/shared/index.ts`, `src/components/shared/MegaMenu.tsx`
- action: componente che ritorna `null` se non attivo, altrimenti striscia full-width
  `bg-[var(--color-primary-dark)]` + testo `--color-primary-foreground`, altezza dai token,
  `role="status"`. Renderizzato come primo figlio di `<header>` (`MegaMenu.tsx:373`),
  sopra microbar desktop e barra mobile.
- verify: ispezione visiva / typecheck
- done: banner visibile su desktop e mobile sopra tutto

### Task 3 — Compensazione offset contenuto
- files: `src/routes/__root.tsx`, `src/components/sections/HeroSection.tsx`,
  `src/components/sections/MobileHeroSection.tsx`
- action: quando il banner è attivo sommare `--announcement-height(-md)` al padding-top
  del `<main>` e sottrarlo dalle altezze hero; comportamento invariato quando è scaduto.
- verify: `pnpm typecheck`; nessun gap sotto l'header dopo la scadenza
- done: nessuna sovrapposizione né spazio vuoto in entrambi gli stati

## Must haves

- truths: il banner scompare da solo il 2026-09-01 senza lasciare spazio vuoto
- artifacts: `AnnouncementBar.tsx`, `lib/announcement.ts`, token `--announcement-height`
- key_links: `src/components/shared/MegaMenu.tsx:373`, `src/routes/__root.tsx:108`
