---
phase: quick-260531-hl4
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/validators/admin.ts
  - src/lib/admin/admin-discounts.server.ts
  - src/lib/admin-functions.ts
  - src/routes/admin.sconti.tsx
  - src/routes/admin.tsx
autonomous: true
requirements: [DISCOUNT-ADMIN-01]
must_haves:
  truths:
    - "Admin can open /admin/sconti from the sidebar (voce 'Codici sconto')"
    - "Admin can create a percentage discount code with code, value%, startsAt, expiresAt, optional maxUses, optional minOrder"
    - "Created codes are persisted with type='percentage' and are immediately usable at checkout (compatible with orders.server.ts discount lookup)"
    - "Admin sees a table of all existing codes with usedCount/maxUses usage display"
    - "Admin can toggle isActive on/off per code without page reload"
  artifacts:
    - path: "src/lib/validators/admin.ts"
      provides: "createDiscountCodeSchema + toggleDiscountCodeSchema Zod schemas"
      contains: "createDiscountCodeSchema"
    - path: "src/lib/admin/admin-discounts.server.ts"
      provides: "listDiscountCodes, createDiscountCode, toggleDiscountCode server functions"
      min_lines: 60
    - path: "src/lib/admin-functions.ts"
      provides: "$listDiscountCodes, $createDiscountCode, $toggleDiscountCode createServerFn wrappers (auth-gated)"
      contains: "$createDiscountCode"
    - path: "src/routes/admin.sconti.tsx"
      provides: "Admin page: creation form + list table + toggle"
      contains: "createFileRoute(\"/admin/sconti\")"
    - path: "src/routes/admin.tsx"
      provides: "NAV_ITEMS entry 'Codici sconto' → /admin/sconti"
      contains: "/admin/sconti"
  key_links:
    - from: "src/routes/admin.sconti.tsx"
      to: "src/lib/admin-functions.ts"
      via: "import { $listDiscountCodes, $createDiscountCode, $toggleDiscountCode }"
      pattern: "\\$createDiscountCode"
    - from: "src/lib/admin-functions.ts"
      to: "src/lib/admin/admin-discounts.server.ts"
      via: "import + requireAdmin() gate"
      pattern: "admin-discounts.server"
    - from: "src/lib/admin/admin-discounts.server.ts"
      to: "prisma.discountCode"
      via: "create / findMany / update"
      pattern: "prisma\\.discountCode\\.(create|findMany|update)"
---

<objective>
Aggiungi una pagina admin `/admin/sconti` per gestire i codici sconto in PERCENTUALE.
La pagina permette di (1) creare un nuovo codice sconto e (2) elencare tutti i codici
esistenti con visualizzazione utilizzi e toggle attiva/disattiva.

Il modello Prisma `DiscountCode` ESISTE GIÀ (schema.prisma ~L505-524) — NON va creata
migration né modificato lo schema. Si costruiscono solo: validators, server functions,
route page e la voce di menu in NAV_ITEMS.

Il campo `type` è sempre fisso a `"percentage"` (la UI offre solo sconti percentuali).
I codici creati devono essere compatibili con la logica di applicazione sconto già
presente in `orders.server.ts` (lookup per `code` uppercase + `isActive` + finestra
`startsAt`/`expiresAt` + `minOrder` + `maxUses`/`usedCount`).

Purpose: Dare all'admin il controllo sui codici promozionali senza toccare il DB a mano.
Output: 1 file nuovo server + 1 file route nuovo + 3 file modificati (validators, wrapper, nav).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@prisma/schema.prisma

<interfaces>
<!-- Tutto estratto dal codebase. L'executor usa questi contratti direttamente. -->

Prisma model DiscountCode (schema.prisma L507-524) — GIÀ ESISTENTE, non modificare:
```prisma
model DiscountCode {
  id        String   @id @default(cuid())
  code      String   @unique
  type      String   @default("percentage") // "percentage" | "fixed"
  value     Decimal  @db.Decimal(10, 2)
  minOrder  Decimal? @db.Decimal(10, 2)
  maxUses   Int?
  usedCount Int      @default(0)
  startsAt  DateTime
  expiresAt DateTime
  isActive  Boolean  @default(true)
  createdAt DateTime @default(now())
}
```

Discount application contract (src/lib/orders.server.ts L180-202) — i codici creati DEVONO
essere compatibili con questa query:
```ts
const code = await prisma.discountCode.findFirst({
  where: {
    code: input.discountCode.toUpperCase(), // ⇒ salvare il code in UPPERCASE
    isActive: true,
    startsAt: { lte: new Date() },
    expiresAt: { gte: new Date() },
  },
});
// poi: if (code.minOrder && subtotal < Number(code.minOrder)) → errore
//      if (code.maxUses && code.usedCount >= code.maxUses) → errore
//      discountAmount = subtotal * Number(code.value) / 100   (type === "percentage")
```
⇒ Implicazioni per la creazione admin: salvare SEMPRE `code.toUpperCase()`,
`value` = percentuale (es. 10 = 10%), `startsAt`/`expiresAt` come DateTime,
`minOrder`/`maxUses` opzionali (null se vuoti), `type: "percentage"` fisso, `usedCount: 0`.

requireAdmin pattern (src/lib/admin-functions.ts L57-63):
```ts
async function requireAdmin() {
  const user = await $getUser();
  if (!user || user.role !== "admin") {
    throw redirect({ to: "/auth/login" });
  }
  return user;
}
```

createServerFn wrapper pattern (src/lib/admin-functions.ts L236-241):
```ts
export const $updateShippingConfig = createServerFn({ method: "POST" })
  .inputValidator((data: UpdateShippingConfigInput) => data)
  .handler(async ({ data }) => {
    const user = await requireAdmin();
    return updateShippingConfig(data, user.id);
  });
```

Server service pattern (src/lib/admin/shipping-config.server.ts):
- plain async exported functions, `import { prisma } from "~/lib/db.server"`,
  `import { createLogger } from "~/lib/logger.server"`, schema.parse(input) lato server,
  Decimal serializzato via `Number(row.x)` quando restituito al client.

Route + form pattern (src/routes/admin.spedizione.tsx):
- `createFileRoute("/admin/spedizione")` con `beforeLoad` che fa il fetch iniziale e lo
  mette in route context; `useForm` (@tanstack/react-form) + `zodValidator()` adapter +
  `validators: { onSubmit: <schema> }`; `// @ts-expect-error` sopra `useForm<...>` per il
  generic strict v1.28; toast (sonner) su success/error; input native stilizzati con design
  tokens (`var(--color-primary)` ecc.), label/helper classi condivise; toggle custom
  `<button role="switch" aria-checked>` (NON serve installare shadcn/Radix Switch).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Validators + server layer (admin-discounts.server.ts)</name>
  <files>src/lib/validators/admin.ts, src/lib/admin/admin-discounts.server.ts</files>
  <action>
**A) src/lib/validators/admin.ts** — aggiungi in fondo al file una nuova sezione
`// ─── Discount Codes ───` con DUE schema (NON inline nelle route, per CLAUDE.md):

```ts
export const createDiscountCodeSchema = z
  .object({
    code: z
      .string()
      .trim()
      .min(2, "Il codice deve avere almeno 2 caratteri")
      .max(50, "Il codice è troppo lungo")
      .regex(/^[A-Za-z0-9_-]+$/, "Solo lettere, numeri, trattino e underscore")
      .transform((c) => c.toUpperCase()),
    value: z.coerce
      .number({ message: "Inserisci una percentuale valida" })
      .positive("La percentuale deve essere maggiore di 0")
      .max(100, "La percentuale non può superare 100"),
    startsAt: z.coerce.date({ message: "Data inizio non valida" }),
    expiresAt: z.coerce.date({ message: "Data scadenza non valida" }),
    minOrder: z.coerce
      .number({ message: "Ordine minimo non valido" })
      .min(0, "L'ordine minimo non può essere negativo")
      .max(99999.99)
      .optional(),
    maxUses: z.coerce
      .number({ message: "Limite utilizzi non valido" })
      .int("Deve essere un numero intero")
      .min(1, "Il limite deve essere almeno 1")
      .max(1000000)
      .optional(),
  })
  .refine((d) => d.expiresAt > d.startsAt, {
    message: "La data di scadenza deve essere successiva alla data di inizio",
    path: ["expiresAt"],
  });

export type CreateDiscountCodeInput = z.infer<typeof createDiscountCodeSchema>;

export const toggleDiscountCodeSchema = z.object({
  id: z.string().cuid(),
  isActive: z.boolean(),
});

export type ToggleDiscountCodeInput = z.infer<typeof toggleDiscountCodeSchema>;
```
NOTA: il `type` field NON è nello schema — è hardcoded a "percentage" lato server.
NOTA: `minOrder`/`maxUses` sono `.optional()`; il form invierà `undefined` quando vuoti
(vedi Task 2: convertire stringa vuota → undefined prima del submit).

**B) src/lib/admin/admin-discounts.server.ts** — NUOVO file server-only. Segui ESATTAMENTE
il pattern di `shipping-config.server.ts` (header doc IT, `import { prisma } from "~/lib/db.server"`,
`import { createLogger } from "~/lib/logger.server"`, parse lato server, Decimal→Number in output).

Definisci un tipo di output serializzabile (no Decimal/Prisma types verso il client):
```ts
export interface DiscountCodeListItem {
  id: string;
  code: string;
  value: number;        // percentuale
  minOrder: number | null;
  maxUses: number | null;
  usedCount: number;
  startsAt: string;     // ISO string (serializzabile via createServerFn)
  expiresAt: string;    // ISO string
  isActive: boolean;
  createdAt: string;    // ISO string
}
```

Tre funzioni esportate:

1) `listDiscountCodes(): Promise<DiscountCodeListItem[]>`
   - `prisma.discountCode.findMany({ orderBy: { createdAt: "desc" } })`
   - mappa ogni row → DiscountCodeListItem: `value: Number(row.value)`,
     `minOrder: row.minOrder === null ? null : Number(row.minOrder)`, idem maxUses
     (`row.maxUses ?? null`), date `.toISOString()`.

2) `createDiscountCode(input: CreateDiscountCodeInput, userId: string): Promise<DiscountCodeListItem>`
   - `const parsed = createDiscountCodeSchema.parse(input)` (code già uppercased dal transform).
   - Pre-check univocità: `const existing = await prisma.discountCode.findUnique({ where: { code: parsed.code } });`
     `if (existing) throw new Error("Esiste già un codice con questo nome");`
   - `prisma.discountCode.create({ data: { code: parsed.code, type: "percentage",
     value: parsed.value, minOrder: parsed.minOrder ?? null, maxUses: parsed.maxUses ?? null,
     startsAt: parsed.startsAt, expiresAt: parsed.expiresAt } })`
     (usedCount=0 e isActive=true vengono dai default schema).
   - `log.info("discount_code.created", { userId, code: parsed.code, value: parsed.value })`
   - ritorna la row mappata a DiscountCodeListItem (riusa un helper `toListItem(row)` interno).

3) `toggleDiscountCode(input: ToggleDiscountCodeInput, userId: string): Promise<DiscountCodeListItem>`
   - `const parsed = toggleDiscountCodeSchema.parse(input)`
   - `prisma.discountCode.update({ where: { id: parsed.id }, data: { isActive: parsed.isActive } })`
   - `log.info("discount_code.toggled", { userId, id: parsed.id, isActive: parsed.isActive })`
   - ritorna la row mappata a DiscountCodeListItem.

Importa i tipi/schema da `~/lib/validators/admin`. Zero `any`, error handling con
`catch (e: unknown)` se necessario (qui usiamo throw esplicito su univocità).
Estrai un helper interno `toListItem(row): DiscountCodeListItem` per DRY tra le 3 funzioni.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "admin-discounts\.server\.ts|validators/admin\.ts" || echo "OK: nessun errore typecheck sui 2 file"</automated>
  </verify>
  <done>
createDiscountCodeSchema + toggleDiscountCodeSchema esistono in validators/admin.ts;
admin-discounts.server.ts esporta listDiscountCodes/createDiscountCode/toggleDiscountCode
+ DiscountCodeListItem; type hardcoded "percentage"; code salvato uppercase; Decimal→Number,
date→ISO string; zero errori typecheck NUOVI sui 2 file (baseline pre-esistente invariata).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Server-fn wrappers + route page /admin/sconti + voce NAV</name>
  <files>src/lib/admin-functions.ts, src/routes/admin.sconti.tsx, src/routes/admin.tsx</files>
  <action>
**A) src/lib/admin-functions.ts** — aggiungi import e 3 wrapper createServerFn auth-gated.

Import (dopo il blocco admin-carts, ~L50):
```ts
import {
  listDiscountCodes,
  createDiscountCode,
  toggleDiscountCode,
  type DiscountCodeListItem,
} from "./admin/admin-discounts.server";
import type {
  CreateDiscountCodeInput,
  ToggleDiscountCodeInput,
} from "./validators/admin";
```
Aggiungi `DiscountCodeListItem` al re-export `export type { ... }` (L53).

In fondo al file, nuova sezione `// ─── Discount Codes ───`:
```ts
export const $listDiscountCodes = createServerFn({ method: "GET" }).handler(async () => {
  await requireAdmin();
  return listDiscountCodes() satisfies Promise<DiscountCodeListItem[]>;
});

export const $createDiscountCode = createServerFn({ method: "POST" })
  .inputValidator((data: CreateDiscountCodeInput) => data)
  .handler(async ({ data }) => {
    const user = await requireAdmin();
    return createDiscountCode(data, user.id) satisfies Promise<DiscountCodeListItem>;
  });

export const $toggleDiscountCode = createServerFn({ method: "POST" })
  .inputValidator((data: ToggleDiscountCodeInput) => data)
  .handler(async ({ data }) => {
    const user = await requireAdmin();
    return toggleDiscountCode(data, user.id) satisfies Promise<DiscountCodeListItem>;
  });
```

**B) src/routes/admin.sconti.tsx** — NUOVO file. Mirror del pattern admin.spedizione.tsx
(createFileRoute + beforeLoad fetch iniziale in route context + useForm + zodValidator +
toast + input native stilizzati con design tokens + toggle custom role="switch").
MAX 200 LOC: se sfori, estrai la riga-tabella in un sub-componente locale `DiscountRow`
nello stesso file. Logica di business sta TUTTA nel server layer (route < 150 LOC di logica).

Struttura:
- `createFileRoute("/admin/sconti")` con `beforeLoad: async () => ({ initialCodes: await $listDiscountCodes() })`.
- Component `AdminDiscountsPage`:
  - state `codes: DiscountCodeListItem[]` inizializzato da `Route.useRouteContext().initialCodes`.
  - state `creating: boolean` e `togglingId: string | null`.
  - **Form di creazione** (TanStack Form, come spedizione, con `// @ts-expect-error` sopra useForm):
    defaultValues: `{ code: "", value: 10, startsAt: <oggi yyyy-mm-dd>, expiresAt: <oggi+30 yyyy-mm-dd>, minOrder: "", maxUses: "" }`.
    Campi (tutti input native stilizzati + label/helper, NO raw HTML non stilizzato):
      - `code` (text, uppercase visivo via `style`/onChange .toUpperCase() opzionale)
      - `value` (number, step 1, min 1, max 100, suffisso "%")
      - `startsAt` (input type="date")
      - `expiresAt` (input type="date")
      - `minOrder` (number, step 0.01, min 0, opzionale — placeholder "Nessuno")
      - `maxUses` (number, step 1, min 1, opzionale — placeholder "Illimitato")
    onSubmit: costruisci il payload convertendo le stringhe vuote opzionali in undefined:
      ```ts
      const payload = {
        code: value.code,
        value: Number(value.value),
        startsAt: new Date(value.startsAt),
        expiresAt: new Date(value.expiresAt),
        minOrder: value.minOrder === "" ? undefined : Number(value.minOrder),
        maxUses: value.maxUses === "" ? undefined : Number(value.maxUses),
      };
      ```
      then `const created = await $createDiscountCode({ data: payload });`
      `setCodes((prev) => [created, ...prev]); form.reset(); toast.success("Codice sconto creato");`
      su errore `toast.error(err instanceof Error ? err.message : "Errore creazione codice")`.
      NB: il `type` NON è nel form — è impostato server-side.
  - **Tabella lista** sotto il form (card bianca, `overflow-x-auto`): colonne
    Codice (monospace) | Sconto (`{value}%`) | Validità (`startsAt → expiresAt`, formato IT breve) |
    Ordine min (`minOrder !== null ? €{minOrder} : "—"`) | Utilizzi (`{usedCount}/{maxUses ?? "∞"}`) |
    Stato (toggle). Stato vuoto: riga "Nessun codice sconto creato".
  - **Toggle isActive** per riga: `<button role="switch" aria-checked={c.isActive}>` (stesso stile
    di spedizione) → onClick chiama:
      ```ts
      setTogglingId(c.id);
      try {
        const updated = await $toggleDiscountCode({ data: { id: c.id, isActive: !c.isActive } });
        setCodes((prev) => prev.map((x) => (x.id === updated.id ? updated : x)));
      } catch (e) { toast.error(e instanceof Error ? e.message : "Errore aggiornamento"); }
      finally { setTogglingId(null); }
      ```
  - Header pagina con icona `Ticket` (o `Percent`/`Tag`) da lucide-react + eyebrow "Codici sconto"
    (stesso layout header di spedizione).
  - Helper inline `formatDateIT(iso: string)` → `new Date(iso).toLocaleDateString("it-IT")`.
  - Zero hex hardcoded (solo `var(--color-*)`), zero `any`.

**C) src/routes/admin.tsx** — aggiungi la voce di menu:
  - Import icona: aggiungi `Ticket` (o `Percent`) all'import lucide-react esistente (L3).
  - In `NAV_ITEMS` (L23-34), aggiungi DOPO "Spedizione" (L30):
    `{ label: "Codici sconto", href: "/admin/sconti", icon: Ticket, matchPath: "/admin/sconti" as const },`
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json 2>&1 | grep -E "admin\.sconti\.tsx|admin-functions\.ts|admin\.tsx" || echo "OK: nessun errore typecheck sui 3 file (routeTree.gen.ts può richiedere rigenerazione build-time)"</automated>
  </verify>
  <done>
$listDiscountCodes/$createDiscountCode/$toggleDiscountCode esistono in admin-functions.ts
(auth-gated via requireAdmin); /admin/sconti renderizza form di creazione + tabella codici
con utilizzi (usedCount/maxUses) + toggle isActive funzionante senza reload; voce "Codici sconto"
presente in NAV_ITEMS dopo "Spedizione" → /admin/sconti; nessun errore typecheck NUOVO sui 3 file
(la rigenerazione di routeTree.gen.ts gitignored è a carico del build CI Railway).
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` → zero errori NUOVI sui 5 file toccati (baseline pre-esistente ~26 invariata;
  errori a cascata su routeTree.gen.ts/@prisma se worktree fresco sono artefatti ambiente, non regressioni).
- grep `prisma\.discountCode\.(create|findMany|update)` in admin-discounts.server.ts → 3 match.
- grep `type: "percentage"` in admin-discounts.server.ts → 1 match (type hardcoded).
- grep `toUpperCase` nel transform di createDiscountCodeSchema → code salvato uppercase (compatibile orders.server.ts).
- grep `/admin/sconti` in admin.tsx → voce NAV presente.
- Zero `any` / zero `as any` nei file nuovi (grep). Zero hex hardcoded nella route (solo var(--color-*)).
- Smoke (deferito a deploy/dev, a carico utente): login admin → /admin/sconti → crea codice
  "ESTATE10" 10% startsAt oggi expiresAt +30gg → compare in tabella usedCount 0/∞ → toggle off/on →
  al checkout applicare "ESTATE10" su un carrello → sconto del 10% applicato.
</verification>

<success_criteria>
- Pagina /admin/sconti raggiungibile dalla sidebar (voce "Codici sconto").
- Form crea codici sconto percentuale persistiti con type="percentage", code uppercase, usedCount 0.
- Codici creati immediatamente utilizzabili al checkout (compatibili con la query di orders.server.ts).
- Tabella elenca tutti i codici con utilizzi usedCount/maxUses e toggle isActive on/off senza reload.
- Convenzioni CLAUDE.md rispettate: zero any, Zod in lib/validators/, server boundary (.server.ts
  via createServerFn), @tanstack/react-form + zod adapter, design tokens, route < 150 LOC logica,
  componente < 200 LOC. NESSUNA migration / modifica schema (modello DiscountCode già esistente).
</success_criteria>

<output>
After completion, create `.planning/quick/260531-hl4-aggiungi-gestione-codici-sconto-nel-back/260531-hl4-SUMMARY.md`
</output>
