---
phase: quick-260518-o48
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - src/lib/email.server.ts
autonomous: true
requirements: [SMTP-01]

must_haves:
  truths:
    - "Se SMTP_HOST è configurato, sendEmail() invia l'email via SMTP nodemailer"
    - "Se SMTP_HOST NON è configurato, sendEmail() usa il percorso Resend HTTP esistente (invariato)"
    - "Se né SMTP_HOST né RESEND_API_KEY sono configurati, sendEmail() logga errore e ritorna { ok: false }"
    - "L'interfaccia pubblica SendEmailOptions / SendEmailResult resta identica"
    - "Porta SMTP 465 usa secure: true, ogni altra porta usa STARTTLS (secure: false)"
  artifacts:
    - path: "src/lib/email.server.ts"
      provides: "sendEmail() con trasporto SMTP + fallback Resend"
      contains: "nodemailer"
    - path: "package.json"
      provides: "dipendenza nodemailer + @types/nodemailer"
      contains: "nodemailer"
  key_links:
    - from: "src/lib/email.server.ts"
      to: "nodemailer"
      via: "createTransport con host/port/auth da process.env"
      pattern: "nodemailer\\.createTransport"
    - from: "src/lib/email.server.ts"
      to: "process.env.SMTP_HOST"
      via: "branch condizionale che sceglie SMTP vs Resend"
      pattern: "process\\.env\\.SMTP_HOST"
---

<objective>
Aggiungere supporto invio email via SMTP (nodemailer) a `src/lib/email.server.ts`, mantenendo il percorso Resend HTTP esistente come fallback.

Purpose: Poter inviare email transazionali (conferme ordine, reset password) tramite l'SMTP di VHosting invece di Resend, senza modificare i call-site esistenti.
Output: `email.server.ts` aggiornato con trasporto SMTP condizionale + dipendenza `nodemailer` installata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md

<interfaces>
<!-- Contratti pubblici da PRESERVARE invariati. Estratti da src/lib/email.server.ts -->

Stato attuale di src/lib/email.server.ts:
```typescript
import { createLogger } from "~/lib/logger.server";

const log = createLogger("email");

interface SendEmailOptions {
  to: string;
  subject: string;
  html: string;
  replyTo?: string;
}

interface SendEmailResult {
  ok: boolean;
  id?: string;
}

export async function sendEmail({ to, subject, html, replyTo }: SendEmailOptions): Promise<SendEmailResult> {
  // percorso Resend: fetch a https://api.resend.com/emails
  // from: process.env.EMAIL_FROM ?? "noreply@calzoleriaprevenzano.it"
}
```

Logger (src/lib/logger.server.ts) — interfaccia disponibile:
```typescript
const log = createLogger("email");
log.error(message: string, data?: unknown): void;  // data tipizzato unknown
```

Vincoli da CLAUDE.md rilevanti:
- Zero `any` policy (non negoziabile): usare `unknown` + type guard, mai `as any`.
- `catch (e: unknown)` + narrowing `instanceof Error`, mai esporre stack trace.
- File `.server.ts` = server-only, mai importato da client.
- Biome: 2-space indent, double quotes, semicolons always.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Installare dipendenza nodemailer</name>
  <files>package.json, pnpm-lock.yaml</files>
  <action>
    Installare nodemailer e i relativi tipi con pnpm:

    ```
    pnpm add nodemailer
    pnpm add -D @types/nodemailer
    ```

    `nodemailer` va in `dependencies` (usato a runtime server-side), `@types/nodemailer`
    in `devDependencies` (solo type-checking). Verificare che `pnpm-lock.yaml` sia
    aggiornato dopo l'installazione.

    Non modificare altre dipendenze. Non eseguire `pnpm update`.
  </action>
  <verify>
    <automated>node -e "require('nodemailer'); console.log('nodemailer OK')" && grep -q '"nodemailer"' package.json && grep -q '"@types/nodemailer"' package.json && echo "package.json OK"</automated>
  </verify>
  <done>`nodemailer` presente in dependencies di package.json, `@types/nodemailer` in devDependencies, `pnpm-lock.yaml` aggiornato, `require('nodemailer')` non lancia errori.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aggiungere trasporto SMTP con fallback Resend in email.server.ts</name>
  <files>src/lib/email.server.ts</files>
  <behavior>
    - Branch SMTP: con `SMTP_HOST` valorizzato, `sendEmail()` chiama `nodemailer.createTransport(...).sendMail(...)` e ritorna `{ ok: true, id: <messageId> }` in caso di successo.
    - Branch Resend invariato: senza `SMTP_HOST` ma con `RESEND_API_KEY`, `sendEmail()` esegue il `fetch` esistente verso `https://api.resend.com/emails`.
    - Nessuna config: senza `SMTP_HOST` e senza `RESEND_API_KEY`, `sendEmail()` logga `log.error` e ritorna `{ ok: false }`.
    - Porta SMTP: con `SMTP_PORT === "465"` → `secure: true`; ogni altra porta (default `587`) → `secure: false` (STARTTLS).
    - Errore SMTP: `catch (error: unknown)` con narrowing `instanceof Error`, log strutturato senza stack trace, ritorna `{ ok: false }`.
  </behavior>
  <action>
    Modificare `src/lib/email.server.ts` per supportare il trasporto SMTP, MANTENENDO
    invariate le interfacce `SendEmailOptions` e `SendEmailResult` e il percorso Resend.

    1. Aggiungere import in cima al file:
       `import nodemailer from "nodemailer";`
       (mantenere l'import esistente di `createLogger`).

    2. Estrarre il percorso Resend attuale (corpo di `sendEmail`) in una funzione
       privata `sendViaResend({ to, subject, html, replyTo }: SendEmailOptions): Promise<SendEmailResult>`.
       Il codice resta IDENTICO a quello attuale (fetch, controllo `response.ok`,
       parsing `{ id: string }`, catch con `error instanceof Error`).

    3. Aggiungere una funzione privata `sendViaSmtp({ to, subject, html, replyTo }: SendEmailOptions): Promise<SendEmailResult>`:
       - Leggere `process.env.SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS`.
       - Calcolare la porta: `const port = Number(process.env.SMTP_PORT ?? "587");`
       - `const secure = port === 465;`
       - Creare il transport:
         ```typescript
         const transporter = nodemailer.createTransport({
           host,
           port,
           secure,
           auth: user ? { user, pass } : undefined,
         });
         ```
         dove `host` è `process.env.SMTP_HOST` (già verificato non vuoto dal chiamante).
       - Inviare:
         ```typescript
         const info = await transporter.sendMail({
           from: process.env.EMAIL_FROM ?? "noreply@calzoleriaprevenzano.it",
           to,
           subject,
           html,
           replyTo,
         });
         return { ok: true, id: info.messageId };
         ```
       - Avvolgere in `try/catch (error: unknown)`: in caso di errore
         `log.error("SMTP send error", { message: error instanceof Error ? error.message : "unknown" })`
         e ritornare `{ ok: false }`. NON esporre lo stack trace.

    4. Riscrivere `sendEmail` come dispatcher:
       ```typescript
       export async function sendEmail(options: SendEmailOptions): Promise<SendEmailResult> {
         if (process.env.SMTP_HOST) {
           return sendViaSmtp(options);
         }
         if (process.env.RESEND_API_KEY) {
           return sendViaResend(options);
         }
         log.error("No email transport configured (SMTP_HOST / RESEND_API_KEY) — email not sent", {
           to: options.to,
           subject: options.subject,
         });
         return { ok: false };
       }
       ```

    5. Aggiornare il commento JSDoc in cima al file: indicare che il servizio usa
       SMTP (nodemailer) se `SMTP_HOST` è configurato, altrimenti fallback su Resend HTTP API.

    Vincoli: zero `any` (usare `unknown` + narrowing), nessuno stack trace nei log,
    file server-only, conservare lo stile Biome (double quotes, semicolons). Tipizzare
    i parametri del transport in modo che `tsc` non lamenti tipi impliciti — l'oggetto
    passato a `createTransport`/`sendMail` è inferito dai tipi `@types/nodemailer`.
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm exec biome check src/lib/email.server.ts</automated>
  </verify>
  <done>`pnpm typecheck` passa senza errori; `biome check` su email.server.ts pulito; `SendEmailOptions`/`SendEmailResult` invariate; `sendEmail` instrada su SMTP quando `SMTP_HOST` è settato, su Resend in caso contrario, e ritorna `{ ok: false }` con log se nessun trasporto è configurato.</done>
</task>

</tasks>

<verification>
- `pnpm typecheck` passa (zero errori TypeScript, zero `any`).
- `pnpm lint` (o `biome check src/lib/email.server.ts`) pulito.
- L'export `sendEmail` ha la stessa firma di prima: `(SendEmailOptions) => Promise<SendEmailResult>`.
- Nessun call-site esistente di `sendEmail` richiede modifiche (interfaccia invariata).
- Smoke check logica: con `SMTP_HOST` settato instrada su nodemailer; senza, instrada su Resend; senza entrambi logga e ritorna `{ ok: false }`.
</verification>

<success_criteria>
- `nodemailer` + `@types/nodemailer` installati con pnpm e presenti in package.json.
- `email.server.ts` invia via SMTP quando `SMTP_HOST` è configurato, con `secure: true` su porta 465 e STARTTLS altrimenti.
- Fallback Resend HTTP invariato quando `SMTP_HOST` non è configurato.
- Interfacce pubbliche, logging strutturato (`createLogger("email")`) ed error handling tipizzato (`catch unknown`, niente stack trace) preservati.
- File resta server-only.
</success_criteria>

<output>
After completion, create `.planning/quick/260518-o48-supporto-invio-email-via-smtp-nodemailer/260518-o48-SUMMARY.md`
</output>
