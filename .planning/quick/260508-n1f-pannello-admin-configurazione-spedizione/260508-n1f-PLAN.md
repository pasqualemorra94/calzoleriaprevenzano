---
phase: quick/260508-n1f
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - src/lib/validators/admin.ts
  - src/lib/admin/shipping-config.server.ts
  - src/lib/admin-functions.ts
  - src/routes/admin.tsx
  - src/routes/admin.spedizione.tsx
  - src/routes/checkout.tsx
  - src/routes/carrello.tsx
  - src/lib/orders.server.ts
  - src/components/checkout/OrderSummary.tsx
autonomous: true
requirements:
  - SHIP-01  # Modello DB ShippingConfig singleton (cost, freeThreshold, enabled) + migrazione
  - SHIP-02  # Server module shipping-config.server.ts con get/update + helper computeShippingCost (cache TTL)
  - SHIP-03  # Pannello admin /admin/spedizione (form TanStack + zod, link sidebar, auth guard)
  - SHIP-04  # Refactor 3 punti hardcoded (checkout.tsx, carrello.tsx, orders.server.ts) + OrderSummary copy quando enabled=false

must_haves:
  truths:
    - "L'admin con role=admin vede /admin/spedizione nella sidebar e accede al form senza errori (non-admin → redirect /auth/login)"
    - "L'admin può modificare cost/freeThreshold/enabled e al salvataggio i valori vengono persistiti in DB (verificabile via prisma studio o reload pagina)"
    - "Carrello e Checkout calcolano la spedizione leggendo da DB (non più da costanti hardcoded), valori coerenti tra le due pagine"
    - "Quando enabled=false → carrello/checkout/OrderSummary mostrano spedizione 0€ e copy 'Spedizione gratuita' (nessun nudge 'aggiungi €X')"
    - "createOrder (POST /api/checkout) calcola shippingCost server-side leggendo dal DB (non hardcoded a 7.9/99) e l'ordine creato ha shippingCost coerente con la config corrente"
    - "Validazione zod attiva: cost < 0 o freeThreshold < 0 → form errore italiano, no submit"
    - "I valori di default seedati al primo accesso sono cost=7.90 / freeThreshold=99.00 / enabled=true (preservano comportamento attuale)"
  artifacts:
    - path: "prisma/schema.prisma"
      provides: "Modello ShippingConfig singleton con id default 'default'"
      contains: "model ShippingConfig"
    - path: "src/lib/admin/shipping-config.server.ts"
      provides: "getShippingConfig, updateShippingConfig, computeShippingCost + cache TTL"
      exports: ["getShippingConfig", "updateShippingConfig", "computeShippingCost", "invalidateShippingConfigCache"]
    - path: "src/lib/validators/admin.ts"
      provides: "updateShippingConfigSchema (cost>=0, freeThreshold>=0, enabled boolean)"
      contains: "updateShippingConfigSchema"
    - path: "src/routes/admin.spedizione.tsx"
      provides: "Pannello admin con form TanStack + zod adapter + toast feedback italiano"
      min_lines: 80
    - path: "src/routes/admin.tsx"
      provides: "Voce 'Spedizione' aggiunta a NAV_ITEMS"
      contains: "/admin/spedizione"
    - path: "src/lib/admin-functions.ts"
      provides: "$getShippingConfig, $updateShippingConfig server functions con auth guard"
      exports: ["$getShippingConfig", "$updateShippingConfig"]
    - path: "src/routes/checkout.tsx"
      provides: "Loader fetch config + uso computeShippingCost + pass shippingEnabled a OrderSummary"
      min_lines: 250
    - path: "src/routes/carrello.tsx"
      provides: "Fetch config + uso computeShippingCost + pass shippingEnabled a OrderSummary"
      min_lines: 180
    - path: "src/lib/orders.server.ts"
      provides: "createOrder usa computeShippingCost(subtotal, await getShippingConfig())"
      contains: "computeShippingCost"
    - path: "src/components/checkout/OrderSummary.tsx"
      provides: "Prop shippingEnabled?: boolean per sopprimere nudge e mostrare 'Spedizione gratuita' quando false"
      contains: "shippingEnabled"
  key_links:
    - from: "src/routes/admin.spedizione.tsx"
      to: "src/lib/admin/shipping-config.server.ts"
      via: "$getShippingConfig + $updateShippingConfig in src/lib/admin-functions.ts"
      pattern: "\\$getShippingConfig|\\$updateShippingConfig"
    - from: "src/routes/checkout.tsx"
      to: "src/lib/admin/shipping-config.server.ts"
      via: "fetch config via server fn / API + computeShippingCost helper"
      pattern: "computeShippingCost|shippingConfig"
    - from: "src/routes/carrello.tsx"
      to: "src/lib/admin/shipping-config.server.ts"
      via: "fetch config + computeShippingCost helper"
      pattern: "computeShippingCost|shippingConfig"
    - from: "src/lib/orders.server.ts"
      to: "src/lib/admin/shipping-config.server.ts"
      via: "import getShippingConfig + computeShippingCost; sostituisce calc hardcoded riga 210"
      pattern: "computeShippingCost|getShippingConfig"
    - from: "src/routes/admin.tsx"
      to: "src/routes/admin.spedizione.tsx"
      via: "Voce NAV_ITEMS con icona Truck + label 'Spedizione' + matchPath /admin/spedizione"
      pattern: "/admin/spedizione"
---

<objective>
Aggiungere un pannello admin per configurare la spedizione (costo, soglia gratuita, toggle attiva) con persistenza DB tramite singleton `ShippingConfig`. Refactorare i 3 punti hardcoded (carrello, checkout, orders.server.ts) per leggere dal DB tramite un helper centralizzato `computeShippingCost`. Italiano in tutta la UI.

Purpose: Eliminare costanti `SHIPPING_COST = 7.9` / `FREE_SHIPPING_THRESHOLD = 99` sparse in 3 file e dare al gestore del negozio controllo runtime su costo/soglia/abilitazione spedizione, senza redeploy. Preserva il comportamento attuale via default seedati.

Output:
- Migrazione Prisma `shipping_config` con tabella singleton
- Server module `shipping-config.server.ts` con cache TTL 30s
- Route admin `/admin/spedizione` con form TanStack Form + zod
- 3 refactor che sostituiscono hardcode con lettura DB + helper
- Update `OrderSummary` per copy "Spedizione gratuita" quando disattivata
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@prisma/schema.prisma
@src/routes/admin.tsx
@src/routes/admin.prodotti.tsx
@src/routes/checkout.tsx
@src/routes/carrello.tsx
@src/components/checkout/OrderSummary.tsx
@src/lib/orders.server.ts
@src/lib/admin-functions.ts
@src/lib/validators/admin.ts

<interfaces>
<!-- Contratti chiave estratti dal codebase. L'executor usa questi pattern direttamente, non esplora. -->

## Auth guard pattern (already in repo)

```ts
// src/lib/admin-functions.ts (lines 38-45)
async function requireAdmin() {
  const user = await $getUser();
  if (!user || user.role !== "admin") {
    throw redirect({ to: "/auth/login" });
  }
  return user;
}
```

```ts
// src/routes/admin.tsx (lines 9-21) — route-level guard
async function adminGuard() {
  const { $getUser } = await import("~/lib/auth-functions");
  const user = await $getUser();
  if (!user || user.role !== "admin") throw redirect({ to: "/auth/login" });
  return user;
}
export const Route = createFileRoute("/admin")({ beforeLoad: adminGuard, component: AdminLayout });
```

## Server function pattern (createServerFn — already in repo)

```ts
// src/lib/admin-functions.ts pattern
export const $myFn = createServerFn({ method: "POST" })
  .inputValidator((data: { ... }) => data)
  .handler(async ({ data }) => {
    await requireAdmin();
    return doSomething(data);
  });
```

## Existing OrderSummary props (src/components/checkout/OrderSummary.tsx lines 15-23)

```ts
interface OrderSummaryProps {
  items: CartItemDetail[];
  subtotal: number;
  shippingCost: number;
  freeShippingThreshold: number;
  submitStatus?: "idle" | "loading" | "success" | "error";
  isCheckout?: boolean;
  disabled?: boolean;
  // NEW (Task 4): shippingEnabled?: boolean (default true)
}
```

## Hardcoded sites to refactor

```ts
// src/routes/checkout.tsx:10-11
const FREE_SHIPPING_THRESHOLD = 99;
const SHIPPING_COST = 7.9;
// :147
const shippingCost = cart.subtotal >= FREE_SHIPPING_THRESHOLD ? 0 : SHIPPING_COST;

// src/routes/carrello.tsx:16-17
const FREE_SHIPPING_THRESHOLD = 99;
const SHIPPING_COST = 7.9;
// :106
const shippingCost = cart.subtotal >= FREE_SHIPPING_THRESHOLD ? 0 : SHIPPING_COST;

// src/lib/orders.server.ts:210 (inside createOrder)
const shippingCost = subtotal >= 99 ? 0 : 7.9;
```

## Decimal convention (already used in Order, Product)

`Decimal @db.Decimal(10, 2)` — same precision as `Order.shippingCost` (line 417 schema.prisma).

## Existing TanStack Form usage (reference: src/components/auth/RegisterForm.tsx, ResetPasswordForm.tsx)

Pattern con `useForm({ defaultValues, validators: { onSubmit: zodSchema }, onSubmit: async ({ value }) => {...} })` + `<form.Field name="...">{(field) => ...}</form.Field>`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Schema ShippingConfig + server module + validator zod</name>
  <files>
    - prisma/schema.prisma
    - src/lib/validators/admin.ts
    - src/lib/admin/shipping-config.server.ts
    - src/lib/admin-functions.ts
  </files>
  <action>
**Step 1.1 — Prisma schema** (`prisma/schema.prisma`): aggiungere alla fine del file (dopo `AiSession` ~riga 602) il nuovo modello singleton:

```prisma
// ─── ShippingConfig (singleton) ────────────────────────────────────────
// Configurazione runtime della spedizione. Singolo record con id="default".
// Modificabile via /admin/spedizione. Default seedati al primo accesso da
// getShippingConfig() (upsert) per preservare comportamento attuale del sito.
model ShippingConfig {
  id            String   @id @default("default")
  cost          Decimal  @db.Decimal(10, 2) @default(7.90)
  freeThreshold Decimal  @db.Decimal(10, 2) @default(99.00)
  enabled       Boolean  @default(true)
  updatedAt     DateTime @updatedAt
  updatedById   String?

  @@map("shipping_config")
}
```

Poi: `pnpm db:migrate --name shipping_config` (genera migrazione + applica DB locale + rigenera Prisma client). Se prompt interattivo blocca, usare `pnpm prisma migrate dev --name shipping_config --create-only` per generare la migrazione senza applicarla, poi `pnpm prisma migrate deploy` o `pnpm db:push`. **Conferma `pnpm db:generate` ha aggiornato `node_modules/.prisma/client` con il tipo `ShippingConfig`.**

**Step 1.2 — Validator zod** (`src/lib/validators/admin.ts`): aggiungere in coda al file:

```ts
// ─── Admin Shipping Config ──────────────────────────────────────────────

export const updateShippingConfigSchema = z.object({
  cost: z.coerce
    .number({ invalid_type_error: "Inserisci un numero valido" })
    .min(0, "Il costo non può essere negativo")
    .max(9999.99, "Costo massimo €9999.99"),
  freeThreshold: z.coerce
    .number({ invalid_type_error: "Inserisci un numero valido" })
    .min(0, "La soglia non può essere negativa")
    .max(99999.99, "Soglia massima €99999.99"),
  enabled: z.boolean(),
});

export type UpdateShippingConfigInput = z.infer<typeof updateShippingConfigSchema>;
```

Nota: usiamo `z.coerce.number()` perché input `<input type="number">` può arrivare come stringa.

**Step 1.3 — Server module** (`src/lib/admin/shipping-config.server.ts`): creare nuovo file. Tetto LOC: 150.

```ts
/**
 * ShippingConfig Service — server-only
 *
 * Singleton config (id="default") per costo/soglia/abilitazione spedizione.
 * Cache in-memory con TTL 30s per ridurre query DB su traffico carrello/checkout.
 * Cache invalidata su update.
 */

import { prisma } from "~/lib/db.server";
import { createLogger } from "~/lib/logger.server";
import {
  updateShippingConfigSchema,
  type UpdateShippingConfigInput,
} from "~/lib/validators/admin";

const log = createLogger("shipping-config");

const SINGLETON_ID = "default";
const CACHE_TTL_MS = 30_000;

export interface ShippingConfigData {
  cost: number;
  freeThreshold: number;
  enabled: boolean;
  updatedAt: Date;
}

let cache: { data: ShippingConfigData; expiresAt: number } | null = null;

export function invalidateShippingConfigCache(): void {
  cache = null;
}

/**
 * Restituisce la config corrente. Se la riga non esiste (primo deploy),
 * la crea con i default schema (cost=7.90, freeThreshold=99.00, enabled=true)
 * preservando il comportamento attuale del sito.
 */
export async function getShippingConfig(): Promise<ShippingConfigData> {
  if (cache && cache.expiresAt > Date.now()) return cache.data;

  const row = await prisma.shippingConfig.upsert({
    where: { id: SINGLETON_ID },
    update: {},
    create: { id: SINGLETON_ID }, // i default sono nello schema
  });

  const data: ShippingConfigData = {
    cost: Number(row.cost),
    freeThreshold: Number(row.freeThreshold),
    enabled: row.enabled,
    updatedAt: row.updatedAt,
  };
  cache = { data, expiresAt: Date.now() + CACHE_TTL_MS };
  return data;
}

/**
 * Aggiorna la config. Valida via zod, upsert sul singleton, invalida cache.
 */
export async function updateShippingConfig(
  input: UpdateShippingConfigInput,
  userId: string,
): Promise<ShippingConfigData> {
  const parsed = updateShippingConfigSchema.parse(input);

  const row = await prisma.shippingConfig.upsert({
    where: { id: SINGLETON_ID },
    update: {
      cost: parsed.cost,
      freeThreshold: parsed.freeThreshold,
      enabled: parsed.enabled,
      updatedById: userId,
    },
    create: {
      id: SINGLETON_ID,
      cost: parsed.cost,
      freeThreshold: parsed.freeThreshold,
      enabled: parsed.enabled,
      updatedById: userId,
    },
  });

  invalidateShippingConfigCache();
  log.info("shipping_config.updated", { userId, ...parsed });

  return {
    cost: Number(row.cost),
    freeThreshold: Number(row.freeThreshold),
    enabled: row.enabled,
    updatedAt: row.updatedAt,
  };
}

/**
 * Helper centralizzato per il calcolo del costo di spedizione.
 * Regola: se enabled=false → 0; se subtotal >= freeThreshold → 0; altrimenti cost.
 *
 * Usato da carrello, checkout, e createOrder per evitare duplicazione.
 * Arrotonda a 2 decimali per evitare floating-point drift.
 */
export function computeShippingCost(
  subtotal: number,
  config: Pick<ShippingConfigData, "cost" | "freeThreshold" | "enabled">,
): number {
  if (!config.enabled) return 0;
  if (subtotal >= config.freeThreshold) return 0;
  return Math.round(config.cost * 100) / 100;
}
```

**Step 1.4 — Server functions** (`src/lib/admin-functions.ts`): aggiungere in coda al file (dopo l'ultima export esistente, prima di eventuale chiusura). Importare in cima con gli altri:

```ts
// in cima, con gli altri import
import {
  getShippingConfig,
  updateShippingConfig,
  type ShippingConfigData,
} from "./admin/shipping-config.server";
import type { UpdateShippingConfigInput } from "./validators/admin";

// re-export type
export type { ShippingConfigData };

// in coda, dopo le altre createServerFn
// ─── Shipping Config ──────────────────────────────────────────────

export const $getShippingConfig = createServerFn({ method: "GET" }).handler(async () => {
  await requireAdmin();
  return getShippingConfig();
});

export const $updateShippingConfig = createServerFn({ method: "POST" })
  .inputValidator((data: UpdateShippingConfigInput) => data)
  .handler(async ({ data }) => {
    const user = await requireAdmin();
    return updateShippingConfig(data, user.id);
  });
```

NOTA: `requireAdmin()` ritorna l'utente — verificalo dal codice esistente. Se non ritorna `user.id`, usa `(await requireAdmin()).id`.

**Decisioni implementative:**
- Decimal in DB, Number in app boundary (cast `Number(row.cost)`) — coerente con come `Order.shippingCost` è già letto altrove
- Cache module-level: in dev/HMR può causare staleness, ma TTL 30s è accettabile per /carrello e /checkout (piccolissimo traffico admin)
- `upsert` con `update: {}` in `getShippingConfig` = idempotente, garantisce auto-seed al primo accesso senza seed file separato
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm prisma format && grep -q "model ShippingConfig" prisma/schema.prisma && grep -q "updateShippingConfigSchema" src/lib/validators/admin.ts && grep -q "computeShippingCost" src/lib/admin/shipping-config.server.ts && grep -q "\\$getShippingConfig\\|\\$updateShippingConfig" src/lib/admin-functions.ts && ls prisma/migrations/*shipping_config*</automated>
  </verify>
  <done>
- Modello `ShippingConfig` presente in `schema.prisma` con id default "default", cost/freeThreshold Decimal(10,2), enabled Boolean
- Migrazione Prisma generata in `prisma/migrations/<timestamp>_shipping_config/migration.sql` e applicata
- `pnpm db:generate` rigenerato il client (tipo `ShippingConfig` disponibile)
- `updateShippingConfigSchema` esportato da `src/lib/validators/admin.ts` con messaggi italiani
- `src/lib/admin/shipping-config.server.ts` esporta `getShippingConfig`, `updateShippingConfig`, `computeShippingCost`, `invalidateShippingConfigCache`
- `src/lib/admin-functions.ts` esporta `$getShippingConfig` e `$updateShippingConfig` con `requireAdmin()` guard
- `pnpm typecheck` passa senza errori
- File server module ≤150 LOC
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Pannello admin /admin/spedizione + voce sidebar</name>
  <files>
    - src/routes/admin.spedizione.tsx
    - src/routes/admin.tsx
  </files>
  <action>
**Step 2.1 — Voce sidebar** (`src/routes/admin.tsx`): aggiungere import dell'icona `Truck` da lucide-react (è già usata in `checkout.tsx`, riusiamo la stessa). Aggiungere voce a `NAV_ITEMS` (riga ~23-32) tra "Resi" e "AI Advisor" o subito dopo "Ordini" — scegli posizione subito dopo "Ordini" (raggruppamento logico commerciale):

```ts
import { ..., Truck } from "lucide-react";

const NAV_ITEMS = [
  { label: "Dashboard", href: "/admin", icon: LayoutDashboard, matchPath: "/admin" as const, exact: true },
  { label: "Prodotti", href: "/admin/prodotti", icon: Package, matchPath: "/admin/prodotti" as const },
  { label: "Media", href: "/admin/media", icon: ImageIcon, matchPath: "/admin/media" as const },
  { label: "Variant templates", href: "/admin/variant-templates", icon: Layers, matchPath: "/admin/variant-templates" as const },
  { label: "Ordini", href: "/admin/ordini", icon: ShoppingCart, matchPath: "/admin/ordini" as const },
  { label: "Spedizione", href: "/admin/spedizione", icon: Truck, matchPath: "/admin/spedizione" as const },
  { label: "Resi", href: "/admin/resi", icon: RotateCcw, matchPath: "/admin/resi" as const },
  { label: "AI Advisor", href: "/admin/ai-advisor", icon: Sparkles, matchPath: "/admin/ai-advisor" as const },
  { label: "Consensi", href: "/admin/consensi", icon: Shield, matchPath: "/admin/consensi" as const },
] as const;
```

**Step 2.2 — Route admin** (`src/routes/admin.spedizione.tsx`): creare nuovo file. Tetto LOC: 200.

Pattern da seguire (TanStack Router file-based + TanStack Form + zod adapter, italiano, coerente con admin esistente):

```tsx
import { createFileRoute } from "@tanstack/react-router";
import { useForm } from "@tanstack/react-form";
import { zodValidator } from "@tanstack/zod-form-adapter";
import type { ReactNode } from "react";
import { useState } from "react";
import { Loader2, Save, Truck } from "lucide-react";
import { toast } from "sonner";
import {
  $getShippingConfig,
  $updateShippingConfig,
} from "~/lib/admin-functions";
import { updateShippingConfigSchema } from "~/lib/validators/admin";

export const Route = createFileRoute("/admin/spedizione")({
  beforeLoad: async () => {
    const config = await $getShippingConfig();
    return { initialConfig: config };
  },
  component: AdminShippingPage,
});

function AdminShippingPage(): ReactNode {
  const { initialConfig } = Route.useRouteContext();
  const [saving, setSaving] = useState(false);

  const form = useForm({
    defaultValues: {
      cost: initialConfig.cost,
      freeThreshold: initialConfig.freeThreshold,
      enabled: initialConfig.enabled,
    },
    validatorAdapter: zodValidator(),
    validators: { onSubmit: updateShippingConfigSchema },
    onSubmit: async ({ value }) => {
      setSaving(true);
      try {
        await $updateShippingConfig({ data: value });
        toast.success("Configurazione spedizione salvata");
      } catch (err) {
        toast.error(err instanceof Error ? err.message : "Errore durante il salvataggio");
      } finally {
        setSaving(false);
      }
    },
  });

  const inputClass =
    "h-10 w-full rounded-md border border-gray-300 bg-white px-3 text-sm text-gray-900 focus:border-[var(--color-primary)] focus:outline-none focus:ring-1 focus:ring-[var(--color-primary)] disabled:cursor-not-allowed disabled:opacity-60";
  const labelClass = "block text-sm font-medium text-gray-700";
  const helperClass = "text-xs text-gray-500";

  return (
    <div className="space-y-6">
      <div className="flex items-center gap-3">
        <Truck className="h-5 w-5 text-[var(--color-primary)]" />
        <span className="text-[10px] font-semibold uppercase tracking-widest text-gray-400">
          Configurazione spedizione
        </span>
      </div>

      <form
        onSubmit={(e) => {
          e.preventDefault();
          e.stopPropagation();
          void form.handleSubmit();
        }}
        className="max-w-2xl rounded-lg bg-white p-6 shadow-sm space-y-6"
      >
        <p className="text-sm text-gray-600">
          Configura il costo della spedizione, la soglia per la spedizione gratuita e l'attivazione globale.
          Le modifiche sono immediate su carrello e checkout.
        </p>

        {/* Toggle enabled */}
        <form.Field name="enabled">
          {(field) => (
            <div className="flex items-start justify-between gap-4 rounded-md border border-gray-200 p-4">
              <div className="space-y-1">
                <label htmlFor="ship-enabled" className={labelClass}>
                  Spedizione attiva
                </label>
                <p className={helperClass}>
                  Quando disattivata, la spedizione è gratuita per tutti gli ordini (€0,00) indipendentemente dalla soglia.
                </p>
              </div>
              <button
                id="ship-enabled"
                type="button"
                role="switch"
                aria-checked={field.state.value}
                onClick={() => field.handleChange(!field.state.value)}
                className={
                  "relative inline-flex h-6 w-11 shrink-0 cursor-pointer rounded-full transition-colors " +
                  (field.state.value ? "bg-[var(--color-primary)]" : "bg-gray-300")
                }
              >
                <span
                  className={
                    "inline-block h-5 w-5 translate-y-0.5 rounded-full bg-white shadow transition-transform " +
                    (field.state.value ? "translate-x-5" : "translate-x-0.5")
                  }
                />
              </button>
            </div>
          )}
        </form.Field>

        {/* cost */}
        <form.Field
          name="cost"
          validators={{
            onChange: ({ value }) => {
              if (value < 0) return "Il costo non può essere negativo";
              return undefined;
            },
          }}
        >
          {(field) => (
            <div className="space-y-2">
              <label htmlFor={field.name} className={labelClass}>
                Costo spedizione (€)
              </label>
              <input
                id={field.name}
                type="number"
                step="0.01"
                min="0"
                value={field.state.value}
                onChange={(e) => field.handleChange(Number(e.target.value))}
                onBlur={field.handleBlur}
                className={inputClass}
              />
              <p className={helperClass}>Costo applicato sotto soglia di spedizione gratuita.</p>
              {field.state.meta.errors.length > 0 && (
                <p className="text-xs text-red-600">{String(field.state.meta.errors[0])}</p>
              )}
            </div>
          )}
        </form.Field>

        {/* freeThreshold */}
        <form.Field
          name="freeThreshold"
          validators={{
            onChange: ({ value }) => {
              if (value < 0) return "La soglia non può essere negativa";
              return undefined;
            },
          }}
        >
          {(field) => (
            <div className="space-y-2">
              <label htmlFor={field.name} className={labelClass}>
                Soglia spedizione gratuita (€)
              </label>
              <input
                id={field.name}
                type="number"
                step="0.01"
                min="0"
                value={field.state.value}
                onChange={(e) => field.handleChange(Number(e.target.value))}
                onBlur={field.handleBlur}
                className={inputClass}
              />
              <p className={helperClass}>
                Per ordini di importo superiore o uguale a questa soglia, la spedizione è gratuita.
              </p>
              {field.state.meta.errors.length > 0 && (
                <p className="text-xs text-red-600">{String(field.state.meta.errors[0])}</p>
              )}
            </div>
          )}
        </form.Field>

        <div className="flex justify-end border-t border-gray-100 pt-4">
          <button
            type="submit"
            disabled={saving}
            className="inline-flex h-10 items-center gap-2 rounded-md bg-[var(--color-primary)] px-4 text-sm font-medium text-white transition-colors hover:bg-[var(--color-primary-dark)] disabled:cursor-not-allowed disabled:opacity-60"
          >
            {saving ? <Loader2 className="h-4 w-4 animate-spin" /> : <Save className="h-4 w-4" />}
            Salva configurazione
          </button>
        </div>
      </form>
    </div>
  );
}
```

**Decisioni implementative:**
- Auth guard ereditato da `/admin` parent route (`adminGuard` in `admin.tsx`) + difesa in profondità da `requireAdmin()` nelle server fn
- Switch custom button-based con `role="switch"` + `aria-checked` (accessibile, no shadcn/Radix Switch da installare per non gonfiare deps in quick-mode)
- TanStack Form pattern coerente con `auth/RegisterForm.tsx` esistente
- Toast italiano via sonner (già usato in admin.prodotti.tsx)
- Layout coerente con `/admin/prodotti/$id` (max-w-2xl, rounded-lg bg-white, shadow-sm)
- File ≤200 LOC come da CLAUDE.md
  </action>
  <verify>
    <automated>pnpm typecheck && grep -q "/admin/spedizione" src/routes/admin.tsx && test -f src/routes/admin.spedizione.tsx && grep -q "useForm\\|@tanstack/react-form" src/routes/admin.spedizione.tsx && grep -q "role=\"switch\"" src/routes/admin.spedizione.tsx && grep -q "Costo spedizione" src/routes/admin.spedizione.tsx && grep -q "Soglia spedizione gratuita" src/routes/admin.spedizione.tsx</automated>
  </verify>
  <done>
- `src/routes/admin.spedizione.tsx` esiste con form TanStack + 3 campi (cost, freeThreshold, enabled)
- Voce "Spedizione" presente in NAV_ITEMS di `src/routes/admin.tsx` con icona Truck
- Form usa zodValidator(updateShippingConfigSchema) + onSubmit chiama `$updateShippingConfig`
- Toggle enabled accessibile (role="switch" + aria-checked)
- Tutti i label/helper/toast in italiano
- File ≤200 LOC
- `pnpm typecheck` passa
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Refactor 3 punti hardcoded + OrderSummary copy disabled</name>
  <files>
    - src/components/checkout/OrderSummary.tsx
    - src/routes/carrello.tsx
    - src/routes/checkout.tsx
    - src/lib/orders.server.ts
  </files>
  <action>
**Step 3.1 — OrderSummary** (`src/components/checkout/OrderSummary.tsx`): aggiungere prop `shippingEnabled?: boolean` (default `true`). Quando `false`:
- Sopprime nudge "Aggiungi €X per la spedizione gratuita"
- Mostra "Spedizione gratuita" come riga spedizione (invece di "Gratis" o costo)

Modifica props interface (riga 15-23):

```ts
interface OrderSummaryProps {
  items: CartItemDetail[];
  subtotal: number;
  shippingCost: number;
  freeShippingThreshold: number;
  shippingEnabled?: boolean;  // NEW: default true
  submitStatus?: "idle" | "loading" | "success" | "error";
  isCheckout?: boolean;
  disabled?: boolean;
}
```

Destrutturazione (riga 25-28):

```ts
export function OrderSummary({
  items, subtotal, shippingCost, freeShippingThreshold,
  shippingEnabled = true,
  submitStatus = "idle", isCheckout = false, disabled = false,
}: OrderSummaryProps): ReactNode {
```

Sostituisci blocco riga 79-92 (riga "Spedizione" + nudge) con:

```tsx
<div className="flex justify-between text-sm">
  <span className="text-[var(--color-text-secondary)]">Spedizione</span>
  {!shippingEnabled ? (
    <span className="font-medium text-green-600">Spedizione gratuita</span>
  ) : shippingCost === 0 ? (
    <span className="font-medium text-green-600">Gratis</span>
  ) : (
    <span className="font-medium">€{shippingCost.toFixed(2)}</span>
  )}
</div>
{shippingEnabled && !isCheckout && subtotal < freeShippingThreshold && (
  <div className="flex items-center gap-2 rounded-[var(--radius-md)] bg-amber-50 px-3 py-2 text-xs text-amber-700">
    <Truck className="h-4 w-4 shrink-0" />
    <span>Aggiungi ancora €{(freeShippingThreshold - subtotal).toFixed(2)} per la spedizione gratuita</span>
  </div>
)}
```

**Step 3.2 — Carrello** (`src/routes/carrello.tsx`):

(a) Rimuovi costanti `FREE_SHIPPING_THRESHOLD = 99` e `SHIPPING_COST = 7.9` (riga 16-17).

(b) Importa il tipo config:
```ts
import type { ShippingConfigData } from "~/lib/admin-functions";
```

(c) Estendi `CartResult` o aggiungi state separato. Approccio più pulito: state separato.

```ts
const [cart, setCart] = useState<CartResult | null>(null);
const [shippingConfig, setShippingConfig] = useState<ShippingConfigData | null>(null);
```

(d) Nel `fetchCart` (o useEffect parallelo) carica anche la config via fetch a un nuovo endpoint pubblico `/api/shipping-config` OPPURE — più semplice — esponi una server function pubblica (no admin guard). **Decisione: usa fetch a un endpoint API pubblico read-only per coerenza con come `fetchCart` carica `/api/cart`.**

Crea endpoint API: questo richiede un piccolo file route. Approccio alternativo più snello: **esporta una server function pubblica `$getPublicShippingConfig`** (nessuna requireAdmin) da un nuovo file `src/lib/shipping-functions.ts` (o aggiungilo a un file esistente come `cart-session.ts`), così client+server riusano la cache TTL.

```ts
// src/lib/shipping-functions.ts (nuovo file ~15 LOC)
import { createServerFn } from "@tanstack/react-start";
import { getShippingConfig } from "~/lib/admin/shipping-config.server";

export const $getPublicShippingConfig = createServerFn({ method: "GET" }).handler(async () => {
  return getShippingConfig();
});
```

Usa nel carrello:
```ts
import { $getPublicShippingConfig } from "~/lib/shipping-functions";
import { computeShippingCost } from "~/lib/admin/shipping-config.server"; // ❌ NO — è .server, non importabile da client

// CORREZIONE: computeShippingCost è una funzione PURA senza side effect server.
// Sposta computeShippingCost in un file shared (non-.server) per usarla client-side.
```

**Decisione architetturale**: estrai `computeShippingCost` da `shipping-config.server.ts` a un file shared `src/lib/utils/shipping.ts` (puro, no DB import). Questo permette:
- Client (carrello, checkout) chiama `computeShippingCost(subtotal, config)` con config presa da server fn
- Server (orders.server.ts) idem, prende config da `getShippingConfig()` poi chiama helper

Modifica Task 1 retroattivamente: in `src/lib/admin/shipping-config.server.ts` mantieni `getShippingConfig`/`updateShippingConfig`/`invalidateShippingConfigCache`. Sposta `computeShippingCost` in nuovo file `src/lib/utils/shipping.ts`:

```ts
// src/lib/utils/shipping.ts (~20 LOC) — shared client/server, no .server import
export interface ShippingConfigShape {
  cost: number;
  freeThreshold: number;
  enabled: boolean;
}

export function computeShippingCost(
  subtotal: number,
  config: ShippingConfigShape,
): number {
  if (!config.enabled) return 0;
  if (subtotal >= config.freeThreshold) return 0;
  return Math.round(config.cost * 100) / 100;
}
```

E `shipping-config.server.ts` può re-esportare per backward compat:
```ts
export { computeShippingCost } from "~/lib/utils/shipping";
```

Ora il carrello:

```tsx
// src/routes/carrello.tsx
import { $getPublicShippingConfig } from "~/lib/shipping-functions";
import { computeShippingCost, type ShippingConfigShape } from "~/lib/utils/shipping";

// rimuovi const FREE_SHIPPING_THRESHOLD / SHIPPING_COST

const [cart, setCart] = useState<CartResult | null>(null);
const [shippingConfig, setShippingConfig] = useState<ShippingConfigShape | null>(null);

const fetchCart = useCallback(async () => {
  setLoading(true);
  try {
    const [cartRes, configRes] = await Promise.all([
      fetch("/api/cart").then((r) => r.json()),
      $getPublicShippingConfig().catch(() => null),
    ]);
    if (cartRes.ok) setCart(cartRes.data);
    if (configRes) setShippingConfig({
      cost: configRes.cost,
      freeThreshold: configRes.freeThreshold,
      enabled: configRes.enabled,
    });
  } catch { /* ignore */ }
  setLoading(false);
}, []);

// più sotto, sostituisci riga 106:
const shippingCost = shippingConfig ? computeShippingCost(cart.subtotal, shippingConfig) : 0;

// JSX OrderSummary (~riga 184-188): aggiungi shippingEnabled e leggi threshold da config
<OrderSummary
  items={cart.items}
  subtotal={cart.subtotal}
  shippingCost={shippingCost}
  freeShippingThreshold={shippingConfig?.freeThreshold ?? 99}
  shippingEnabled={shippingConfig?.enabled ?? true}
/>
```

**Step 3.3 — Checkout** (`src/routes/checkout.tsx`): identico pattern al carrello.

(a) Rimuovi `const FREE_SHIPPING_THRESHOLD = 99; const SHIPPING_COST = 7.9;` (riga 10-11)
(b) Importa `$getPublicShippingConfig` + `computeShippingCost` + tipo
(c) Aggiungi state `shippingConfig`
(d) Nel useEffect di mount, fetch config in parallelo a `/api/cart`
(e) Sostituisci riga 147 con `computeShippingCost(cart.subtotal, shippingConfig ?? defaultFallback)`
(f) Pass `shippingEnabled` e `freeShippingThreshold` dinamico a OrderSummary (riga 248-253)

Esempio:
```tsx
const shippingCost = shippingConfig
  ? computeShippingCost(cart.subtotal, shippingConfig)
  : 0;

<OrderSummary
  items={cart.items} subtotal={cart.subtotal} shippingCost={shippingCost}
  freeShippingThreshold={shippingConfig?.freeThreshold ?? 99}
  shippingEnabled={shippingConfig?.enabled ?? true}
  submitStatus={submitStatus} isCheckout
  disabled={!acceptedTerms}
/>
```

**Step 3.4 — orders.server.ts** (`src/lib/orders.server.ts`): refactor `createOrder` (~riga 210).

(a) Importa in cima:
```ts
import { getShippingConfig } from "~/lib/admin/shipping-config.server";
import { computeShippingCost } from "~/lib/utils/shipping";
```

(b) Sostituisci riga 210 (`const shippingCost = subtotal >= 99 ? 0 : 7.9;`) con:
```ts
const shippingConfig = await getShippingConfig();
const shippingCost = computeShippingCost(subtotal, shippingConfig);
```

(c) Lascia tutto il resto invariato. `total = netAfterDiscount + shippingCost` resterà coerente.

**Verifica regressione**: con config default (cost=7.90, freeThreshold=99, enabled=true) il comportamento è IDENTICO al precedente hardcode. Test manuale: aggiungi prodotto < €99 → shippingCost = 7.90; aggiungi prodotti ≥ €99 → 0; spegni toggle in admin → 0 sempre.

**Decisioni implementative:**
- `computeShippingCost` spostato in `src/lib/utils/shipping.ts` (puro, shared) per evitare il problema "non puoi importare .server da client"
- `$getPublicShippingConfig` in nuovo file `src/lib/shipping-functions.ts` senza auth guard (è pubblico — è solo costo/soglia)
- Fallback `?? 99` / `?? true` solo durante loading iniziale (race UI); il server (`orders.server.ts`) ha sempre config affidabile
  </action>
  <verify>
    <automated>pnpm typecheck && pnpm lint && ! grep -E "FREE_SHIPPING_THRESHOLD\s*=\s*99|SHIPPING_COST\s*=\s*7\.9" src/routes/checkout.tsx src/routes/carrello.tsx && ! grep -E "subtotal\s*>=\s*99\s*\?\s*0\s*:\s*7\.9" src/lib/orders.server.ts && grep -q "shippingEnabled" src/components/checkout/OrderSummary.tsx && grep -q "computeShippingCost" src/lib/orders.server.ts src/routes/checkout.tsx src/routes/carrello.tsx && test -f src/lib/utils/shipping.ts && test -f src/lib/shipping-functions.ts</automated>
  </verify>
  <done>
- Costanti `FREE_SHIPPING_THRESHOLD`/`SHIPPING_COST` rimosse da `checkout.tsx` e `carrello.tsx`
- Linea hardcoded `subtotal >= 99 ? 0 : 7.9` rimossa da `orders.server.ts`
- `src/lib/utils/shipping.ts` esiste con `computeShippingCost` puro
- `src/lib/shipping-functions.ts` esiste con `$getPublicShippingConfig` senza auth guard
- `OrderSummary` accetta `shippingEnabled?: boolean` e mostra "Spedizione gratuita" quando false (sopprime nudge)
- carrello e checkout fetchano config in parallelo con cart e passano `shippingEnabled` + `freeShippingThreshold` dinamico a OrderSummary
- `createOrder` in `orders.server.ts` legge config DB e usa `computeShippingCost`
- `pnpm typecheck && pnpm lint` passa
- Comportamento di default identico al precedente (cost=7.90, threshold=99, enabled=true)
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verifica end-to-end pannello + carrello + checkout + ordine</name>
  <what-built>
- Modello DB `ShippingConfig` con migrazione Prisma
- Server module + helper centralizzato + cache TTL 30s
- Pannello admin `/admin/spedizione` con TanStack Form (cost, freeThreshold, switch enabled)
- Voce "Spedizione" nella sidebar admin con icona Truck
- Refactor di carrello, checkout, orders.server.ts che ora leggono dal DB
- OrderSummary mostra "Spedizione gratuita" quando enabled=false e sopprime nudge
  </what-built>
  <how-to-verify>
1. **Build & runtime**: `pnpm dev` (o equivalente), nessun errore in console o overlay React.

2. **Admin panel**:
   - Login come utente admin → vai a `/admin`
   - Verifica voce "Spedizione" presente nella sidebar (icona Truck) tra "Ordini" e "Resi"
   - Click su "Spedizione" → carica `/admin/spedizione`, form mostra valori correnti (default: cost=7.90, soglia=99.00, attiva=ON)
   - Cambia cost a 9.50, soglia a 120, click "Salva configurazione" → toast verde "Configurazione spedizione salvata"
   - Reload pagina → valori persistono (DB OK)

3. **Validation italiana**:
   - Inserisci cost = -1 → errore "Il costo non può essere negativo", form non si invia
   - Inserisci freeThreshold = -1 → errore "La soglia non può essere negativa"

4. **Carrello con enabled=true**:
   - Apri pagina prodotto, aggiungi 1 articolo da ~€50 → vai a `/carrello`
   - Verifica "Spedizione: €9.50" (i nuovi valori salvati al passo 2) e nudge "Aggiungi ancora €X per la spedizione gratuita" con €X = 120 - subtotal
   - Aumenta quantità o aggiungi prodotti fino a superare €120 → "Spedizione: Gratis" (no nudge)

5. **Checkout con enabled=true**:
   - Click "Procedi al checkout" → `/checkout`
   - Riepilogo ordine mostra stessi valori del carrello (Spedizione e Totale coerenti)

6. **Disabilita spedizione**:
   - Torna a `/admin/spedizione`, toggle su OFF, salva → toast OK
   - Vai a `/carrello` (anche con un solo prodotto economico) → riga spedizione mostra "Spedizione gratuita" (verde), nessun nudge "aggiungi €X"
   - Vai a `/checkout` → idem, totale = subtotale (no spedizione aggiunta)

7. **Persistenza ordine**:
   - Con enabled=false, completa un ordine fittizio (Stripe test) o ferma prima del redirect Stripe e ispeziona DB:
   - `pnpm prisma studio` → tabella `Order` → ultimo ordine ha `shippingCost = 0.00`
   - Riattiva spedizione (enabled=true), cost=7.90, soglia=99, fai un altro ordine sotto soglia → DB `Order.shippingCost = 7.90`

8. **Auth guard**:
   - Logout → tentativo di accedere a `/admin/spedizione` → redirect a `/auth/login`
   - Login come utente non-admin → idem redirect

9. **Cache TTL**:
   - Cambia cost in admin → ricarica `/carrello` entro 30s → potresti vedere ancora il vecchio valore (cache TTL); ricarica dopo 30s o riavvia dev server → nuovo valore appare. Nota: in dev `pnpm dev` HMR può azzerare la cache automaticamente.

**Comandi build finali (CI-equivalent):**
```bash
pnpm typecheck
pnpm lint
pnpm build
```
Tutti devono passare senza errori.
  </how-to-verify>
  <resume-signal>Type "approvato" or describe issues</resume-signal>
</task>

</tasks>

<verification>

## Comandi automatici (riepilogo phase)

```bash
# 1. Schema + migrazione
grep -q "model ShippingConfig" prisma/schema.prisma
ls prisma/migrations/*shipping_config*

# 2. Server module + validator
test -f src/lib/admin/shipping-config.server.ts
test -f src/lib/utils/shipping.ts
test -f src/lib/shipping-functions.ts
grep -q "updateShippingConfigSchema" src/lib/validators/admin.ts
grep -q "computeShippingCost" src/lib/utils/shipping.ts

# 3. Server functions auth-guarded
grep -q "\$getShippingConfig" src/lib/admin-functions.ts
grep -q "\$updateShippingConfig" src/lib/admin-functions.ts

# 4. Admin route + sidebar
test -f src/routes/admin.spedizione.tsx
grep -q "/admin/spedizione" src/routes/admin.tsx
grep -q "useForm" src/routes/admin.spedizione.tsx
grep -q "role=\"switch\"" src/routes/admin.spedizione.tsx

# 5. Refactor — niente più hardcode
! grep -E "FREE_SHIPPING_THRESHOLD\s*=\s*99|SHIPPING_COST\s*=\s*7\.9" src/routes/checkout.tsx src/routes/carrello.tsx
! grep -E "subtotal\s*>=\s*99\s*\?\s*0\s*:\s*7\.9" src/lib/orders.server.ts

# 6. Wiring corretto
grep -q "computeShippingCost" src/lib/orders.server.ts src/routes/checkout.tsx src/routes/carrello.tsx
grep -q "shippingEnabled" src/components/checkout/OrderSummary.tsx

# 7. Build gates
pnpm typecheck
pnpm lint
pnpm build
```

</verification>

<success_criteria>

- [ ] Modello `ShippingConfig` in `schema.prisma` (id="default", Decimal(10,2), enabled boolean) + migrazione applicata
- [ ] Default (cost=7.90, threshold=99, enabled=true) auto-seedati via `upsert` al primo `getShippingConfig()`
- [ ] Server module `src/lib/admin/shipping-config.server.ts` con cache TTL 30s
- [ ] Helper puro `src/lib/utils/shipping.ts` con `computeShippingCost`
- [ ] Server fn `$getShippingConfig`/`$updateShippingConfig` con `requireAdmin()`
- [ ] Server fn pubblica `$getPublicShippingConfig` (no guard) per consumo client
- [ ] Validator zod `updateShippingConfigSchema` con messaggi italiani (cost≥0, threshold≥0)
- [ ] Route `/admin/spedizione` con form TanStack + zod adapter + 3 campi + toggle accessibile + toast italiano
- [ ] Voce "Spedizione" + icona Truck in NAV_ITEMS di `admin.tsx`
- [ ] Carrello e checkout fetchano config in parallelo con cart e usano `computeShippingCost`
- [ ] `createOrder` in `orders.server.ts` legge config DB (linea hardcoded `subtotal >= 99 ? 0 : 7.9` rimossa)
- [ ] OrderSummary accetta prop `shippingEnabled` (default true), quando false mostra "Spedizione gratuita" e sopprime nudge
- [ ] `pnpm typecheck && pnpm lint && pnpm build` pass
- [ ] Component admin route ≤200 LOC, server module ≤150 LOC
- [ ] Tutta la copy in italiano (label, helper, toast, errori validation)
- [ ] Auth guard verificato: non-admin redirect a `/auth/login`
- [ ] Default behavior preservato: con config default valori carrello/checkout/ordine identici a prima del refactor

</success_criteria>

<output>
Dopo completamento, crea `.planning/quick/260508-n1f-pannello-admin-configurazione-spedizione/260508-n1f-SUMMARY.md` riassumendo:
- Migrazione Prisma applicata (path migration.sql)
- File creati e modificati
- Decisioni architetturali (Decimal, cache TTL, helper puro shared, switch custom no-shadcn)
- Comportamento preservato vs nuovo (toggle enabled=false → 0€ + copy)
- Eventuali gotcha (es. cache TTL può mostrare valori stale per 30s)
</output>
